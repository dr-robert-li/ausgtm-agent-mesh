{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "google_drive_search.input",
  "description": "Input for the Google Drive search read tool. READ-category: no approval required.",
  "type": "object",
  "x-tool-category": "read",
  "x-approval-required": false,
  "additionalProperties": false,
  "required": ["query"],
  "properties": {
    "query": { "type": "string", "minLength": 1, "description": "Full-text search query." },
    "max_results": { "type": "integer", "minimum": 1, "maximum": 50, "default": 10 },
    "drive_id": { "type": "string", "description": "Optional shared-drive id to scope the search." }
  }
}
