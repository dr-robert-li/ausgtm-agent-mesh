{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "beehiiv_create_post.input",
  "description": "Input for the Beehiiv create-post publishing tool. WRITE/publishing-category: approval required. title is required; status defaults to draft so posts never auto-publish.",
  "type": "object",
  "x-tool-category": "publishing",
  "x-approval-required": true,
  "additionalProperties": true,
  "required": ["title"],
  "properties": {
    "title": { "type": "string", "minLength": 1, "description": "Post title." },
    "status": {
      "type": "string",
      "enum": ["draft", "confirmed"],
      "default": "draft",
      "description": "Publish state; defaults to draft so posts are never auto-published."
    },
    "subtitle": { "type": "string" },
    "body_content": { "type": "string" }
  }
}
