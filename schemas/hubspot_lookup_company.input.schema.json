{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "hubspot_lookup_company.input",
  "description": "Input for the HubSpot company/contact lookup read tool. READ-category: no approval required.",
  "type": "object",
  "x-tool-category": "read",
  "x-approval-required": false,
  "additionalProperties": false,
  "required": ["object_type", "query"],
  "properties": {
    "object_type": { "type": "string", "enum": ["companies", "contacts"] },
    "query": { "type": "string", "minLength": 1, "description": "Name, domain, or email to look up." },
    "limit": { "type": "integer", "minimum": 1, "maximum": 25, "default": 5 }
  }
}
