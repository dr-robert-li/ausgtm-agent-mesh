---
phase: 04-tool-gateway-framework-first-adapters-aggregators
plan: 09
type: execute
wave: 6
depends_on: ["04-05", "04-06", "04-07", "04-08"]
files_modified:
  - docs/credentials/README.md
  - RUNBOOK.md
  - tests/test_credential_docs.py
autonomous: true
requirements: [TOOL-01, TOOL-04]

must_haves:
  truths:
    - "A single discoverable per-provider credential/scope setup index exists covering HubSpot, Google Workspace (all six products), Composio, and Nango (D-12)"
    - "Every live-lane env var the adapters read is documented with how to mint it and the exact scopes/setup (D-12)"
    - "The live-lane opt-in matrix (each provider independently skippable on its own creds) is documented (D-11)"
  artifacts:
    - path: "docs/credentials/README.md"
      provides: "per-provider credential/scope setup index (D-12 deliverable)"
      contains: "HUBSPOT_PRIVATE_APP_TOKEN"
    - path: "tests/test_credential_docs.py"
      provides: "guard: every adapter env var name appears in the docs (D-12 completeness)"
      contains: "GOOGLE_WORKSPACE_OAUTH"
  key_links:
    - from: "docs/credentials/README.md"
      to: "docs/credentials/{hubspot,google_workspace,composio,nango}.md"
      via: "index links to each per-provider doc"
      pattern: "hubspot.md"
    - from: "tests/test_credential_docs.py"
      to: "the documented env var names"
      via: "assert each live-lane env var is documented"
      pattern: "COMPOSIO_API_KEY"
---

<objective>
Consolidate the per-provider credential/scope setup docs (D-12 deliverable) into a single
discoverable index and add a completeness guard. Each adapter plan (04-05/06/07/08) ships its
own provider doc (`docs/credentials/{hubspot,google_workspace,composio,nango}.md`); this plan
ties them together with `docs/credentials/README.md` (the index the user reads to supply creds
for the live lane), records the per-provider opt-in live-lane matrix (D-11), links it from
RUNBOOK.md, and adds a test asserting every live-lane env var the adapters actually read is
documented so the deliverable can't silently drift.

Depends on all four adapter plans so every per-provider doc exists before the index references
them.

Purpose: make D-12 a real, complete, drift-guarded deliverable — the thing that lets the user
mint and supply creds for each independently skippable live lane.
Output: the credential index, a RUNBOOK link, and a doc-completeness test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-CONTEXT.md
@.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-RESEARCH.md
@.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-PATTERNS.md

<interfaces>
<!-- per-provider docs shipped by adapter plans:
     docs/credentials/hubspot.md (HUBSPOT_PRIVATE_APP_TOKEN + scopes + sandbox) — 04-05
     docs/credentials/google_workspace.md (GOOGLE_WORKSPACE_OAUTH blob + all six products' scopes) — 04-06/07
     docs/credentials/composio.md (COMPOSIO_API_KEY + managed-auth) — 04-08
     docs/credentials/nango.md (NANGO_SECRET_KEY/NANGO_HOST/NANGO_CONNECTION_ID/NANGO_PROVIDER_CONFIG_KEY + self-host) — 04-08 -->
<!-- live-lane env vars to enumerate (RESEARCH Runtime State Inventory):
     HUBSPOT_PRIVATE_APP_TOKEN, GOOGLE_WORKSPACE_OAUTH, COMPOSIO_API_KEY,
     NANGO_SECRET_KEY, NANGO_HOST, NANGO_CONNECTION_ID, NANGO_PROVIDER_CONFIG_KEY -->
<!-- RUNBOOK.md exists at repo root (local smoke + deployment) — add a credentials link -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credential index + live-lane matrix + RUNBOOK link + completeness guard</name>
  <files>docs/credentials/README.md, RUNBOOK.md, tests/test_credential_docs.py</files>
  <read_first>
    - docs/credentials/hubspot.md (04-05), docs/credentials/google_workspace.md (04-06/07), docs/credentials/composio.md + docs/credentials/nango.md (04-08) — the per-provider docs this index links + summarizes
    - 04-RESEARCH.md "Runtime State Inventory" (the exact live-lane env var names) + "Validation Architecture / Requirements -> Test Map" (the per-provider live-lane skip pattern, D-11)
    - 04-CONTEXT.md D-11 (per-provider opt-in matrix) + D-12 (the deliverable definition)
    - RUNBOOK.md (repo root — where to add the credentials section link; match its existing heading style)
  </read_first>
  <action>
    Write `docs/credentials/README.md` as the index: a one-line intro that the default lane is creds-free and live calls are per-provider opt-in (D-11); a table mapping each provider (HubSpot, Google Workspace, Composio, Nango) to its env var(s), its category of operations (read / approval-gated write), a link to its per-provider doc, and the marker that absence of its creds simply skips that provider's live tests. Enumerate EVERY live-lane env var: `HUBSPOT_PRIVATE_APP_TOKEN`, `GOOGLE_WORKSPACE_OAUTH`, `COMPOSIO_API_KEY`, `NANGO_SECRET_KEY`, `NANGO_HOST`, `NANGO_CONNECTION_ID`, `NANGO_PROVIDER_CONFIG_KEY`. State how to run the live lane (`make test-live` / `pytest -m live`) and that each provider's tests skip independently when its creds are absent. Add a short "Credentials & live lane" section to `RUNBOOK.md` linking `docs/credentials/README.md`. Author `tests/test_credential_docs.py` (default lane): assert each of the seven env var names appears in `docs/credentials/README.md`, and assert each of the four per-provider docs exists and is non-empty. This guards D-12 completeness so a future adapter env var can't be added without documenting it.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest -q -m "not live" tests/test_credential_docs.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HUBSPOT_PRIVATE_APP_TOKEN" docs/credentials/README.md` >= 1 and all seven env var names appear in the index
    - The index links all four per-provider docs (hubspot.md, google_workspace.md, composio.md, nango.md)
    - `grep -ci "credential" RUNBOOK.md` >= 1 (RUNBOOK links the credentials index)
    - `tests/test_credential_docs.py` asserts presence of all seven env var names AND existence of all four per-provider docs
    - `PYTHONPATH=src python -m pytest -q -m "not live"` exits 0
  </acceptance_criteria>
  <done>A single credential/scope setup index covers all four providers and every live-lane env var, the live-lane opt-in matrix is documented, RUNBOOK links it, and a test guards completeness — D-12 delivered and drift-proofed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> operator | the index tells a human how to mint high-value secrets; must not encourage insecure handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Information disclosure | the docs embedding real tokens/secrets as examples | mitigate | the index uses env-var NAMES and placeholder instructions only; no real secret values are committed; a reviewer check + the completeness test reference names, never values |
| T-04-09-02 | Repudiation | an undocumented live-lane env var silently added later | mitigate | tests/test_credential_docs.py asserts every live-lane env var name is documented (drift guard) |

No high-severity threat: this plan ships documentation + a guard test only; no execution path, no credential handling.
</threat_model>

<verification>
- `make test` exits 0 with the doc-completeness guard
- The credential index lists all seven env vars and links all four per-provider docs
- RUNBOOK links the credentials index
</verification>

<success_criteria>
A complete, discoverable, drift-guarded per-provider credential/scope setup index (D-12)
covering HubSpot, Google Workspace (six products), Composio, and Nango, with the per-provider
opt-in live-lane matrix (D-11) documented and linked from RUNBOOK.
</success_criteria>

<output>
Create `.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-09-SUMMARY.md` when done
</output>
