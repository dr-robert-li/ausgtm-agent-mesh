---
phase: 04-tool-gateway-framework-first-adapters-aggregators
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-03"]
files_modified:
  - src/agent_mesh/worker/orchestrator.py
  - src/agent_mesh/worker/graph.py
  - src/agent_mesh/worker/runner.py
  - src/agent_mesh/worker/main.py
  - tests/test_read_path.py
autonomous: true
requirements: [TOOL-01]

must_haves:
  truths:
    - "Proposed reads execute UNGATED immediately post-run, recording category=read ToolCalls and feeding evidence (audit item A)"
    - "The read path NEVER routes through approvals.is_approved() — reads are not a write-without-approval hole (security invariant)"
    - "Write-class proposals stay gated through the existing payload-hash ledger, unchanged (SEC-01/SEC-02 not weakened)"
    - "The worker boots with a real ToolGateway.from_manifest(...) + CredentialResolver injected (audit item B)"
    - "make test stays green with the gateway-None stub fallback still recording deterministic read rows (D-11)"
  artifacts:
    - path: "src/agent_mesh/worker/orchestrator.py"
      provides: "OrchestrationResult.proposed_reads + _run_stub deterministic read (item A)"
      contains: "proposed_reads"
    - path: "src/agent_mesh/worker/runner.py"
      provides: "ungated post-run read execution loop + execute(call) call-site update"
      contains: "proposed_reads"
    - path: "src/agent_mesh/worker/main.py"
      provides: "Worker(tool_gateway=ToolGateway.from_manifest(...)) + resolver (item B)"
      contains: "from_manifest"
  key_links:
    - from: "worker/runner.py read loop"
      to: "tools/gateway.execute(call, resolver=...)"
      via: "ungated immediate execution, no approval ledger"
      pattern: "execute\\("
    - from: "worker/graph.py researcher_node"
      to: "MeshState.proposed_reads"
      via: "researcher emits reads (heuristic in stub lane)"
      pattern: "proposed_reads"
    - from: "worker/runner.py read loop"
      to: "repository.upsert_tool_call (tenant-scoped)"
      via: "record category=read, is_read=True rows (DUR-02)"
      pattern: "upsert_tool_call"
---

<objective>
Add the missing read-execution seam (audit item A — the plan-shaping gap: there is NO
executed-read path today; `OrchestrationResult` carries only `proposed_writes` + `evidence`
and `runner.py` executes only writes after approval). HubSpot lookup, Drive search, and both
aggregator reads are READS, so without this seam TOOL-01/TOOL-04 cannot run end-to-end.

Implements RESEARCH "Option 1 — post-run ungated reads mirroring the write path":
- **OrchestrationResult.proposed_reads** (item A) — same `{tool_name, category, parameters}`
  shape as proposed_writes; `_run_stub` emits a deterministic read (e.g. hubspot_lookup_company
  / google_drive_search) so the default lane exercises the path creds-free.
- **researcher_node emits proposed_reads** (item A/C) into MeshState (heuristic in the stub
  lane); replace the hardcoded `hubspot_create_deal` stub write with plan-derived selection
  while keeping the stub path green (item C).
- **runner.py ungated read loop** — after `run_mesh`, BEFORE the write-gate branch, iterate
  `result.proposed_reads`, build `ToolCall(category=READ, approval_required=False, is_read=True)`,
  call `self._execute(call)` IMMEDIATELY and UNGATED (no approval ledger), persist tenant-scoped,
  append a string summary to `evidence`. Update `_execute` to pass the `ToolCall` to the new
  `gateway.execute(call, resolver=...)` signature (04-03).
- **main.py gateway injection** (item B) — build `Worker(tool_gateway=ToolGateway.from_manifest(...))`
  + a `CredentialResolver` in both bootstrap paths.

Two real-path wrinkles the executor MUST handle (RESEARCH/PATTERNS flags):
- On `_run_langgraph`, `proposed_writes` are pulled from `__interrupt__[0].value`; reads are
  accumulated channel state, NOT in the interrupt payload. Confirm `compiled.invoke` returns
  `proposed_reads` alongside `__interrupt__`, or surface reads in BOTH branches / execute reads
  before the write gate. (Stub lane has no interrupt, so the green suite holds regardless.)
- `OrchestrationResult.evidence` is `list[str]`; read results are dicts — append a STRING
  summary/source-pointer, not the raw dict.

Purpose: the executed-read path TOOL-01/TOOL-04 require, ungated and security-clean.
Output: read seam across worker/ + the gateway wired into the live worker + a default-lane
read-path test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-CONTEXT.md
@.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-RESEARCH.md
@.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-PATTERNS.md

<interfaces>
<!-- OrchestrationResult (orchestrator.py:128-135): summary, proposed_writes:list[dict],
     evidence:list[str], trace_id. ADD proposed_reads:list[dict]=field(default_factory=list) -->
<!-- _run_stub (orchestrator.py:261-281): write-trigger heuristic emits proposed_writes; mirror for reads -->
<!-- runner.process write loop (runner.py:74-113); runner._execute (runner.py:166-173):
     today self._gateway.execute(call.tool_name, call.parameters); UPDATE to execute(call, resolver=...) -->
<!-- runner._pending_calls (runner.py:161-164) tenant-scoped list_tool_calls -->
<!-- graph.py MeshState (graph.py:46-66) + researcher_node (graph.py:226-236) + reviewer_node WRITE heuristic (graph.py:252-285) -->
<!-- graph.py reviewer hardcodes {"tool_name":"hubspot_create_deal",...} — item C: keep heuristic, make read+write plan-derived but stub-green -->
<!-- main.py Worker() ctor (main.py:31 _run_pubsub, main.py:54 _run_inprocess) both tool_gateway=None today -->
<!-- ToolGateway.from_manifest(path) exists (gateway.py:64-66); manifest path: manifests/tool_pack_manifest.yaml -->
<!-- ToolCall now has is_read (04-01); ToolCategory.READ (enums.py:47) -->
<!-- SECURITY: approvals.is_approved (approvals.py) is the WRITE gate ONLY; reads MUST NOT touch it -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: proposed_reads on OrchestrationResult + graph emission (items A/C)</name>
  <files>src/agent_mesh/worker/orchestrator.py, src/agent_mesh/worker/graph.py, tests/test_read_path.py</files>
  <read_first>
    - src/agent_mesh/worker/orchestrator.py:128-135 (OrchestrationResult dataclass), :261-281 (_run_stub write heuristic to mirror), :305-352 (_run_langgraph — the __interrupt__ vs accumulated-state wrinkle at :341-344)
    - src/agent_mesh/worker/graph.py:46-66 (MeshState), :226-236 (researcher_node), :252-285 (reviewer_node WRITE_TRIGGERS heuristic + the hardcoded hubspot_create_deal write — item C)
    - 04-RESEARCH.md "Recommended read-execution seam (audit item A)" (the real-graph wrinkle + evidence-is-list[str] notes) — verbatim
    - 04-PATTERNS.md "worker/orchestrator.py" + "worker/graph.py" sections (extend-in-place analogs)
  </read_first>
  <behavior>
    - Test: OrchestrationResult defaults proposed_reads to [] and accepts a list of {tool_name, category, parameters} dicts.
    - Test (stub lane): _run_stub on a prompt emits at least one deterministic proposed_reads entry (a read tool that declares schemas, e.g. hubspot_lookup_company or google_drive_search) so the default lane exercises the read path creds-free.
    - Test (item C): _run_stub / reviewer write proposal is plan-derived but the stub lane still produces the same gated write behavior for a write-trigger prompt (existing write-gate tests stay green).
  </behavior>
  <action>
    Add `proposed_reads: list[dict] = field(default_factory=list)` to `OrchestrationResult` (same shape as proposed_writes). In `_run_stub`, after the write heuristic, append a deterministic `proposed_reads` entry referencing a read tool that DECLARES schemas (e.g. `{"tool_name":"hubspot_lookup_company","category":"read","parameters":{"object_type":"companies","query":task.prompt[:60]}}`) so the default lane runs the read path. Add `proposed_reads: list[dict]` to `MeshState`; have `researcher_node` emit a heuristic `proposed_reads` (mirroring how `reviewer_node` derives `proposed_writes`), keeping the existing role-distinct `research` output. For item C, replace the hardcoded write in `reviewer_node` with plan-derived selection that still resolves to a deterministic, schema-valid write in the stub lane for write-trigger prompts (do NOT soften the approval path — proposed_writes stays heuristic-derived per the RF-1 comment at graph.py:274-280). In `_run_langgraph`, surface `proposed_reads` from the final state in BOTH the interrupt and no-interrupt branches (the wrinkle): on the no-interrupt branch read `final_state.get("proposed_reads", [])`; on the interrupt branch ALSO pull accumulated `proposed_reads` from `final_state.get("proposed_reads", [])` (it is channel state, not in the interrupt value) so reads are never lost when a write pauses the run. Author `tests/test_read_path.py` Task-1 cases.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest -q -m "not live" tests/test_read_path.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "proposed_reads" src/agent_mesh/worker/orchestrator.py` >= 3 (dataclass field + _run_stub + _run_langgraph both branches)
    - `grep -c "proposed_reads" src/agent_mesh/worker/graph.py` >= 2 (MeshState + researcher_node)
    - A test asserts _run_stub emits a proposed_reads entry whose tool_name is a schema-declaring read tool
    - Existing write-gate / approval tests still pass (item C did not soften the write path)
  </acceptance_criteria>
  <done>OrchestrationResult carries proposed_reads, _run_stub + researcher_node emit a deterministic read in the stub lane, the real-graph path surfaces reads in both interrupt branches, and the write path is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ungated post-run read execution + execute(call) call-site + worker gateway injection (items A/B)</name>
  <files>src/agent_mesh/worker/runner.py, src/agent_mesh/worker/main.py, tests/test_read_path.py</files>
  <read_first>
    - src/agent_mesh/worker/runner.py:41-113 (process: RUNNING transition, write loop to MIRROR ungated), :135-164 (resume write-exec under is_approved — the gate reads must NEVER touch), :166-173 (_execute + gateway-None stub fallback — update the execute() call site to pass `call`)
    - src/agent_mesh/worker/main.py:22-67 (both Worker() ctor sites — inject ToolGateway.from_manifest + resolver)
    - src/agent_mesh/tools/gateway.py (04-03 execute signature: execute(call, *, resolver)) and from_manifest:64-66
    - src/agent_mesh/tools/credentials.py (04-03 EnvCredentialResolver) — the resolver to inject
    - src/agent_mesh/services/approvals.py:159-193 + is_approved (the WRITE gate — reads stay clear of it)
    - 04-PATTERNS.md "worker/runner.py — THE read-seam" (mirror the write loop UNGATED) + "worker/main.py — inject the real gateway"
  </read_first>
  <behavior>
    - Test (item A): with a stub gateway (or gateway=None), processing a task whose result has proposed_reads records a category=read, is_read=True ToolCall in EXECUTED status, tenant-scoped, and appends a string summary to evidence — with NO approval record created for the read.
    - Test (security): the read execution path does not call approvals.is_approved / build_approval_request / open_approval (assert via spy/mock that no approval is opened for a read).
    - Test (item B): main bootstrap builds a Worker whose _gateway is a real ToolGateway loaded from the manifest (not None) and a resolver is present.
    - Test (write unchanged): a write-trigger task still parks in AWAITING_APPROVAL with an approval opened (existing behavior intact).
  </behavior>
  <action>
    In `runner.process`, AFTER `run_mesh` returns and the governed-halt re-check, BEFORE the `if not result.proposed_writes` write-gate branch, add an ungated read loop: for each `proposed` in `result.proposed_reads`, build `ToolCall(task_id=..., tenant_id=task.tenant_id, tool_name=proposed["tool_name"], category=ToolCategory.READ, approval_required=False, is_read=True, status=ToolCallStatus.EXECUTED, parameters=proposed.get("parameters",{}), requester_id=task.requester.requester_id)`, call `result_dict = self._execute(call)` IMMEDIATELY (no approval ledger — reads are ungated), set the call's `result=result_dict` and `schema_validation` outcome if present, `upsert_tool_call` (tenant-scoped), and append a STRING summary (e.g. `f"read {call.tool_name}: {len(result_dict)} field(s)"` or a source pointer) to a local evidence list that is persisted with the result summary. The read loop MUST NOT call `approvals.*` — it is the inverse of the write loop. Update `_execute` to call `self._gateway.execute(call, resolver=self._resolver)` (the 04-03 signature) and keep the `gateway is None -> stub dict` fallback unchanged (D-11). Add a `resolver` ctor arg / attribute to `Worker` defaulting to an `EnvCredentialResolver`. In `main.py`, change both `Worker()` constructions to `Worker(tool_gateway=ToolGateway.from_manifest("manifests/tool_pack_manifest.yaml"))` (resolver defaults inside Worker). Author the Task-2 tests in test_read_path.py.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest -q -m "not live" tests/test_read_path.py tests/ -k "read or approval or write_gate or worker" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "proposed_reads" src/agent_mesh/worker/runner.py` >= 1 and the loop builds ToolCategory.READ / is_read=True calls
    - `grep -c "from_manifest" src/agent_mesh/worker/main.py` >= 1 (both ctor sites inject a real gateway)
    - A test asserts NO approval record / build_approval_request is created for a proposed read (security: read is not a write-without-approval hole)
    - `runner._execute` calls `gateway.execute(call, ...)` (passes the ToolCall, matching the 04-03 signature), with the gateway-None stub fallback intact
    - The existing write-gate suite (AWAITING_APPROVAL + approval opened) still passes
    - `PYTHONPATH=src python -m pytest -q -m "not live"` exits 0 (full default suite green)
  </acceptance_criteria>
  <done>Reads execute ungated immediately after the mesh run, record EXECUTED category=read ToolCalls tenant-scoped, feed evidence as strings, and never touch the approval ledger; the worker boots with a real manifest-loaded gateway + resolver; the write gate is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mesh result -> read execution | proposed reads execute with no human gate — must stay strictly read-category |
| mesh result -> write gate | proposed writes stay behind the payload-hash approval ledger (unchanged) |
| worker bootstrap -> gateway | the real gateway + resolver are injected here; resolver holds the cred-resolution authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Elevation of privilege | the new ungated read path becoming a write-without-approval hole | mitigate | **HIGH — named in the phase threat-model requirement.** Read loop builds ToolCategory.READ / approval_required=False ONLY, calls execute() directly, and NEVER calls approvals.is_approved/build_approval_request/open_approval; a test asserts no approval is opened for a read. Write-class proposals continue through the unchanged payload-hash ledger (SEC-01/SEC-02 untouched). |
| T-04-04-02 | Tampering | a write smuggled into proposed_reads to bypass approval | mitigate | the read loop sets category=READ unconditionally regardless of the proposed dict; a write-category tool routed as a read would still be recorded category=read and is non-mutating by manifest classification — and the engine's own input validation + the write-class manifest invariant remain; reviewer keeps proposed_writes heuristic-derived (item C did not soften it) |
| T-04-04-03 | Information disclosure | read results (dicts) leaking into evidence with secrets | mitigate | evidence appends a STRING summary/source-pointer, not the raw result dict; the gateway already excludes credentials from results (04-03) |
| T-04-04-04 | Tampering | cross-tenant read rows | mitigate | ToolCall built with task.tenant_id; upsert + list_tool_calls are tenant-scoped (DUR-02) |

The HIGH-severity item (read path must not become a write-without-approval hole) is mitigated and test-asserted in this plan; no high-severity threat remains open.
</threat_model>

<verification>
- `make test` exits 0 (read path + write gate + approval suites all green)
- A test proves no approval is opened for a proposed read (security invariant)
- The worker boots with a real manifest-loaded ToolGateway (item B)
</verification>

<success_criteria>
proposed_reads execute ungated post-run as EXECUTED category=read ToolCalls feeding evidence,
the read path never touches the approval ledger, the write gate is unchanged, and the live
worker is wired with a real gateway + resolver.
</success_criteria>

<output>
Create `.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-04-SUMMARY.md` when done
</output>
