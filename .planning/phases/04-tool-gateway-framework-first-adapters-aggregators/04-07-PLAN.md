---
phase: 04-tool-gateway-framework-first-adapters-aggregators
plan: 07
type: execute
wave: 5
depends_on: ["04-06"]
files_modified:
  - src/agent_mesh/tools/adapters/google_workspace.py
  - docs/credentials/google_workspace.md
  - tests/test_gws_live.py
  - tests/test_gws_adapter.py
autonomous: false
requirements: [TOOL-01]

must_haves:
  truths:
    - "Calendar (list read + create write), Docs (get read + create write), and Slides (get read + create write) complete the full GWS direct suite (D-08 full suite live)"
    - "Each new read runs unconditionally in the live lane; each new write is approval-gated (D-08)"
    - "The new ops reuse the 04-06 shared refresh-token auth scaffold without rearchitecture (one OAuth client covers all six products)"
    - "make test stays green and creds-free; each op degrades to the stub when GOOGLE_WORKSPACE_OAUTH is absent (D-11)"
  artifacts:
    - path: "src/agent_mesh/tools/adapters/google_workspace.py"
      provides: "Calendar/Docs/Slides ops registered (completes D-08)"
      contains: "calendar"
    - path: "docs/credentials/google_workspace.md"
      provides: "Calendar/Docs/Slides per-product scopes added (completes D-12 GWS slice)"
      contains: "calendar"
  key_links:
    - from: "tools/adapters/google_workspace.py (Calendar/Docs/Slides)"
      to: "_build_credentials shared scaffold (04-06)"
      via: "reuse the same auth helper for new products"
      pattern: "_build_credentials"
---

<objective>
Complete the full Google Workspace direct suite (D-08 "full suite live, not a starter subset")
by adding Calendar, Docs, and Slides ops to the SAME `tools/adapters/google_workspace.py`
established in 04-06 — reusing the shared refresh-token auth scaffold (`_build_credentials` +
`_service`) so the long tail is thin-op-plus-registration, never a rearchitecture (D-01). This
plan depends on 04-06 because it edits the same file (serialized, not parallel).

Ops added (one representative read + one representative write per product, per RESEARCH Open
Question #1 / planner discretion):
- Calendar: `google_calendar_list_events` (read), `google_calendar_create_event` (write, gated)
- Docs: `google_docs_get` (read), `google_docs_create` (write, gated)
- Slides: `google_slides_get` (read), `google_slides_create` (write, gated)

Manifest entries + schemas for all six already exist (04-01). Reads ungated, writes
approval-gated. Each op degrades to the stub without creds (D-11). The GWS credential doc gains
the Calendar/Docs/Slides scopes (completing D-12's GWS slice).

Purpose: deliver the user's hard directive — prove ALL of Workspace works this phase.
Output: the three remaining products registered, opt-in live coverage for at least one new
read, the doc completed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-CONTEXT.md
@.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-RESEARCH.md
@.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-PATTERNS.md

<interfaces>
<!-- 04-06 shared scaffold to REUSE: _build_credentials(credential, scopes) -> Credentials ;
     _service(product, version, creds) -> build(...) ; adapter contract (spec, params, *, credential) -> dict -->
<!-- scopes (RESEARCH table): calendar.readonly / calendar.events ; documents.readonly / documents ;
     presentations.readonly / presentations -->
<!-- service builds: build("calendar","v3"), build("docs","v1"), build("slides","v1") -->
<!-- manifest entries + schemas authored in 04-01: google_calendar_list_events/create_event,
     google_docs_get/create, google_slides_get/create -->
<!-- live-lane: extend tests/test_gws_live.py (pytest.mark.live + GOOGLE_WORKSPACE_OAUTH skip) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calendar/Docs/Slides ops on the shared scaffold + registrations + live read + doc</name>
  <files>src/agent_mesh/tools/adapters/google_workspace.py, docs/credentials/google_workspace.md, tests/test_gws_live.py, tests/test_gws_adapter.py</files>
  <read_first>
    - src/agent_mesh/tools/adapters/google_workspace.py (04-06: the _build_credentials + _service scaffold + the Drive/Gmail/Sheets op + register pattern to copy)
    - src/agent_mesh/tools/adapters/__init__.py (04-03 registry contract)
    - 04-RESEARCH.md "Exact Google scopes" table (calendar/docs/slides rows) + the build("calendar"/"docs"/"slides", ...) example lines
    - the 04-01 manifest entries + schemas for google_calendar_*/google_docs_*/google_slides_* (output-mapping targets)
    - docs/credentials/google_workspace.md (04-06: the "Calendar/Docs/Slides — see 04-07" placeholder to fill in)
  </read_first>
  <action>
    In the EXISTING `google_workspace.py`, add six op functions following the 04-06 pattern and the adapter contract, each reusing `_build_credentials` + `_service` (no new auth code): `google_calendar_list_events` (read; `calendar.readonly`; `build("calendar","v3")`; events().list), `google_calendar_create_event` (write; `calendar.events`; events().insert), `google_docs_get` (read; `documents.readonly`; `build("docs","v1")`; documents().get), `google_docs_create` (write; `documents`; documents().create), `google_slides_get` (read; `presentations.readonly`; `build("slides","v1")`; presentations().get), `google_slides_create` (write; `presentations`; presentations().create). Map each result to its 04-01 output schema. Each returns the stub-fallback sentinel when `credential` is None (D-11). Register all six with the 04-03 registry. Extend `tests/test_gws_adapter.py` with a credential-None stub test for at least one new product and a result-mapping unit test for one calendar/docs/slides read against a fake service. Extend `tests/test_gws_live.py` with one real live read (e.g. `google_calendar_list_events`) gated on `GOOGLE_WORKSPACE_OAUTH`. Fill in the Calendar/Docs/Slides scope section in `docs/credentials/google_workspace.md` (the six scope URLs from the RESEARCH table), noting that the single OAuth client must be granted the UNION of all six products' scopes.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest -q -m "not live" tests/test_gws_adapter.py -x && PYTHONPATH=src python -m pytest -q -m live tests/test_gws_live.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "register" src/agent_mesh/tools/adapters/google_workspace.py` >= 9 (3 from 04-06 + 6 new = all six products)
    - `grep -c "calendar\|docs\|slides\|presentations" src/agent_mesh/tools/adapters/google_workspace.py` >= 3
    - `grep -c "_build_credentials" src/agent_mesh/tools/adapters/google_workspace.py` >= 7 (each new op reuses the shared scaffold, no new auth path)
    - `grep -c "calendar.events\|documents\|presentations" docs/credentials/google_workspace.md` >= 3
    - `PYTHONPATH=src python -m pytest -q -m live tests/test_gws_live.py` SKIPS when GOOGLE_WORKSPACE_OAUTH unset
    - `PYTHONPATH=src python -m pytest -q -m "not live"` exits 0 (default suite green; module imports without the SDK)
  </acceptance_criteria>
  <done>All six GWS products (Drive/Gmail/Sheets/Calendar/Docs/Slides) are registered as direct adapters over one shared auth scaffold, a new live read passes in the live lane, writes stay approval-gated, the suite degrades to stub without creds, and the credential doc covers all six products.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> Calendar/Docs/Slides APIs | OAuth creds + agent params cross to real GWS calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | EoP | create_event / docs_create / slides_create executing without approval | mitigate | all three are write-class -> approval_required (manifest); reached only post-approval via the unchanged ledger; the new ops add no gating bypass |
| T-04-07-02 | Information disclosure | refresh token reuse across the new products leaking the secret | mitigate | reuses the 04-06 _build_credentials path (execution-time resolution, never returned/logged); no new secret-handling code |
| T-04-07-03 | Spoofing | scope creep when adding products | mitigate | least-privilege per-product scopes documented; the single OAuth client requests exactly the six products' read/write scopes, nothing broader |

No high-severity threat: this plan adds ops on the already-mitigated 04-06 auth path; writes stay approval-gated; no new credential surface. (Supply-chain install gate already cleared in 04-06.)
</threat_model>

<verification>
- `make test` exits 0 (module imports without the SDK; new unit tests green)
- `pytest -m live tests/test_gws_live.py` skips without creds, runs a real Calendar read with them
- The GWS credential doc now covers all six products' scopes
</verification>

<success_criteria>
The full six-product GWS direct suite is registered over one shared auth scaffold (D-08
satisfied), reads run live + writes stay approval-gated, the suite degrades to stub without
creds, and the credential doc is complete — default suite green and creds-free.
</success_criteria>

<output>
Create `.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-07-SUMMARY.md` when done
</output>
