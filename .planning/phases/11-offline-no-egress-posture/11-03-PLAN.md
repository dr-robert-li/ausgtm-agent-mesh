---
phase: 11-offline-no-egress-posture
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - .env.offline.example
  - RUNBOOK.md
  - tests/test_offline_posture_env.py
  - tests/test_zero_src_invariant.py
autonomous: false

requirements: [OFFLINE-01]
user_setup: []

must_haves:
  truths:
    - "An operator can express the offline posture by copying .env.offline.example: cloud-LLM/gateway creds blank, MODEL_GATEWAY_BASE_URL local, SaaS creds left normal"
    - "RUNBOOK documents the offline / no-egress posture: how to express it, how to verify it, and the scope caveat (offline = no cloud-hosted LLM, not blanket no-network)"
    - "A test asserts .env.offline.example carries NO valued cloud-LLM key (key-present-but-blank = OK)"
    - "A durable env-gated zero-src test asserts git diff <base>..HEAD -- src/ is empty when ZERO_SRC_BASE is set, loud-skips when unset"
  artifacts:
    - path: ".env.offline.example"
      provides: "Concrete offline posture artifact (D-03) — cloud-LLM creds blank, local api_base via use-vllm/use-ollama swap, SaaS creds normal"
      contains: "ANTHROPIC_API_KEY="
    - path: "RUNBOOK.md"
      provides: "Offline / no-egress posture section"
      contains: "Offline / no-egress posture"
    - path: "tests/test_offline_posture_env.py"
      provides: "Valued-cloud-key sweep over .env.offline.example (OFFLINE-01)"
      contains: "_CLOUD_KEY_NAMES"
    - path: "tests/test_zero_src_invariant.py"
      provides: "Durable env-gated loud-skip git-diff zero-src assertion"
      contains: "ZERO_SRC_BASE"
  key_links:
    - from: "tests/test_offline_posture_env.py"
      to: ".env.offline.example"
      via: "valued-key sweep (key-present-but-blank OK)"
      pattern: "_CLOUD_KEY_NAMES"
    - from: "tests/test_zero_src_invariant.py"
      to: "git diff $ZERO_SRC_BASE..HEAD -- src/"
      via: "subprocess.run + env-gated loud-skip"
      pattern: "ZERO_SRC_BASE|git.*diff"
---

<objective>
Make OFFLINE-01 an asserted+documented property and durably enforce the milestone's
zero-`src/` guardrail. Ship `.env.offline.example` (D-03) — the cloud-LLM/gateway creds
blanked, local `api_base` supplied by the `use-vllm`/`use-ollama` file-swap (NOT a new
runtime env var, which zero-`src/` forbids), SaaS/tool-pack creds left normal — plus a
RUNBOOK "Offline / no-egress posture" section. Add a test sweeping `.env.offline.example`
for valued cloud-LLM keys, and a DURABLE env-gated zero-`src/` assertion that replaces
P10's one-shot bash gate.

Purpose: A concrete, copy-pasteable, testable posture beats documentation-only; the
valued-key sweep keeps it honest; the durable zero-`src/` test enforces the guardrail
continuously without the time-bomb of a hardcoded base or the vacuity of git-diff-on-main.
Output: `.env.offline.example`, RUNBOOK section, `tests/test_offline_posture_env.py`,
`tests/test_zero_src_invariant.py`. The two tests slot into `make test -m "not live"`;
the env artifact + RUNBOOK are operator-facing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-offline-no-egress-posture/11-CONTEXT.md
@.planning/phases/11-offline-no-egress-posture/11-RESEARCH.md
@.planning/phases/11-offline-no-egress-posture/11-PATTERNS.md

<interfaces>
<!-- .env.example cloud-LLM key block (:50-59) — the derive-from source:
     MODEL_GATEWAY_MASTER_KEY=  MODEL_GATEWAY_SHARED_SECRET=  CF_AIG_WRAPPER_URL=
     ANTHROPIC_API_KEY=  VERTEX_PROJECT_ID=  VERTEX_LOCATION=australia-southeast1
     GOOGLE_APPLICATION_CREDENTIALS=  LANGFUSE_PUBLIC_KEY=  LANGFUSE_SECRET_KEY=
     NOTE: VERTEX_LOCATION (:56) carries a VALUE — the offline variant MUST blank it.
     MODEL_GATEWAY_BASE_URL=http://localhost:4000 (:26) is already local — keep it.
     SaaS creds COMPOSIO_API_KEY (:63) / BITSCALE_API_KEY (:66) — leave NORMAL. -->

<!-- Valued-key analog (PATTERNS load-bearing correction): the .env.offline.example
     sweep's analog is the VALUED-KEY check (key-present-but-blank = OK), NOT the
     test_local_profiles substring sweep — the posture file legitimately contains the
     cloud-LLM key NAMES blank, so "anthropic" in text would false-trip. -->
_CLOUD_KEY_NAMES = ("ANTHROPIC_API_KEY","VERTEX_PROJECT_ID","VERTEX_LOCATION","CF_AIG_WRAPPER_URL","MODEL_GATEWAY_SHARED_SECRET","MODEL_GATEWAY_MASTER_KEY")

<!-- Zero-src durable analog: conftest.py pg_dsn loud-skip (:75-88). ZERO_SRC_BASE is
     operator/CI-set; HEAD at planning = 5092323 (pre-P11 base). Do NOT hardcode the
     base in the test (time-bomb). branch-relative main...HEAD is vacuous on main. -->

<!-- RUNBOOK existing local sections: ## Local inference lane (:96),
     ## Local data & telemetry plane (:184), ## Full-stack local compose (:250),
     ## Credentials & live lane (:340). Insert "## Offline / no-egress posture" near :250/:340. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship .env.offline.example (D-03) + RUNBOOK "Offline / no-egress posture" section + valued-key sweep test (OFFLINE-01)</name>
  <files>.env.offline.example, RUNBOOK.md, tests/test_offline_posture_env.py</files>
  <read_first>
    - .env.example (the derive-from source; cloud-LLM key block at :50-59; MODEL_GATEWAY_BASE_URL at :26; SaaS creds at :61-66; note VERTEX_LOCATION carries a value at :56)
    - RUNBOOK.md (the existing local-section structure — ## Full-stack local compose near :250, ## Credentials & live lane near :340 — mirror their shape; insert the new section near :250/:340)
    - tests/test_offline_compose_env.py (sibling plan 11-01's valued-key check — mirror the _CLOUD_KEY_NAMES semantics for the .env sweep; co-locate the .env sweep HERE, do not fold into the compose-env file, to keep OFFLINE-01 in this plan)
    - .planning/phases/11-offline-no-egress-posture/11-PATTERNS.md (.env.offline.example derive-from + RUNBOOK assignment, lines 182-218; the valued-key load-bearing correction, lines 24-31)
  </read_first>
  <action>
    Create .env.offline.example by deriving from .env.example. Blank the cloud-LLM/gateway creds:
    CF_AIG_WRAPPER_URL=, ANTHROPIC_API_KEY=, VERTEX_PROJECT_ID=, and ALSO blank VERTEX_LOCATION=
    (it currently carries australia-southeast1 — a value the valued-key check would flag), plus
    MODEL_GATEWAY_MASTER_KEY=, MODEL_GATEWAY_SHARED_SECRET=, GOOGLE_APPLICATION_CREDENTIALS=. Keep
    MODEL_GATEWAY_BASE_URL=http://localhost:4000 (already local). Leave SaaS/tool-pack creds
    (COMPOSIO_API_KEY, BITSCALE_API_KEY) and the local DATABASE_URL/TEST_DATABASE_URL/LANGFUSE_HOST as
    NORMAL — offline zeroes only the cloud-LLM/gateway creds. Add a header comment explaining: the
    local model api_base is NOT a new env var (zero-`src/` forbids a new runtime read) — it comes from
    the `make use-vllm`/`use-ollama` profile file-swap; carry forward the COMPOSE-02 doc-note style from
    .env.example:35-40.
    Add a RUNBOOK "## Offline / no-egress posture" section near :250/:340 mirroring the existing local
    sections: (1) what the posture is (no cloud-hosted LLM egress — Vertex/Anthropic/CF — NOT blanket
    no-network; local Postgres/Langfuse/service-DNS/SaaS are legitimate); (2) how to express it
    (`cp .env.offline.example .env` + `make use-vllm` or `make use-ollama`); (3) how to verify
    (`make test` runs the profile sweep + compose-env sweep + deny-guard; the durable zero-src
    assertion via `ZERO_SRC_BASE=<sha> make test`); (4) the scope caveat verbatim.
    Create tests/test_offline_posture_env.py: read .env.offline.example, parse KEY=VALUE lines, and
    assert the VALUED-key rule over `_CLOUD_KEY_NAMES` (ANTHROPIC_API_KEY, VERTEX_PROJECT_ID,
    VERTEX_LOCATION, CF_AIG_WRAPPER_URL, MODEL_GATEWAY_SHARED_SECRET, MODEL_GATEWAY_MASTER_KEY) —
    key-present-but-blank = OK, key-present-and-valued = violation. Also assert MODEL_GATEWAY_BASE_URL
    resolves local (startswith http://localhost or http://127.0.0.1) and that SaaS keys are NOT required
    to be blank (do not over-assert). Use the valued-key analog, NOT a substring sweep (the file
    legitimately contains the blank cloud-LLM key NAMES).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_offline_posture_env.py -m "not live" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_offline_posture_env.py -m "not live" -x` exits 0 (does NOT skip)
    - .env.offline.example exists; ANTHROPIC_API_KEY / VERTEX_PROJECT_ID / VERTEX_LOCATION / CF_AIG_WRAPPER_URL / MODEL_GATEWAY_MASTER_KEY / MODEL_GATEWAY_SHARED_SECRET are all present-and-BLANK
    - MODEL_GATEWAY_BASE_URL is local (http://localhost or 127.0.0.1)
    - COMPOSIO_API_KEY / BITSCALE_API_KEY / DATABASE_URL kept normal (not blanked, not asserted blank)
    - RUNBOOK.md contains a "## Offline / no-egress posture" heading with the scope caveat ("no cloud-hosted LLM, not blanket no-network")
    - The test uses valued-key semantics, NOT a "anthropic" substring sweep (which would false-trip on the blank key name)
  </acceptance_criteria>
  <done>.env.offline.example + RUNBOOK section shipped; valued-key sweep over it passes in the CI lane.</done>
</task>

<task type="auto">
  <name>Task 2: Durable env-gated zero-src invariant test — git diff $ZERO_SRC_BASE..HEAD -- src/ (loud-skip when unset)</name>
  <files>tests/test_zero_src_invariant.py</files>
  <read_first>
    - tests/conftest.py (the pg_dsn env-gated loud-skip-when-unset shape at :75-88 — mirror exactly)
    - .planning/phases/11-offline-no-egress-posture/11-RESEARCH.md (the durable zero-src code example at lines 314-334; Open Question 3 trilemma at lines 424-426; A4 base-SHA note at line 410)
    - .planning/phases/11-offline-no-egress-posture/11-PATTERNS.md (test_zero_src_invariant assignment + the "do NOT hardcode the base SHA" note, lines 156-179)
    - .planning/phases/10-full-stack-local-compose/10-03-SUMMARY.md (the P10 one-shot bash zero-src gate this DURABLE test supersedes; phase-base SHA convention)
  </read_first>
  <action>
    Create tests/test_zero_src_invariant.py. Read `ZERO_SRC_BASE` from env; when unset,
    `pytest.skip("ZERO_SRC_BASE unset; durable zero-src assertion needs the phase-base SHA")` —
    mirroring conftest pg_dsn EXACTLY so default `make test` loud-skips and stays green on any box.
    When set: `subprocess.run(["git", "diff", f"{base}..HEAD", "--", "src/"], cwd=_REPO,
    capture_output=True, text=True, check=True)` and assert stdout == "" (empty), with a message
    showing the diff if non-empty. Do NOT hardcode the base SHA in the test (time-bomb — would break
    the next legitimate src-touching milestone's `make test`); do NOT use branch-relative `main...HEAD`
    (vacuous on `main`, where `make test` runs post-merge). Env-gated loud-skip is the only formulation
    that mirrors repo convention AND avoids both failure modes.
    Document at the top of the file: the operator/CI exports `ZERO_SRC_BASE=<phase-base-SHA>` (the
    pre-P11 base, currently 5092323) for the enforcing run; default `make test` loud-skips.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_zero_src_invariant.py -m "not live" -q</automated>
  </verify>
  <acceptance_criteria>
    - With ZERO_SRC_BASE UNSET: `.venv/bin/python -m pytest tests/test_zero_src_invariant.py -m "not live"` reports the test SKIPPED (loud-skip), suite green
    - The skip message names ZERO_SRC_BASE (mirrors pg_dsn convention)
    - The test does NOT hardcode any SHA and does NOT use `main...HEAD`
    - The git-diff uses `$ZERO_SRC_BASE..HEAD -- src/` and asserts empty stdout
  </acceptance_criteria>
  <done>Durable env-gated zero-src test in place; loud-skips by default, enforces when ZERO_SRC_BASE is set.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [BLOCKING]: ENFORCING zero-src run — prove the durable test is non-vacuous and src/ is unchanged since the phase base</name>
  <files>(verification only — no files produced)</files>
  <read_first>
    - tests/test_zero_src_invariant.py (the durable assertion created in Task 2)
    - .planning/phases/11-offline-no-egress-posture/11-RESEARCH.md (Sampling Rate "Phase gate" at line 463; A4 base-SHA at line 410)
  </read_first>
  <action>
    Run the durable zero-src test ENFORCING (base supplied), confirm src/ is unchanged since the phase
    base, and confirm the full default lane is green — then pause for human approval. This is a
    verification-only checkpoint: it produces no files, it proves the Task-2 guardrail is non-vacuous at
    the phase gate (a SKIP counts as FAILURE) before the phase can close. Phase base = 5092323 (HEAD at
    planning; pre-P11, untouched by docs/plan commits). See <how-to-verify> for the exact commands.
  </action>
  <what-built>
    A durable env-gated zero-`src/` assertion (Task 2) plus all Phase-11 config/docs/tests artifacts.
    This checkpoint proves the guardrail ENFORCING — not vacuously skipped. The phase base is
    5092323 (HEAD at planning; pre-P11, no src/ touched by docs/plan commits).
  </what-built>
  <how-to-verify>
    Run the durable test ENFORCING (base supplied) and confirm it does NOT skip and PASSES:
      1. `ZERO_SRC_BASE=5092323 .venv/bin/python -m pytest tests/test_zero_src_invariant.py -x -q`
         → must report 1 passed, 0 skipped (a SKIP here counts as FAILURE — it would mean the guard
         is vacuous at the phase gate).
      2. Cross-check directly: `git diff 5092323..HEAD -- src/` → must print NOTHING (src/ unchanged).
      3. Full lane stays green: `make test` (`.venv/bin/python -m pytest -q -m "not live"`) → all
         four Phase-11 CI-wired tests collected, suite green.
    If src/ shows any diff or the enforcing run skips, STOP — the zero-`src/` milestone guardrail is
    breached and must be resolved before the phase can complete.
  </how-to-verify>
  <verify>
    <automated>ZERO_SRC_BASE=5092323 .venv/bin/python -m pytest tests/test_zero_src_invariant.py -x -q</automated>
  </verify>
  <done>Enforcing run passes WITHOUT skipping; `git diff 5092323..HEAD -- src/` empty; `make test` green; human approved.</done>
  <acceptance_criteria>
    - `ZERO_SRC_BASE=5092323 .venv/bin/python -m pytest tests/test_zero_src_invariant.py -x` reports 1 passed, 0 skipped (a SKIP counts as FAILURE — vacuous guard at the phase gate)
    - `git diff 5092323..HEAD -- src/` prints nothing (src/ unchanged since the phase base)
    - `make test` (`.venv/bin/python -m pytest -q -m "not live"`) collects all four Phase-11 CI-wired tests (test_local_profiles.py, test_offline_compose_env.py, test_offline_deny_guard.py, test_offline_posture_env.py + test_zero_src_invariant.py loud-skipped) and the suite is green
    - Human types "approved" only after all three above hold
  </acceptance_criteria>
  <resume-signal>Type "approved" once the enforcing run passes (not skipped), `git diff 5092323..HEAD -- src/` is empty, and `make test` is green; or describe the breach.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator .env / src tree → committed state | A valued cloud-LLM cred in the posture file, or any src/ edit, breaches the offline+zero-src invariants this phase asserts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Information Disclosure | .env.offline.example ships a VALUED cloud-LLM cred (e.g. VERTEX_LOCATION carried over from .env.example:56) | mitigate | Task 1 blanks VERTEX_LOCATION + valued-key sweep test asserts all `_CLOUD_KEY_NAMES` blank |
| T-11-10 | Assurance gap (false positive) | A substring sweep over .env.offline.example trips on the file's own intentionally-blank cloud-LLM key NAMES | mitigate | Valued-key semantics (key-present-but-blank = OK), NOT substring sweep (PATTERNS load-bearing correction) |
| T-11-11 | Tampering / Assurance gap | An unintended src/ change defeats the zero-src guardrail, OR the durable test passes vacuously by skipping | mitigate | Task 2 durable env-gated git-diff test + Task 3 [BLOCKING] ENFORCING run (ZERO_SRC_BASE=5092323 must NOT skip) |
| T-11-12 | Assurance gap (time-bomb / vacuity) | A hardcoded-base always-on test breaks the next src-touching milestone, or main...HEAD is vacuous on main | accept-with-control | Env-gated loud-skip formulation (the only non-time-bomb, non-vacuous form per RESEARCH Open-Q3) |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (all deps present); slopcheck N/A per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `make test` collects test_offline_posture_env.py + test_zero_src_invariant.py; the zero-src test loud-skips by default; suite green.
- ENFORCING (Task 3): `ZERO_SRC_BASE=5092323 .venv/bin/python -m pytest tests/test_zero_src_invariant.py -x` passes WITHOUT skipping; `git diff 5092323..HEAD -- src/` empty.
- .env.offline.example valued-key sweep passes; RUNBOOK has the offline section.
</verification>

<success_criteria>
- OFFLINE-01 asserted+documented: copy-pasteable `.env.offline.example` (cloud-LLM creds blank, local api_base via swap, SaaS normal) + RUNBOOK section; valued-key sweep enforces it.
- Durable zero-`src/` guardrail enforced non-vacuously via the [BLOCKING] ENFORCING run (replaces P10's one-shot bash gate).
</success_criteria>

<output>
Create `.planning/phases/11-offline-no-egress-posture/11-03-SUMMARY.md` when done.
</output>
