---
phase: 11-offline-no-egress-posture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_local_profiles.py
  - tests/test_offline_compose_env.py
autonomous: true
requirements: [OFFLINE-02]
user_setup: []

must_haves:
  truths:
    - "All 4 local model profiles pass a whole-file cloud-LLM-marker sweep (no Vertex/Anthropic/CF marker anywhere in the file)"
    - "Every route api_base in all 4 local profiles is loopback OR in-stack service-DNS (no cloud api_base)"
    - "config/model_gateway.cloud.yaml DOES trip the sweep (negative control proves the sweep is non-vacuous)"
    - "docker-compose.yml api/worker env blocks carry no VALUED cloud-LLM key and no cloud host in any env value"
  artifacts:
    - path: "tests/test_local_profiles.py"
      provides: "Extended whole-file sweep across 4 profiles + service-DNS allowlist + cloud.yaml negative control (D-01)"
      contains: "model_gateway.vllm.compose.yaml"
    - path: "tests/test_offline_compose_env.py"
      provides: "docker-compose.yml api/worker env-block valued-cloud-key sweep (D-04)"
      contains: "_CLOUD_KEY_NAMES"
  key_links:
    - from: "tests/test_local_profiles.py"
      to: "config/model_gateway.{vllm,ollama,vllm.compose,cpu.compose}.yaml"
      via: "read_text() whole-file sweep"
      pattern: "_FILE_DENY|_CLOUD_MARKERS"
    - from: "tests/test_local_profiles.py"
      to: "config/model_gateway.cloud.yaml"
      via: "negative-control assertion (must trip)"
      pattern: "cloud.yaml.*trip|does_trip"
    - from: "tests/test_offline_compose_env.py"
      to: "docker-compose.yml"
      via: "yaml.safe_load + api/worker env normalization"
      pattern: "services.*api.*worker.*environment"
---

<objective>
Make OFFLINE-02 an asserted property: extend the existing LOCAL-04 profile guard to a
whole-file cloud-LLM-marker sweep across ALL FOUR local profiles (D-01), and add a NEW
test sweeping the assembled `docker-compose.yml` api/worker env blocks (D-04). Together
they prove the local/offline model surface is egress-free by construction — no cloud
`api_base`, no Vertex/Anthropic/CF marker, no valued cloud-LLM key.

Purpose: A stray cloud reference outside `api_base` (the current LOCAL-04 scope) would
leak undetected. This phase closes that gap with a whole-file sweep + a negative control
that fails loudly if the marker list is ever emptied.
Output: Extended `tests/test_local_profiles.py`; new `tests/test_offline_compose_env.py`.
Both slot into the default `make test -m "not live"` lane (the CI-wired Phase-11 artifacts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-offline-no-egress-posture/11-CONTEXT.md
@.planning/phases/11-offline-no-egress-posture/11-RESEARCH.md
@.planning/phases/11-offline-no-egress-posture/11-PATTERNS.md

<interfaces>
<!-- Existing seam in tests/test_local_profiles.py (EXTEND, do not duplicate). -->
_PROFILES: dict[str, Path]            # currently {"vllm":..., "ollama":...}
_NAMES = {"low-complexity","medium-complexity","high-complexity"}
_LOOPBACK = ("http://localhost", "http://127.0.0.1")
_CLOUD_MARKERS = ("os.environ/CF_AIG_WRAPPER_URL","vertex_ai","anthropic","googleapis.com","anthropic.com")
# Parametrized tests: test_profile_has_three_exact_deployment_names,
#   test_every_route_api_base_is_loopback, test_no_cloud_named_route,
#   test_build_router_constructs_with_no_network
# Config-load pattern: cfg = yaml.safe_load(path.read_text()); route = cfg["model_list"][i]["litellm_params"]["api_base"]

<!-- Negative-control analog: tests/test_d06_chokepoint.py:172/180
     test_guard_has_real_coverage / test_guard_detects_a_synthetic_offender -->

<!-- EMPIRICALLY VERIFIED (this planning session): all 4 local profiles produce ZERO
     marker hits (sk- does NOT false-positive); cloud.yaml trips CF_AIG_WRAPPER_URL,
     vertex_ai, anthropic. Whole-file sweep is safe as designed. -->

<!-- docker-compose.yml api env block (:40-49), worker env block (:75-83):
     both set only DATABASE_URL (@postgres), LANGFUSE_HOST (@langfuse-web),
     blank LANGFUSE_PUBLIC_KEY/SECRET_KEY, TMPDIR — all legitimate; sweep passes today. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend test_local_profiles.py — whole-file sweep across 4 profiles + service-DNS allowlist + cloud.yaml negative control (D-01)</name>
  <files>tests/test_local_profiles.py</files>
  <read_first>
    - tests/test_local_profiles.py (the file being modified; its docstring designates the whole-file sweep as "Phase 11's job" at :16-18)
    - tests/test_d06_chokepoint.py (the inverted config-load pattern + the negative-control analog test_guard_detects_a_synthetic_offender at :180)
    - config/model_gateway.cloud.yaml (the negative-control target — confirmed to contain CF_AIG_WRAPPER_URL, vertex_ai, anthropic)
    - .planning/phases/11-offline-no-egress-posture/11-RESEARCH.md (Pattern 1 + Pattern 2, lines 134-177; Final Marker List, lines 366-383)
    - .planning/phases/11-offline-no-egress-posture/11-PATTERNS.md (the EXTEND assignment, lines 35-83)
  </read_first>
  <action>
    Add the 2 compose profiles to `_PROFILES`: key "vllm-compose" → config/model_gateway.vllm.compose.yaml,
    key "cpu-compose" → config/model_gateway.cpu.compose.yaml (both confirmed present).
    Add `_SERVICE_DNS = ("http://vllm:8000", "http://ollama:11434")` and `_LOCAL_ALLOW = _LOOPBACK + _SERVICE_DNS`.
    Change the existing `test_every_route_api_base_is_loopback` so its `startswith` accepts `_LOCAL_ALLOW`
    (rename to reflect loopback-OR-service-DNS); the 2 loopback profiles still satisfy it, the 2 compose
    profiles need service-DNS.
    Add a `_FILE_DENY` tuple of whole-file deny markers — the D-01 floor exactly:
    ("CF_AIG_WRAPPER_URL", "vertex_ai", "googleapis.com", "anthropic", "anthropic.com", "sk-").
    Do NOT add "openai" (no OpenAI route exists; would false-positive on vLLM's "OpenAI-compatible"
    prose) — keep cloud-LLM-scoped per CONTEXT Discretion + RESEARCH A3.
    Add a NEW parametrized test over all 4 profiles asserting `read_text()` contains none of `_FILE_DENY`
    (whole-file, not just api_base) — the gap LOCAL-04 left open.
    Add the negative-control test (mirror test_d06_chokepoint.py:180): read the REAL
    config/model_gateway.cloud.yaml and assert `any(m in text for m in _FILE_DENY)` is TRUE, with a
    message that the sweep is vacuous/broken if it stops tripping. This defends against a future edit
    emptying `_FILE_DENY`.
    Do NOT edit any config/*.yaml — the 4 profiles + cloud.yaml are FROZEN sweep targets / the negative
    control (zero-`src/` is separate; these are config files but P8/P10 froze them and the negative
    control depends on cloud.yaml staying dirty).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_local_profiles.py -m "not live" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_local_profiles.py -m "not live" -x` exits 0
    - `_PROFILES` contains exactly 4 keys including "vllm-compose" and "cpu-compose"
    - The whole-file sweep test PASSES for all 4 local profiles (empirically: zero marker hits)
    - The negative-control test asserts config/model_gateway.cloud.yaml DOES trip `_FILE_DENY`
    - The api_base test accepts service-DNS (`http://vllm:8000`, `http://ollama:11434`) for the 2 compose profiles
    - git shows no change under config/ (`git status --porcelain config/` empty)
  </acceptance_criteria>
  <done>4 profiles swept whole-file; cloud.yaml negative control trips; service-DNS allowlisted; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: New tests/test_offline_compose_env.py — docker-compose api/worker env-block valued-cloud-key sweep (D-04)</name>
  <files>tests/test_offline_compose_env.py</files>
  <read_first>
    - docker-compose.yml (the sweep target; api env block at :40-49, worker env block at :75-83)
    - tests/test_compose_config.py (the only CI-wired P10 artifact — mirror its module/docstring shape; this NEW file is YAML-static so it needs NO docker skipif)
    - .planning/phases/11-offline-no-egress-posture/11-RESEARCH.md (D-04 compose env-block sweep code example, lines 336-364)
    - .planning/phases/11-offline-no-egress-posture/11-PATTERNS.md (test_offline_compose_env.py assignment + valued-key semantics, lines 86-116)
  </read_first>
  <action>
    Create tests/test_offline_compose_env.py. Load docker-compose.yml with `yaml.safe_load(Path(...).read_text())`
    — purely static, NO docker daemon, so do NOT add `skipif(shutil.which("docker"))`.
    Define `_CLOUD_KEY_NAMES = ("ANTHROPIC_API_KEY", "VERTEX_PROJECT_ID", "VERTEX_LOCATION",
    "CF_AIG_WRAPPER_URL", "MODEL_GATEWAY_SHARED_SECRET")`.
    For each of services "api" and "worker": read `.get("environment", {})`; normalize — compose
    `environment:` may be a dict OR a `KEY=VALUE` list (handle both per RESEARCH:353-354).
    Assert the VALUED-key rule: a named cloud-LLM key present-but-blank is OK; present-AND-non-empty is
    the violation (key-present ≠ violation; key-VALUED = violation). This is the correct semantics
    (not a substring sweep) because the compose env legitimately omits these keys today.
    Also assert no env VALUE in either service contains a cloud host substring: "googleapis.com",
    "anthropic", "gateway.ai.cloudflare.com". Local Postgres (@postgres), self-hosted Langfuse
    (@langfuse-web), blank LANGFUSE_* keys, and TMPDIR must NOT trip — they are legitimate.
    Mirror test_compose_config.py's docstring discipline: state what is asserted (api/worker env no
    valued cloud-LLM key / no cloud host) vs out of scope (the service-DNS api_base mount is covered by
    the Task-1 profile sweep, not here; offline = cloud-LLM-scoped, not blanket no-network).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_offline_compose_env.py -m "not live" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_offline_compose_env.py -m "not live" -x` exits 0 (does NOT skip)
    - The test reads docker-compose.yml statically (no docker binary required; no `skipif(shutil.which("docker"))`)
    - api and worker env blocks both pass today (only DATABASE_URL/LANGFUSE_HOST/blank LANGFUSE_*/TMPDIR present)
    - `_CLOUD_KEY_NAMES` valued-key semantics: present-but-blank OK, present-and-valued fails
    - env normalization handles both dict and `KEY=VALUE` list forms
    - `make test` (`-m "not live"`) collects and passes this new file
  </acceptance_criteria>
  <done>Compose api/worker env sweep asserts no valued cloud-LLM key / no cloud host; passes static, in CI lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config profiles / compose → model gateway | A cloud `api_base`/key/marker in a "local" artifact is the egress that this phase forbids; the test surface is where it is caught. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | A local profile carries a stray cloud marker OUTSIDE `api_base` (LOCAL-04 only scoped api_base) | mitigate | Task 1 whole-file `read_text()` sweep across all 4 profiles with `_FILE_DENY` |
| T-11-02 | Information Disclosure | A VALUED cloud-LLM key reaches the assembled `docker-compose.yml` api/worker stack | mitigate | Task 2 valued-key sweep over both env blocks (present-but-blank OK, valued = fail) |
| T-11-03 | Assurance gap (vacuous guard) | A future edit empties `_FILE_DENY`, silently disabling the sweep | mitigate | Task 1 negative control: cloud.yaml MUST trip `_FILE_DENY` (mirror test_d06_chokepoint.py:180) |
| T-11-04 | False positive (allowlist too narrow) | Legitimate service-DNS (`vllm:8000`,`ollama:11434`) or @postgres/@langfuse env flagged as egress | accept-with-control | `_SERVICE_DNS` allowlist + cloud-LLM-scoped key/host lists; never a blanket network/host block |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (all deps present); slopcheck N/A per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `make test` (`.venv/bin/python -m pytest -q -m "not live"`) collects both files and stays green.
- The negative control fails loudly if `_FILE_DENY` is ever emptied (cloud.yaml must trip).
- `git status --porcelain config/ src/` empty — no frozen-config or src change.
</verification>

<success_criteria>
- OFFLINE-02 asserted: 4 local profiles whole-file-clean + service-DNS-allowed api_base; compose api/worker env carries no valued cloud-LLM key/host; cloud.yaml negative control trips.
- Both tests in the default `make test -m "not live"` lane; no docker binary required.
</success_criteria>

<output>
Create `.planning/phases/11-offline-no-egress-posture/11-01-SUMMARY.md` when done.
</output>
