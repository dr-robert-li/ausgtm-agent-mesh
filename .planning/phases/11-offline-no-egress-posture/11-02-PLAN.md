---
phase: 11-offline-no-egress-posture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_offline_deny_guard.py
autonomous: true
requirements: [OFFLINE-03]
user_setup: []

must_haves:
  truths:
    - "An anthropic/ call under the deny-guard fixture raises on the SYNC path (positive control wired)"
    - "An anthropic/ call under the deny-guard fixture raises on the ASYNC (acompletion) path — the path the real worker uses"
    - "A completion routed through the active LOCAL profile (loopback/service-DNS api_base) does NOT trip the deny-guard"
    - "The fixture is file-local autouse (function-scoped, not scope=module) and no-ops/skips when real provider creds are present (does not break make test-live)"
  artifacts:
    - path: "tests/test_offline_deny_guard.py"
      provides: "socket.getaddrinfo autouse deny-guard raising only on cloud-LLM hosts + sync/async positive controls + representative local-profile path (D-02)"
      contains: "getaddrinfo"
  key_links:
    - from: "tests/test_offline_deny_guard.py"
      to: "socket.getaddrinfo"
      via: "monkeypatch.setattr — raise only on cloud-LLM hosts"
      pattern: "getaddrinfo"
    - from: "tests/test_offline_deny_guard.py"
      to: "agent_mesh.worker.model_gateway.build_router"
      via: "representative default-lane path under the fixture (loopback profile)"
      pattern: "build_router"
---

<objective>
Make OFFLINE-03 an asserted property: a `socket.getaddrinfo` autouse deny-guard (D-02)
that raises ONLY on known cloud-LLM hosts, proving the default creds-free lane performs no
outbound to a real LLM provider/gateway — while every other host (Postgres, Langfuse,
SaaS, loopback, service-DNS) is allowed.

Purpose: The default lane is creds-free and `build_router` is lazy (Pitfall 6), so the
posture should pass clean — but only a NON-VACUOUS proof counts. The guard is paired with
an `anthropic/` positive control (sync AND async — the worker is async, RESEARCH Pitfall 1)
and a representative completion driven through the active LOCAL profile, so the guard is
proven wired AND discriminating.
Output: New `tests/test_offline_deny_guard.py`, in the default `make test -m "not live"` lane.
Zero `src/` change — the guard lives entirely in tests/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-offline-no-egress-posture/11-CONTEXT.md
@.planning/phases/11-offline-no-egress-posture/11-RESEARCH.md
@.planning/phases/11-offline-no-egress-posture/11-PATTERNS.md

<interfaces>
<!-- The deny-guard MECHANISM is research-derived (no autouse/socket-patch exists in the
     repo). Take it from RESEARCH "Code Examples" (the full fixture, RESEARCH:242-312).
     EMPIRICALLY VERIFIED there: socket.getaddrinfo catches BOTH sync (completion) and
     async (acompletion) litellm 1.83.7; socket.create_connection MISSES async. -->

build_router(path: str) -> litellm.Router      # agent_mesh.worker.model_gateway — lazy, no provider call at construction

<!-- Creds-gating analog: tests/conftest.py:124-136 live_creds — probes
     ANTHROPIC_API_KEY / VERTEX_PROJECT_ID / CF_AIG_WRAPPER_URL. Reuse this exact probe
     set, INVERTED: the deny-guard no-ops/skips when creds ARE present (Pitfall 4 —
     do not block the real cloud calls in `make test-live`). -->

<!-- Pitfall 2: vertex_ai/ dies at DefaultCredentialsError BEFORE connect in the
     creds-free lane → positive control MUST use anthropic/, NOT vertex_ai/.
     Pitfall 3: set LITELLM_LOCAL_MODEL_COST_MAP=True so litellm does not fetch its
     cost map from raw.githubusercontent.com at call time. -->

<!-- Cloud-LLM hosts (D-02): api.anthropic.com / anthropic.com (VERIFIED at the seam),
     aiplatform.googleapis.com + <region>-aiplatform.googleapis.com (Vertex, ASSUMED),
     gateway.ai.cloudflare.com + *.gateway.ai.cloudflare.com (CF, ASSUMED). Match the
     PRECISE Vertex host, NOT bare googleapis.com (a SaaS adapter may use other Google APIs). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New tests/test_offline_deny_guard.py — socket.getaddrinfo autouse deny-guard + sync/async positive controls (D-02)</name>
  <files>tests/test_offline_deny_guard.py</files>
  <read_first>
    - tests/conftest.py (the live_creds creds-gating analog at :124-136 and pg_dsn loud-skip at :75-88; note there is NO autouse fixture in the repo — the mechanism is research-derived)
    - .planning/phases/11-offline-no-egress-posture/11-RESEARCH.md (the full deny-guard fixture Code Example at lines 242-312; Pitfalls 1-6 at lines 204-238; Final Host List at lines 385-391)
    - .planning/phases/11-offline-no-egress-posture/11-PATTERNS.md (test_offline_deny_guard.py assignment + the "No Analog Found" mechanism note, lines 119-154 + 250-254)
  </read_first>
  <action>
    Create tests/test_offline_deny_guard.py. Define `_CLOUD_LLM_HOSTS` = ("api.anthropic.com",
    "anthropic.com", "aiplatform.googleapis.com", "gateway.ai.cloudflare.com"), and a helper
    `_is_cloud_llm_host(host)` that lowercases and also matches the regional/subdomain shapes
    `<region>-aiplatform.googleapis.com` (endswith "-aiplatform.googleapis.com") and
    `*.gateway.ai.cloudflare.com` (endswith ".gateway.ai.cloudflare.com"). Match the PRECISE Vertex
    host, NEVER bare "googleapis.com" (V5 input-validation precision — a SaaS adapter may call other
    Google APIs legitimately).
    Define a `@pytest.fixture(autouse=True)` deny-guard, file-local to THIS module (NOT root
    conftest.py — Pitfall 4). Keep the fixture FUNCTION-SCOPED (the autouse=True default); do NOT
    set `scope="module"` — `monkeypatch` is function-scoped and `scope="module"` raises
    pytest `ScopeMismatch`. "File-local" here means it applies to every test in this file via
    autouse, NOT a pytest module scope. Inside it, `monkeypatch.setenv("LITELLM_LOCAL_MODEL_COST_MAP", "True")`
    (Pitfall 3), then wrap `socket.getaddrinfo` with a guard `def guard(host, *args, **kwargs)` (host
    positional per Pitfall 6) that raises RuntimeError("OFFLINE deny: cloud-LLM host ...") when
    `_is_cloud_llm_host(host)`, else delegates to the real `socket.getaddrinfo`.
    Pitfall 4 — gate the guard OFF when real creds are present: reuse the conftest live_creds probe set
    (ANTHROPIC_API_KEY / VERTEX_PROJECT_ID / CF_AIG_WRAPPER_URL); when any is set, the autouse fixture
    yields without patching (so `make test-live` real cloud calls are not blocked). The guard targets
    the default creds-free lane only.
    Add the SYNC positive control: under the fixture, `litellm.completion(model="anthropic/claude-sonnet-4-6",
    ..., api_key="sk-ant-fake", max_retries=0)` MUST raise, asserting "OFFLINE deny" or "anthropic.com"
    in the error. Add the ASYNC positive control: same call via `litellm.acompletion` driven through
    `asyncio.run(...)` MUST also raise — this is the path the real LangGraph/Deep Agents worker uses,
    so async coverage is mandatory (RESEARCH A5). Do NOT use vertex_ai/ for either control (Pitfall 2).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_offline_deny_guard.py -m "not live" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_offline_deny_guard.py -m "not live" -x` exits 0
    - The sync positive control (`litellm.completion` with `anthropic/`) raises under the guard
    - The async positive control (`litellm.acompletion` with `anthropic/`) raises under the guard
    - `_is_cloud_llm_host("aiplatform.googleapis.com")` is True; `_is_cloud_llm_host("sheets.googleapis.com")` is False (precise Vertex match, not bare googleapis.com)
    - The autouse fixture is in THIS file (function-scoped via autouse=True, NOT `scope="module"`), not in root conftest.py
    - The fixture no-ops when ANTHROPIC_API_KEY/VERTEX_PROJECT_ID/CF_AIG_WRAPPER_URL is set (make test-live unaffected)
    - `LITELLM_LOCAL_MODEL_COST_MAP=True` is set in the fixture (no raw.githubusercontent.com fetch)
  </acceptance_criteria>
  <done>getaddrinfo deny-guard wired; sync+async anthropic/ positive controls trip; creds-gated; cost-map fetch suppressed; in CI lane.</done>
</task>

<task type="auto">
  <name>Task 2: Representative default-lane path — drive a completion through the active LOCAL profile under the guard (non-vacuous proof, Pitfall 5)</name>
  <files>tests/test_offline_deny_guard.py</files>
  <read_first>
    - tests/test_local_profiles.py (the build_router-offline pattern at :79-90 — reuse the exact build_router call)
    - .planning/phases/11-offline-no-egress-posture/11-RESEARCH.md (Pitfall 5 non-vacuous representative path at lines 228-233; the representative-path code example at lines 298-312)
    - .planning/phases/11-offline-no-egress-posture/11-PATTERNS.md (representative-path analog note, lines 146-153)
  </read_first>
  <action>
    In tests/test_offline_deny_guard.py, add the representative default-lane test. Under the autouse
    guard, `build_router("config/model_gateway.vllm.yaml")` (loopback api_base — the active local
    profile shape) and drive ONE completion through a local deployment name (e.g. "low-complexity").
    Wrap the call in try/except: a connection-refused / timeout to http://localhost:8000 is ACCEPTABLE
    (it proves egress targeted loopback, not a cloud host); the ONLY failure condition is the string
    "OFFLINE deny" appearing in the error — that would mean a cloud-LLM host was contacted from the
    local profile. Assert "OFFLINE deny" NOT in str(e). This closes Pitfall 5: `build_router` is lazy,
    so without driving a completion the guard asserts nothing — this test makes the proof non-vacuous
    by genuinely exercising a routed call path under the fixture.
    Do NOT add any new src/ — this reuses the existing build_router seam.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_offline_deny_guard.py -m "not live" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_offline_deny_guard.py -m "not live" -x` exits 0
    - The representative test builds a Router from a loopback profile and drives one completion under the guard
    - The test treats connection-refused-to-localhost as PASS; only "OFFLINE deny" in the error fails it
    - The guard is genuinely exercised (not just a Router construction) — proof is non-vacuous (Pitfall 5)
    - `git status --porcelain src/` empty (no src change)
  </acceptance_criteria>
  <done>A routed completion through the local loopback profile runs under the guard without tripping it; non-vacuous OFFLINE-03 proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-test litellm call → network (socket.getaddrinfo) | The DNS-resolution chokepoint below both httpx sync + async backends; the cloud-LLM egress the default lane must never make crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Information Disclosure / Exfiltration | Default creds-free lane silently egresses to a cloud LLM via the ASYNC path (worker is async) | mitigate | Task 1 `socket.getaddrinfo` guard (catches sync+async) + async `anthropic/` positive control (RESEARCH Pitfall 1) |
| T-11-06 | Assurance gap (vacuous guard) | Guard passes for the wrong reason — never exercised because `build_router` is lazy | mitigate | Task 2 representative routed completion through the local profile under the guard (Pitfall 5) |
| T-11-07 | Assurance gap (wrong-reason pass) | Vertex "caught" only because it dies at DefaultCredentialsError before connect | mitigate | Positive control uses `anthropic/` (reaches the seam), NOT vertex_ai/ (Pitfall 2); doc states the file-marker sweep is Vertex's real guard |
| T-11-08 | False positive (allowlist too broad) | Guard flags a legitimate non-Vertex Google API or blocks make test-live | accept-with-control | Precise `aiplatform.googleapis.com` match (not bare googleapis.com) + creds-gated no-op (Pitfall 4) |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (all deps present); slopcheck N/A per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `make test` collects test_offline_deny_guard.py and stays green (default lane passes clean — posture proven).
- Positive controls (sync + async) trip; representative local-profile path does not trip.
- `make test-live` unaffected: the autouse guard no-ops when real creds are present.
- `git status --porcelain src/` empty.
</verification>

<success_criteria>
- OFFLINE-03 asserted non-vacuously: cloud-LLM-host deny-guard wired (sync+async positive controls), default-lane local-profile path runs without tripping it, make test-live not broken.
</success_criteria>

<output>
Create `.planning/phases/11-offline-no-egress-posture/11-02-SUMMARY.md` when done.
</output>
