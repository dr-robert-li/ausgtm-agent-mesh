---
phase: 01-durable-core-approval-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent_mesh/services/repository.py
  - src/agent_mesh/worker/runner.py
  - src/agent_mesh/services/sessions.py
  - pyproject.toml
  - tests/conftest.py
  - tests/test_approval_gating.py
  - tests/smoke.py
  - tests/test_repository_sql.py
autonomous: true
requirements: [DUR-01, DUR-02]
must_haves:
  truths:
    - "After a worker process restart, a queued/in-flight task resumes from Postgres with no lost state (task row + task_events audit trail both present)"
    - "A read scoped to one tenant never returns another tenant's task events or evaluations"
    - "The resume-after-approval path executes the approved write against RepositorySQL (no silent drop) — proven by a SQL-backed test, not just the in-memory fake"
    - "No caller reaches into repository private dict state (repo._tool_calls / repo._approvals); all access goes through the protocol"
    - "When DATABASE_URL is unset, the in-memory repository remains the fallback and the existing suite stays green"
  artifacts:
    - path: "src/agent_mesh/services/repository.py"
      provides: "Widened Repository protocol (tenant_id on list_events/list_evaluations; new list_tool_calls/list_approvals) + RepositorySQL (sync psycopg3 ConnectionPool) + DATABASE_URL routing in get_repository"
      contains: "class RepositorySQL"
    - path: "tests/test_repository_sql.py"
      provides: "RepositorySQL round-trip, restart-survival, cross-tenant-empty, and resume-executes-approved-write tests (skipif TEST_DATABASE_URL unset)"
      contains: "TEST_DATABASE_URL"
  key_links:
    - from: "src/agent_mesh/worker/runner.py"
      to: "Repository.list_tool_calls"
      via: "_pending_calls now calls repo.list_tool_calls(task_id, tenant_id)"
      pattern: "list_tool_calls"
    - from: "src/agent_mesh/services/repository.py:get_repository"
      to: "RepositorySQL"
      via: "DATABASE_URL set -> RepositorySQL, else InMemoryRepository"
      pattern: "DATABASE_URL"
---

<objective>
Replace the in-memory singleton with a durable Postgres-backed `RepositorySQL` (DUR-01) and make every repository read path tenant-scoped (DUR-02). This is the phase linchpin: the `Repository` protocol must be WIDENED and every private reach-in migrated BEFORE any SQL is written, or the resume-after-approval loop silently completes a task without executing the approved write while tests still pass (the in-memory fake hides it).

Purpose: durable state that survives restart + application-layer tenant isolation, the foundation 01-03 (SEC-01/02) sits on.
Output: widened protocol, migrated callers, `RepositorySQL` over the existing `0001`/`0002` schema, `DATABASE_URL` routing, and a SQL-backed test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-durable-core-approval-security/01-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- Confirmed from the codebase this session. Executor uses these directly. -->

Current Repository protocol (src/agent_mesh/services/repository.py) — WIDEN these two and ADD two:
  list_events(self, task_id: str) -> list[TaskEvent]                 # ADD tenant_id param (DUR-02)
  list_evaluations(self, proposal_id: str) -> list[EvaluationResult] # ADD tenant_id param (DUR-02)
  # ADD: list_tool_calls(self, task_id: str, tenant_id: str) -> list[ToolCall]      (replaces runner reach-in)
  # ADD: list_approvals(self, task_id: str, tenant_id: str) -> list[ApprovalRecord] (replaces test reach-ins)

Private reach-ins to migrate (grep-confirmed):
  src/agent_mesh/worker/runner.py:120  -> store = getattr(self._repo, "_tool_calls", {})
  tests/test_approval_gating.py:57,73,84 -> repo._approvals
  tests/smoke.py:35 -> repo._approvals

Schema columns (migrations/0001_init.sql):
  tasks(task_id, tenant_id, client_slug, entrypoint, session_id, requester_id, requester JSONB, prompt,
        state, model_route_profile, result_summary, error, created_at, updated_at)
  task_metadata(task_id, tenant_id, key, value JSONB, updated_at)   # TaskRecord.metadata lands HERE
  task_events(event_id, task_id, tenant_id, state, note, payload JSONB, created_at)
  sessions(session_id, tenant_id, client_slug, entrypoint, requester_id, active_task_id, created_at, updated_at)
  tool_calls(tool_call_id, task_id, tenant_id, tool_name, category, approval_required, status,
             parameters JSONB, result JSONB, requester_id, model_route, approval_record_id, created_at)
  approval_records(approval_record_id, approval_request_id, task_id, tenant_id, tool_call_id, decision,
                   approver_id, channel, payload_hash, decided_at, created_at)

Models use ConfigDict(use_enum_values=True) -> task.state may already be a str; coerce with str(...) on write.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen Repository protocol and migrate all private reach-ins (the linchpin — do this FIRST)</name>
  <read_first>
    - src/agent_mesh/services/repository.py (Repository protocol + InMemoryRepository)
    - src/agent_mesh/worker/runner.py (lines around 90-121: _resume_after_approval + _pending_calls)
    - tests/test_approval_gating.py (lines 57, 73, 84 — repo._approvals reach-ins)
    - tests/smoke.py (line 35 — repo._approvals reach-in)
  </read_first>
  <action>
    In `repository.py` Repository protocol: add a `tenant_id: str` parameter to `list_events` and `list_evaluations` (DUR-02); add two new methods `list_tool_calls(self, task_id: str, tenant_id: str) -> list[ToolCall]` and `list_approvals(self, task_id: str, tenant_id: str) -> list[ApprovalRecord]`. Implement all four in `InMemoryRepository` — the two list_* additions iterate the existing `_tool_calls`/`_approvals` dicts filtering by BOTH `task_id` and `tenant_id`; the two widened methods add the `tenant_id` filter to their existing comprehensions so a cross-tenant read returns an empty list.
    Migrate `runner.py:_pending_calls` off `getattr(self._repo, "_tool_calls", {})` to `self._repo.list_tool_calls(task_id, task.tenant_id)` — fetch `task` in `_resume_after_approval` (already available there) and pass its `tenant_id` through.
    Migrate the test reach-ins: in `test_approval_gating.py` (lines 57, 84) and `smoke.py` (line 35), replace `list(repo._approvals.keys())` with the new `repo.list_approvals(task.task_id, "t")` (tenant matches the test's tenant_id="t") and take the single record's `approval_record_id`; replace the `len(repo._approvals) == 0` assertion (line 73) with `repo.list_approvals(task.task_id, "t") == []`.
    Do NOT write any SQL in this task. This task is verifiable entirely against the in-memory fake.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_approval_gating.py -x` exits 0 (migrated tests pass against InMemoryRepository).
    - Grep gate (role grep-gate hygiene — filter comments first): `grep -rn 'repo\._\(tool_calls\|approvals\)\|_repo\._\(tool_calls\|approvals\)' src tests | grep -v '^\s*[0-9]*:\s*#'` returns ONLY the `self._tool_calls`/`self._approvals` definition/use lines inside `repository.py` (no caller in runner.py/tests references private dicts).
    - `Repository` protocol declares `list_tool_calls`, `list_approvals`, and `tenant_id` params on `list_events`/`list_evaluations` (source assertion).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement RepositorySQL (sync psycopg3 ConnectionPool) and route get_repository on DATABASE_URL</name>
  <read_first>
    - src/agent_mesh/services/repository.py (InMemoryRepository is the behavioral source-of-truth RepositorySQL must mirror, now widened by Task 1)
    - migrations/0001_init.sql (column names/types — see <interfaces>)
    - src/agent_mesh/services/sessions.py (ensure_session — SQL path upserts a sessions row)
    - pyproject.toml (runtime extra — add psycopg-pool)
    - .planning/phases/01-durable-core-approval-security/01-RESEARCH.md (Pattern 2, Pitfalls 1/5/6/7, Open Questions)
  </read_first>
  <action>
    Add `psycopg-pool>=3.2` to the `runtime` optional-dependencies list in `pyproject.toml` (it is NOT a transitive dep of `psycopg[binary]`, which is already declared). 01-01 solely owns pyproject.toml this wave.
    Implement `RepositorySQL` in `repository.py` mirroring every InMemoryRepository method (all 18 protocol methods + the 4 from Task 1). Construction: one module-level-style `psycopg_pool.ConnectionPool(conninfo=dsn, min_size=1, max_size=8, open=True)`. GOTCHA (Pitfall 1): pass any connection options via the pool `kwargs=`, NEVER appended to the DSN string. Each method borrows a connection via `with self._pool.connection() as conn:` and uses parameterized `%s` placeholders only (never string-format SQL). Adapt dict/JSONB columns with `psycopg.types.json.Jsonb`.
    Resolve the four research Open Questions explicitly: (a) Pitfall 5 — `create_task` upserts non-empty `TaskRecord.metadata` keys into `task_metadata` rows (do NOT silently drop; `tasks` has no metadata column); (b) `ensure_session`/`RepositorySQL` upserts a real `sessions` row (DUR-01 says sessions survive restart) — add a session upsert method or upsert in create_task's session path; (c) migrations are applied EXTERNALLY (`psql -f`/`make migrate`) — `RepositorySQL` does NOT self-apply migrations on connect (keeps the CONCURRENTLY/autocommit concern out of Phase 1); (d) use SYNC `psycopg_pool.ConnectionPool` only — NO async, NO LangGraph checkpointer setup (that is Phase 2 / ORCH-02).
    `transition_task` is two writes (UPDATE tasks + INSERT task_events) — wrap them in ONE `with conn:` transaction (Pitfall 7) so a crash cannot advance state without the matching audit row. Coerce state with `str(task.state)` on write (Pitfall 6). List reads (`list_events`, `list_evaluations`, `list_tool_calls`, `list_approvals`) add `WHERE ... AND tenant_id = %s` (DUR-02).
    Route `get_repository()` on `settings.database_url` (already in settings.py): when set -> return a process-wide `RepositorySQL(database_url)`; when unset -> keep the InMemoryRepository singleton fallback unchanged.
  </action>
  <acceptance_criteria>
    - `class RepositorySQL` exists in repository.py and implements all four Task-1 protocol additions plus the original 18 methods (source assertion: each protocol method name resolves to a RepositorySQL method).
    - `pyproject.toml` `runtime` extra contains `psycopg-pool` (source assertion).
    - `get_repository()` returns `RepositorySQL` when `DATABASE_URL` is set and `InMemoryRepository` when unset (source assertion on the branch reading `settings.database_url`).
    - `transition_task` performs the UPDATE and the task_events INSERT inside a single `with conn:` block (source assertion — no two separately-committed execs).
    - No SQL string in RepositorySQL uses f-string/`%`-format interpolation of values; all use `%s` parameters (source assertion).
    - `pytest -q` exits 0 with `DATABASE_URL` unset (in-memory fallback suite unaffected).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SQL-backed test suite + pg conftest fixture (round-trip, restart-survival, cross-tenant, resume-executes-write)</name>
  <read_first>
    - tests/conftest.py (existing `repo` fixture pattern)
    - tests/test_approval_gating.py (resume pause->approve->complete flow to mirror against SQL)
    - src/agent_mesh/worker/runner.py (resume path — the test must drive it against RepositorySQL)
    - migrations/0001_init.sql, migrations/0002_self_improvement.sql (applied to the test DB by the fixture)
  </read_first>
  <behavior>
    - Round-trip: create_task with non-empty metadata, re-read -> task fields AND task_metadata survive (catches Pitfall 5).
    - Restart-survival: BEFORE the pool drop, persist all four DUR-01 state families: (a) a queued task + a transition (writes a task row + a task_events row), (b) a session row via the SQL session-upsert path (Task 2 (b)), and (c) at least one approval_record OR tool_call row. Drop the pool, open a NEW RepositorySQL on the same DSN, and assert ALL of the following are readable after reopen: the task row, its task_events audit row, the session row, AND the approval_record/tool_call row. DUR-01 names "tasks, sessions, approvals, and tool-calls survive a worker restart" — asserting only task + task_events under-covers the requirement (Success Criterion 1).
    - Cross-tenant: write events/approvals under tenant "t1"; `list_events(task_id, "t2")` and `list_approvals(task_id, "t2")` return `[]` (DUR-02).
    - Resume-executes-write: drive the worker pause->approve->resume path against RepositorySQL and assert the approved tool_call reaches EXECUTED status (catches Pitfall 3 — the silent-drop bug the whole phase exists to close; this MUST run against SQL, not the fake).
  </behavior>
  <action>
    Add an optional pg fixture to `tests/conftest.py` gated on `TEST_DATABASE_URL`: `pytest.skip` (or skipif) when the env var is unset (matches TESTING.md "no external deps for tests"); when set, the fixture creates a `RepositorySQL(TEST_DATABASE_URL)`, applies `migrations/0001_init.sql` and `0002_self_improvement.sql` to that DB (executor applies them via psycopg — the repo does not self-apply), and truncates/cleans between tests for isolation. IMPORTANT: `0001_init.sql:15` runs `CREATE EXTENSION IF NOT EXISTS vector;` — `IF NOT EXISTS` does NOT install pgvector, so `TEST_DATABASE_URL` MUST point at a pgvector-enabled Postgres (e.g. the `pgvector/pgvector:pg16` image), not a vanilla `postgres` image, or migration application fails before any table is created. (pgcrypto on line 16 ships with contrib; pgvector does not.)
    Create `tests/test_repository_sql.py` implementing the four behaviors above, all guarded by the `TEST_DATABASE_URL` skip so the default `make test` run (no DB) stays green. The resume-executes-write test reuses the `TaskService`/`Worker` pattern from `test_approval_gating.py` but injects the SQL repo.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_repository_sql.py -q` with `TEST_DATABASE_URL` UNSET -> all tests SKIP, exit 0 (no external dep required for default CI).
    - `pytest tests/test_repository_sql.py -q` with `TEST_DATABASE_URL` set to a local Postgres -> all four tests pass, exit 0.
    - The restart-survival test, after the pool-drop/reopen cycle, asserts ALL FOUR DUR-01 state families survive: the task row AND its task_events row are present, a session row written before the drop is readable, AND at least one approval_record or tool_call row written before the drop is readable (behavior assertion — under-coverage of any family fails the gate).
    - The resume-executes-write test asserts the tool_call status is `executed` after approval against RepositorySQL (behavior assertion — fails if Pitfall 3 regresses).
    - `pytest -q` (full suite, no DB) exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Repository protocol | Callers must not depend on storage internals; the protocol is the only contract |
| application → Postgres | Tenant-scoped reads enforced at the application read layer (RLS deferred §3) |
| package install → runtime | psycopg-pool added to dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | list_events / list_evaluations / list_tool_calls / list_approvals | mitigate | `tenant_id` mandated in protocol + `WHERE tenant_id = %s` in RepositorySQL; cross-tenant test asserts `[]` (DUR-02) |
| T-01-02 | Tampering | RepositorySQL queries | mitigate | psycopg3 parameterized `%s` placeholders only; acceptance gate forbids value interpolation in SQL |
| T-01-03 | Repudiation | transition_task (state UPDATE + task_events INSERT) | mitigate | both writes in one `with conn:` transaction so a crash cannot advance state without the audit row (Pitfall 7); restart-survival test proves it |
| T-01-04 | Tampering / Elevation | approved-write resume path on RepositorySQL | mitigate | protocol widened + reach-ins migrated BEFORE SQL exists; SQL-backed resume-executes-write test catches the silent-drop bug (Pitfall 3) |
| T-01-SC | Tampering (supply chain) | `psycopg-pool` install | accept | RESEARCH Package Legitimacy Audit dispositions it "Approved — first-party companion" to already-trusted psycopg (same maintainer/repo, github.com/psycopg/psycopg). slopcheck unavailable in env; human checkpoint judged low-value. Plan stays autonomous. |
</threat_model>

<verification>
- Full suite green with no database: `pytest -q` exits 0.
- Linchpin proven before SQL: Task 1 grep gate returns no private reach-ins outside repository.py.
- DUR-01: restart-survival test (Task 3) passes against a local Postgres (`TEST_DATABASE_URL` set), asserting all four state families (tasks, sessions, approvals/tool-calls) survive the reopen.
- DUR-02: cross-tenant read returns `[]` for both events and approvals.
- Pitfall 3 closed: resume-executes-write test passes against RepositorySQL.
- Test DB note: `TEST_DATABASE_URL` targets a pgvector-enabled Postgres (0001 creates the `vector` extension).
</verification>

<success_criteria>
- Repository protocol widened (tenant_id on the two list reads; list_tool_calls/list_approvals added) and all callers migrated off private dicts.
- RepositorySQL implements every protocol method over the existing schema, with atomic transition_task and parameterized SQL.
- get_repository routes on DATABASE_URL; InMemoryRepository remains the unset fallback.
- SQL-backed tests prove round-trip, restart-survival (all four DUR-01 state families), cross-tenant isolation, and resume-executes-approved-write; all skip cleanly when TEST_DATABASE_URL is unset.
</success_criteria>

<output>
Create `.planning/phases/01-durable-core-approval-security/01-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
