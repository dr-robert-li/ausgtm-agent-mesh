---
phase: 09-local-data-telemetry-plane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - tests/conftest.py
  - tests/test_local_data_plane.py
  - .env.example
  - RUNBOOK.md
autonomous: true
requirements: [LDATA-01, LDATA-02, LDATA-03]

must_haves:
  truths:
    # Derived from ROADMAP §"Phase 9" Success Criteria 1-3 (L238-240), NOT back-derived from tasks.
    - "A local Postgres(pgvector) run path is documented + make-wired to DATABASE_URL/TEST_DATABASE_URL and applies the existing migrations (SC-1)"
    - "A self-hosted Langfuse local run path is documented with its env wiring (LANGFUSE_HOST etc.) for local trace ingestion (SC-2)"
    - "The durable lane runs locally end to end via `make test-pg` against the local DSN; a test asserts the local-DSN path applies migrations and is reachable, loud-skipping when unset (SC-3)"
    - "Zero src/ change: `git diff e9d4104..HEAD -- src/` is empty"
  artifacts:
    - path: "Makefile"
      provides: "best-effort run-pg target (single pgvector/pgvector:pg16 container) + extended test-pg file list"
      contains: "run-pg:"
    - path: "tests/test_local_data_plane.py"
      provides: "DSN-gated DDL schema-assertion test for 0003 columns + 0004 tables + reachability"
      contains: "def test_"
    - path: "tests/conftest.py"
      provides: "docstring-corrected _apply_migrations (functional body UNCHANGED — already applies 0001->0004)"
      contains: "_MIGRATIONS"
    - path: ".env.example"
      provides: "pinned dev-only local DSN defaults + local LANGFUSE_HOST; keys left blank"
      contains: "TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agent_mesh"
    - path: "RUNBOOK.md"
      provides: "new Local data & telemetry plane section (Postgres run path + Langfuse self-host doc-only path)"
      contains: "Local data & telemetry plane"
  key_links:
    - from: "Makefile run-pg container creds/port/db"
      to: ".env.example DATABASE_URL / TEST_DATABASE_URL default"
      via: "shared literal postgres:postgres@localhost:5432/agent_mesh (D-01 <-> D-04 lockstep)"
      pattern: "postgres:postgres@localhost:5432/agent_mesh"
    - from: "Makefile test-pg file list"
      to: "tests/test_local_data_plane.py"
      via: "explicit pytest file path appended to the named durable lane"
      pattern: "tests/test_local_data_plane.py"
    - from: "tests/test_local_data_plane.py"
      to: "tests/conftest.py:pg_dsn fixture"
      via: "test function takes pg_dsn (reuses loud-skip + 0001->0004 apply)"
      pattern: "pg_dsn"
---

<objective>
Document and wire a fully local persistence + observability plane so the durable lane
runs on-box: local Postgres(pgvector) replacing Cloud SQL, and self-hosted Langfuse
replacing cloud Langfuse, for local dev. Delivered through Makefile targets + RUNBOOK
docs + ONE test + `.env.example` wiring + a conftest docstring fix — ZERO `src/` change
(same milestone guardrail as Phase 8).

Purpose: Phase 10's full-stack compose and Phase 11's offline posture both consume a
local Postgres + Langfuse run path. This phase stands up the single best-effort Postgres
container, pins copy-paste local defaults, documents the upstream Langfuse self-host path,
and adds a DDL-level schema-assertion test proving migrations 0003/0004 land in the live
migrated schema (no existing test asserts this at the DDL layer).

Output: `make run-pg` target; extended `test-pg` lane; `tests/test_local_data_plane.py`;
corrected conftest docstring; pinned `.env.example` local defaults; new RUNBOOK section.

CRITICAL D-03 reconciliation (do NOT regress): CONTEXT D-03's premise that
`_apply_migrations` "currently applies only 0001/0002" is FACTUALLY STALE (committed
9011377, 2026-06-07). The `_MIGRATIONS` tuple (conftest L8-13) ALREADY lists 0001->0004
and `_truncate` (L14-27) already covers the 0003/0004 tables. Applier-completeness is
ALREADY DONE. The ONLY conftest change is the stale L37 docstring ("Apply 0001/0002").
Do NOT touch the loop body, the comment-strip regex, the `;`-split, or the tuple. Do NOT
swap the tuple for a directory glob (behavior change — would auto-apply a future 0005 and
risk non-migration `.sql` files). The genuinely-new LDATA-03 deliverable is the
schema-assertion TEST, not an applier fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-local-data-telemetry-plane/09-CONTEXT.md
@.planning/phases/09-local-data-telemetry-plane/09-RESEARCH.md
@.planning/phases/09-local-data-telemetry-plane/09-PATTERNS.md

<interfaces>
<!-- Ground-truth extracted from the codebase (2026-06-10). Use directly — no exploration. -->

Existing tests/conftest.py (CURRENT STATE — applier ALREADY applies all four):
- _MIGRATIONS = ("0001_init.sql", "0002_self_improvement.sql", "0003_tool_call_fields.sql", "0004_active_version.sql")  # L8-13
- _apply_migrations docstring L37 reads "Apply 0001/0002 to the test DB." — STALE, the ONLY thing to fix; keep the pgvector-image NOTE that follows.
- Applier loop strips `--` comments via re.sub(r"(?m)--.*$", "", raw), splits on ';' — DO NOT TOUCH.
- pg_dsn fixture L74-87 — REUSE in the new test: skips on unset TEST_DATABASE_URL, then _apply_migrations(0001->0004), then _truncate, returns dsn.

Ground-truth migration objects to assert (read from the actual DDL):
- migrations/0003_tool_call_fields.sql L19-21 — adds columns to tool_calls: integration_style, schema_validation, is_read
- migrations/0004_active_version.sql L20-45 — creates tables: self_improvement_active_version, ai_bom_snapshots

Existing Makefile analog: run-vllm/run-ollama best-effort block L70-84; test-pg lane L47-49; .PHONY L1; help L20-24.

Existing .env.example lines: DATABASE_URL= (L13, blank); LANGFUSE_HOST=https://cloud.langfuse.com (L27); LANGFUSE_PUBLIC_KEY= (L44, blank); LANGFUSE_SECRET_KEY= (L45, blank). NOTE: TEST_DATABASE_URL has NO existing line — ADD one.

RUNBOOK insertion point: new "## Local data & telemetry plane" section goes AFTER the "Local inference lane" section (ends L183, after "### api_base path gotcha") and BEFORE "## Credentials & live lane" (L184).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Makefile — add best-effort run-pg target and wire the new test into test-pg (LDATA-01)</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (being modified): .PHONY L1, help L20-24, test-pg lane L47-49, the run-vllm/run-ollama best-effort block L70-84 (the EXACT analog to mirror)
    - .planning/phases/09-local-data-telemetry-plane/09-PATTERNS.md (Makefile section: three discrete edits, exact comment tone)
  </read_first>
  <action>
    Three discrete edits to Makefile, modeling the existing run-vllm/run-ollama block verbatim for comment tone and structure.

    Edit 1 — .PHONY (L1): append run-pg to the existing .PHONY list (currently ends "... use-vllm use-ollama use-cloud"). Single new target only — no migrate/db-up alias (D-01 locks one target).

    Edit 2 — add the run-pg target after the local-inference block (after L84). Lead with a comment header stating it is best-effort, operator-run, and DELIBERATELY never wired into test or CI (it requires Docker and may fail acceptably on a box without it, documented in RUNBOOK.md), and that creds/port/db MUST match the pinned DATABASE_URL default in .env.example (D-01 <-> D-04). Use overridable vars: PG_IMAGE defaulting to pgvector/pgvector:pg16, PG_CONTAINER defaulting to agent_mesh_pg, PG_VOLUME defaulting to agent_mesh_pgdata. The recipe runs a single persistent detached container: docker run -d --name PG_CONTAINER with env POSTGRES_USER=postgres, POSTGRES_PASSWORD=postgres, POSTGRES_DB=agent_mesh, publish -p 5432:5432, mount -v PG_VOLUME:/var/lib/postgresql/data, image PG_IMAGE. Use persistent -d + named volume (RESEARCH Q3) so data survives restarts and make test-pg can re-run. Also add a help: echo line for run-pg mirroring the existing run-target help entries (e.g. "run-pg       Start a local pgvector Postgres (best-effort, operator-only, never CI)").

    Edit 3 — extend the test-pg target file list (L48-49) by appending tests/test_local_data_plane.py to the existing two explicit files (tests/e2e/test_e2e_mcp_durable_job_live.py and tests/test_checkpointer_resume.py). Keep the lane explicit and the -m "not live" selector intact — this is what makes the new LDATA-03 assertion actually run under the named durable lane (RESEARCH Pitfall 3). Do NOT restructure the lane; only append the file.

    Locked values (do NOT alter): image pgvector/pgvector:pg16; creds postgres:postgres; port 5432; db agent_mesh. Do NOT wire run-pg into test/CI. Do NOT add a make run-langfuse target (D-02 forbids it).
  </action>
  <verify>
    <automated>grep -q '^run-pg:' Makefile && grep -q 'pgvector/pgvector:pg16' Makefile && grep -q 'tests/test_local_data_plane.py' Makefile && grep '^\.PHONY' Makefile | grep -q 'run-pg' && ! grep -A2 '^test:' Makefile | grep -q 'run-pg' && ! grep -q 'run-langfuse' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^run-pg:' Makefile` (target exists)
    - `grep -q 'pgvector/pgvector:pg16' Makefile` (image pinned)
    - `grep -q 'POSTGRES_PASSWORD=postgres' Makefile` AND `grep -q 'POSTGRES_DB=agent_mesh' Makefile` AND `grep -q '5432:5432' Makefile` (creds/port/db lockstep with D-04)
    - The .PHONY line (L1) contains run-pg
    - test-pg target file list contains `tests/test_local_data_plane.py` (Pitfall 3 wiring)
    - The `test:` target body does NOT reference run-pg (never in CI)
    - No run-langfuse target added: `! grep -q 'run-langfuse' Makefile`
    - `git diff e9d4104..HEAD -- src/` is empty (zero-src guardrail)
  </acceptance_criteria>
  <done>
    Makefile has a best-effort run-pg target (single pgvector/pgvector:pg16 container, persistent -d + named volume, creds/port/db matching the pinned DSN), a help entry, run-pg in .PHONY, and the test-pg file list extended with tests/test_local_data_plane.py. run-pg is never referenced by test/CI; no run-langfuse target exists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: conftest docstring fix + new DSN-gated DDL schema-assertion test (LDATA-03)</name>
  <files>tests/conftest.py, tests/test_local_data_plane.py</files>
  <read_first>
    - tests/conftest.py (being modified): _MIGRATIONS L8-13 (ALREADY 0001->0004), _apply_migrations L36-64 (the STALE L37 docstring is the ONLY change), pg_dsn fixture L74-87 (REUSE — do not rebuild)
    - migrations/0003_tool_call_fields.sql L19-21 (ground-truth columns: integration_style, schema_validation, is_read)
    - migrations/0004_active_version.sql L20-45 (ground-truth tables: self_improvement_active_version, ai_bom_snapshots)
    - tests/test_checkpointer_resume.py (skip-gating SHAPE analog for a DSN-gated Postgres test)
    - .planning/phases/09-local-data-telemetry-plane/09-PATTERNS.md (conftest + new-test sections; D-03 reconciliation)
  </read_first>
  <behavior>
    - With TEST_DATABASE_URL UNSET: the new test loud-skips via the existing pg_dsn fixture (message "TEST_DATABASE_URL unset; ...") — make test stays green and DB-free.
    - With TEST_DATABASE_URL set to a local pgvector DSN: the test asserts tool_calls has columns integration_style, schema_validation, is_read (0003); asserts tables self_improvement_active_version and ai_bom_snapshots exist in schema public (0004); asserts DB reachable (SELECT 1 returns 1).
  </behavior>
  <action>
    Edit 1 — tests/conftest.py, DOCSTRING ONLY. Change the _apply_migrations docstring first line (L37) from "Apply 0001/0002 to the test DB." to state it applies ALL migrations in _MIGRATIONS (0001->0004) in lexical order. Keep the existing pgvector-image NOTE intact. Do NOT touch the loop body, the comment-strip regex re.sub matching double-dash-to-end-of-line, the semicolon split, the _MIGRATIONS tuple, or _truncate. Do NOT swap the tuple for a directory glob — that is a forbidden behavior change (RESEARCH Q1 / PATTERNS D-03 note). This task changes ZERO applier behavior; it only corrects a stale comment.

    Edit 2 — create tests/test_local_data_plane.py (NEW, test-code only). Import psycopg. Write a test function that takes the existing pg_dsn fixture as its only argument (this REUSES the loud-skip gate + the 0001->0004 apply + truncate — add NO new skip helper). Inside, psycopg.connect(pg_dsn) and introspect the LIVE migrated schema via information_schema: query information_schema.columns WHERE table_name='tool_calls' and assert the set {"integration_style", "schema_validation", "is_read"} is a subset (<=) of the returned column names (0003); query information_schema.tables WHERE table_schema='public' and assert {"self_improvement_active_version", "ai_bom_snapshots"} is a subset (<=) of returned table names (0004); assert SELECT 1 returns 1 (reachability). Use subset (<=) assertions, NOT equality — the migrated schema has many more objects; assert the specific 0003/0004 objects are PRESENT.

    This test rides the existing test-pg lane (wired in Task 1) AND collects under plain make test where it loud-skips via pg_dsn.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_local_data_plane.py 2>&1 | grep -Eq 'skipped|passed'; grep -q 'def _apply_migrations' tests/conftest.py && ! grep -q 'Apply 0001/0002 to the test DB' tests/conftest.py && grep -q 'pg_dsn' tests/test_local_data_plane.py</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'Apply 0001/0002 to the test DB' tests/conftest.py` (stale docstring corrected)
    - conftest applier body UNCHANGED: `_MIGRATIONS` still the explicit 4-tuple; no directory glob introduced (`! grep -q 'glob' tests/conftest.py`); `_apply_migrations` loop/regex/split untouched
    - tests/test_local_data_plane.py exists, imports psycopg, and its test function takes `pg_dsn` (reuses loud-skip): `grep -q 'pg_dsn' tests/test_local_data_plane.py`
    - Test asserts the three 0003 columns AND the two 0004 tables AND reachability (subset assertions, source-visible)
    - Behavior: `PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_local_data_plane.py` (TEST_DATABASE_URL unset) reports skipped, exits 0 — no hard failure
    - `git diff e9d4104..HEAD -- src/` is empty (zero-src guardrail; this is test-code only)
  </acceptance_criteria>
  <done>
    conftest _apply_migrations docstring corrected to reflect 0001->0004 (functional body untouched, tuple kept). tests/test_local_data_plane.py exists, depends on pg_dsn, and asserts the 0003 tool_calls columns + 0004 tables + reachability against the live migrated schema; loud-skips when TEST_DATABASE_URL is unset.
  </done>
</task>

<task type="auto">
  <name>Task 3: .env.example local defaults + RUNBOOK Local data & telemetry plane section (LDATA-01/02)</name>
  <files>.env.example, RUNBOOK.md</files>
  <read_first>
    - .env.example (being modified): DATABASE_URL= L13, LANGFUSE_HOST L27, LANGFUSE_PUBLIC_KEY L44, LANGFUSE_SECRET_KEY L45
    - RUNBOOK.md (being modified): "Local inference lane" section L96-183 (structure/honesty-tone analog), "Postgres durable checkpointer lane" L72-94 (DSN/test-pg/pgvector facts to stay consistent with), "## Credentials & live lane" L184 (the section the new one precedes)
    - .planning/phases/09-local-data-telemetry-plane/09-PATTERNS.md (.env.example + RUNBOOK sections; Langfuse client-vs-server key boundary, Pitfall 4)
    - .planning/phases/09-local-data-telemetry-plane/09-RESEARCH.md (Pattern 2 doc-only Langfuse; Pitfall 4 client/server keys; v3 stack)
  </read_first>
  <action>
    Edit set A — .env.example (D-04, dev-only local defaults; creds/port/db LOCKED to the run-pg container):
    - L13: edit the blank DATABASE_URL to DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agent_mesh
    - Add a NEW line adjacent to L13 (TEST_DATABASE_URL has NO existing line): TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agent_mesh, with a one-line comment that it is the DSN-gated make test-pg lane var.
    - L27: edit LANGFUSE_HOST to the self-host default http://localhost:3000 (move/keep the cloud value as a trailing comment note).
    - L44/L45: LEAVE BLANK — LANGFUSE_PUBLIC_KEY and LANGFUSE_SECRET_KEY stay empty (UI-generated per-project; Pitfall 4/5). Do NOT pin secrets into the tracked .env.example.
    - Add a short comment near the DSN lines disclosing these are dev-only local defaults; operators copy .env.example to an untracked .env.

    Edit set B — RUNBOOK.md: add a NEW top-level section "## Local data & telemetry plane" AFTER the "Local inference lane" section (after L183, before "## Credentials & live lane" at L184). Two subsections:

    (1) Local Postgres run path: mirror the inference-lane honesty tone — state run-pg is best-effort, operator-only, and never CI, and fails acceptably on a box without Docker. Carry the pgvector-image fact already at L82-84 (a vanilla postgres image fails CREATE EXTENSION vector — use pgvector/pgvector:pg16) without contradicting it. Include a copy-paste operator block tying run-pg to make test-pg to the pinned DSN: run "make run-pg", then export TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agent_mesh, then "make test-pg" (DSN-gated; loud-skips when unset); teardown "docker rm -f agent_mesh_pg" (optional "docker volume rm agent_mesh_pgdata"). State the .env.example DSN defaults are dev-only.

    (2) Self-hosted Langfuse (DOCUMENTATION-ONLY — D-02 ships NO standup target): document the upstream self-host path (git clone the langfuse repo, then docker compose up; UI at http://localhost:3000; v3 stack = web+worker+postgres+clickhouse+redis/valkey+minio — mark this datable/MEDIUM-confidence, re-verify before Phase 10). Make the client-vs-server key boundary explicit (Pitfall 4): the mesh's LANGFUSE_PUBLIC_KEY/LANGFUSE_SECRET_KEY are UI-generated per-project keys the operator creates after first boot and exports into their .env — NOT server env vars; server-side secrets (NEXTAUTH_SECRET, SALT, ENCRYPTION_KEY, DATABASE_URL, ClickHouse/Redis/S3) live in the upstream compose CHANGEME lines and are Phase 10 territory. State telemetry loud-skips when keys are unset (existing behavior, no src change), and that the full multi-container standup is deferred to Phase 10's compose. Do NOT document a make run-langfuse target — it does not exist and D-02 forbids it.
  </action>
  <verify>
    <automated>grep -q 'TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agent_mesh' .env.example && grep -q 'DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agent_mesh' .env.example && grep -q 'LANGFUSE_HOST=http://localhost:3000' .env.example && grep -q 'Local data & telemetry plane' RUNBOOK.md && grep -q 'localhost:3000' RUNBOOK.md && ! grep -q 'run-langfuse' RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agent_mesh` (L13 edited)
    - `.env.example` contains a NEW `TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agent_mesh` line
    - `.env.example` contains `LANGFUSE_HOST=http://localhost:3000`
    - `.env.example` LANGFUSE_PUBLIC_KEY and LANGFUSE_SECRET_KEY remain blank (no secret pinned): the lines end with `=` and nothing after
    - RUNBOOK.md contains a `## Local data & telemetry plane` heading positioned after the Local inference lane section and before `## Credentials & live lane`
    - RUNBOOK Postgres subsection contains the `make run-pg` / `make test-pg` copy-paste block and the pinned DSN, and discloses dev-only + best-effort + never-CI
    - RUNBOOK Langfuse subsection documents the upstream docker compose self-host path, `http://localhost:3000`, and the client-vs-server key boundary; states standup deferred to Phase 10
    - No `make run-langfuse` documented: `! grep -q 'run-langfuse' RUNBOOK.md`
    - `git diff e9d4104..HEAD -- src/` is empty (zero-src guardrail)
  </acceptance_criteria>
  <done>
    .env.example pins dev-only local defaults (DATABASE_URL + new TEST_DATABASE_URL = the run-pg DSN, LANGFUSE_HOST=http://localhost:3000) with keys left blank and a dev-only disclosure. RUNBOOK has a new "Local data & telemetry plane" section with a Postgres run path (run-pg/test-pg copy-paste, pgvector-image fact, honest dev-only/best-effort/never-CI disclosure) and a documentation-only Langfuse self-host subsection (upstream compose path, UI port 3000, client-vs-server key boundary, standup deferred to Phase 10). No run-langfuse target documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → local Docker | `make run-pg` starts a localhost-bound container; no network-exposed surface, no untrusted input crosses it. |
| tracked `.env.example` → operator `.env` | `.env.example` is a template the operator copies; it carries dev-only NON-secret defaults, never real credentials. |

This phase touches only Makefile/RUNBOOK/.env.example/tests — no runtime/`src/` code, no
network-exposed endpoint, no request-handling surface. The honest dominant concern is
dev-only credentials in `.env.example` + RUNBOOK, addressed below.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `postgres:postgres@localhost:5432` DSN pinned in `.env.example` + RUNBOOK | accept | These are NON-secret, localhost-only, dev-only defaults — not real credentials and not a deployed posture. They are disclosed AS dev-only in both `.env.example` (comment) and RUNBOOK; production secrets come from Secret Manager (existing `.env.example` header). Pinning them is required so `make test-pg` works out-of-box (D-04). No real secret is committed. |
| T-09-02 | Information Disclosure | `LANGFUSE_PUBLIC_KEY` / `LANGFUSE_SECRET_KEY` | mitigate | Left BLANK in `.env.example`; documented as UI-generated per-project keys the operator exports into an untracked `.env`. No Langfuse secret is committed (Pitfall 4/5). |
| T-09-03 | Elevation of Privilege | best-effort `run-pg` operator target | accept | Operator-only, localhost-bound container, never wired into `test`/CI; identical risk posture to the accepted Phase 8 `run-vllm`/`run-ollama` operator targets. No automated/remote invocation path. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No language-package installs this phase (RESEARCH Package Legitimacy Audit: none). Only Docker images (`pgvector/pgvector:pg16` pre-existing in-repo; `langfuse` documented-path-only, not pulled by any P9 target). slopcheck N/A — no PyPI/npm/crates installs. |
</threat_model>

<verification>
Phase-level checks (config/docs/test only):

1. Zero-src guardrail (the hard milestone constraint): `git diff e9d4104..HEAD -- src/` is empty.
2. Default lane stays green and DB-free: `make test` passes (the new test loud-skips on unset TEST_DATABASE_URL via pg_dsn).
3. New test wired into the named durable lane: `grep -q 'tests/test_local_data_plane.py' Makefile` (in the test-pg file list).
4. D-01 <-> D-04 lockstep: the run-pg container creds/port/db (`postgres:postgres`, `5432`, `agent_mesh`) match the pinned DSN literal in .env.example.
5. conftest applier behavior unchanged: explicit 4-tuple kept, no directory glob (`! grep -q 'glob' tests/conftest.py`), stale docstring corrected.
6. No forbidden targets: `! grep -q 'run-langfuse' Makefile RUNBOOK.md`.
7. (Operator, optional) With a real pgvector DSN exported, `make test-pg` runs the new schema assertion and it passes.
</verification>

<success_criteria>
- LDATA-01: a local Postgres(pgvector) run path (`make run-pg`, single pgvector/pgvector:pg16 container) is documented in RUNBOOK and make-wired; `.env.example` pins matching DATABASE_URL/TEST_DATABASE_URL; the existing migrations apply via the conftest fixture under `make test-pg`.
- LDATA-02: a self-hosted Langfuse local run path is documented (upstream docker compose, UI port 3000, client-vs-server key boundary) with env wiring (LANGFUSE_HOST pinned local, keys blank/UI-generated); standup deferred to Phase 10.
- LDATA-03: `tests/test_local_data_plane.py` asserts the 0003 columns + 0004 tables exist in the migrated schema + DB reachability, runs under `make test-pg`, and loud-skips when TEST_DATABASE_URL is unset; the stale conftest docstring is corrected with zero applier-behavior change.
- Zero `src/` change across all three tasks.
</success_criteria>

<output>
Create `.planning/phases/09-local-data-telemetry-plane/09-01-SUMMARY.md` when done.
</output>
