---
phase: 06-self-improvement
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/agent_mesh/services/eval_harness.py
  - src/agent_mesh/services/self_improvement.py
  - tests/test_eval_harness.py
  - tests/fixtures/holdout_baseline.json
autonomous: true
requirements: [SI-01, SI-01a, SI-01b, SI-01c]
must_haves:
  truths:
    - "evaluate_proposal scores a candidate via a real Langfuse run_experiment over a versioned held-out dataset, not the boolean stub"
    - "The held-out items are deterministic frozen-context snapshots so candidate and baseline see identical inputs"
    - "Promotion-eligibility requires candidate >= baseline aggregate AND no item-level regression beyond a configured threshold"
    - "The whole harness + gate runs creds-free in the default lane (make test)"
  artifacts:
    - path: "src/agent_mesh/services/eval_harness.py"
      provides: "Creds-free run_experiment wrapper + pure-Python no-regression gate + frozen-item builder + held-out pool accessor"
      min_lines: 60
    - path: "src/agent_mesh/services/self_improvement.py"
      provides: "evaluate_proposal body swapped to call eval_harness; pending/can_run contract preserved"
    - path: "tests/fixtures/holdout_baseline.json"
      provides: "Committed golden baseline EvaluationResult scores keyed by version (reproducible offline)"
  key_links:
    - from: "src/agent_mesh/services/self_improvement.py"
      to: "src/agent_mesh/services/eval_harness.py"
      via: "evaluate_proposal calls the harness scoring engine + no-regression gate"
      pattern: "eval_harness"
    - from: "src/agent_mesh/services/eval_harness.py"
      to: "langfuse.run_experiment"
      via: "creds-free local experiment run over frozen items"
      pattern: "run_experiment"
---

<objective>
Replace the boolean `evaluate_proposal` stub (the documented `self_improvement.py:149`
scoring stub) with a REAL held-out evaluation harness: a creds-free Langfuse
`run_experiment` over a versioned held-out dataset of deterministic frozen-context
snapshots, feeding a pure-Python no-regression gate (aggregate AND item-level vs a stored
baseline). This is the foundation the proposer loop (06-03) and live judge (06-04) build on.

Purpose: satisfy SI-01 (real harness), SI-01a (held-out pool distinct from optimization
signal — this plan OWNS the held-out pool identity; the zero-overlap test lives in 06-03),
SI-01b (item + run no-regression gate), SI-01c (deterministic frozen items). Keep it all
in the default creds-free lane — `run_experiment` runs locally with no keys (verified in RESEARCH).
Output: `eval_harness.py`, swapped `evaluate_proposal` body, committed golden baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-self-improvement/06-CONTEXT.md
@.planning/phases/06-self-improvement/06-RESEARCH.md
@.planning/phases/06-self-improvement/06-PATTERNS.md
@docs/self-improvement-loop.md

<interfaces>
<!-- Extracted from the codebase. Use these directly. -->

self_improvement.py — the stub body to replace lives in evaluate_proposal (:149-194).
KEEP UNTOUCHED: the can_run=False / pending branch (:165-176) — test_pending_evaluation_does_not_pass
gates on it; the EvaluationResult shape; the repo.upsert_evaluation + _set_status calls; the
ProposalStatus.EVALUATION_PASSED/EVALUATION_FAILED mapping. ONLY swap the scoring engine
(the deterministic_checks block :178-194) to delegate to eval_harness.

EvaluationResult (contracts/models.py :307-328): fields proposal_id, tenant_id, passed: bool,
pending: bool, checks: list[dict], evaluator: str (default "deterministic-stub"), summary: str|None.

langfuse 4.7.1 (VERIFIED creds-free over local data):
- from langfuse import Langfuse        # v4 top-level import — NOT langfuse.callback (DEAD under 4.x)
- from langfuse.experiment import Evaluation
- Langfuse() with no keys -> "disabled": no upload, but run_experiment STILL executes locally.
- lf.run_experiment(name=..., data=<list of {"input":..., "expected_output":..., "metadata":...}>,
    task=<fn(*, item, **_) -> output>, evaluators=[<fn(*, input, output, expected_output, metadata, **_) -> Evaluation>])
  returns ExperimentResult with .item_results (per-item scores).
- Dataset version pinning: get_dataset(name, version=<datetime>) — public path. Do NOT use the
  private _dataset_version= kwarg.

conftest fixtures from 06-01: frozen_holdout_items (deterministic LocalExperimentItem-shaped dicts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: eval_harness.py — creds-free runner + frozen-item builder + held-out pool</name>
  <files>src/agent_mesh/services/eval_harness.py, tests/test_eval_harness.py</files>
  <read_first>
    - src/agent_mesh/observability.py (the langfuse lazy-optional-import + creds-free-degrade pattern; v4 import paths)
    - .planning/phases/06-self-improvement/06-PATTERNS.md (eval_harness.py section: Patterns 1+2; anti-patterns)
    - .planning/phases/06-self-improvement/06-RESEARCH.md (Pattern 1 runner, Pattern 3 held-out isolation)
    - tests/conftest.py (frozen_holdout_items fixture added in 06-01)
  </read_first>
  <behavior>
    - run_candidate(items, task_fn) returns an ExperimentResult-shaped object with per-item scores, with NO Langfuse keys set (creds-free).
    - build_frozen_items(snapshots) produces deterministic LocalExperimentItem dicts: the same snapshot input always yields the same item dict (SI-01c).
    - held_out_item_ids() returns a stable, importable set of item_ids — the held-out pool identity (consumed by 06-03's zero-overlap test).
    - The default-lane task fn is a deterministic offline replay (no model call).
  </behavior>
  <action>
    Create `src/agent_mesh/services/eval_harness.py`. Lazily import `langfuse` (mirror
    `observability.py`'s feature-gated import — never crash a default-lane import). Provide:
    (1) `build_frozen_items(snapshots) -> list[dict]` freezing each context snapshot into a
    deterministic `{"input":..., "expected_output":..., "metadata": {"item_id": <stable id>}}`
    dict (SI-01c determinism); (2) `held_out_pool() -> list[dict]` and `held_out_item_ids() -> set[str]`
    returning the versioned held-out set + its stable item_ids (this is the held-out POOL IDENTITY
    for SI-01a — keep it a distinct importable surface; the proposer must never read it);
    (3) `run_candidate(items, task_fn, *, evaluators) -> object` calling `Langfuse().run_experiment(...)`
    creds-free over local data; (4) a deterministic default `exact_match` evaluator returning
    `Evaluation(name="exact_match", value=float(output == expected_output))`. Do NOT import
    `live_creds` and do NOT use the private `_dataset_version=` kwarg (use `get_dataset(name, version=)`
    when a version is supplied). Make iteration-cap / held-out-size / threshold values config-driven
    defaults (read from settings or module constants, not hardcoded literals at call sites).
    Create `tests/test_eval_harness.py::test_frozen_items_deterministic` (same snapshot -> identical
    item dict twice) and `::test_run_candidate_creds_free` (run with no env keys, assert item_results present).
  </action>
  <verify>
    <automated>python -m pytest -q -m "not live" tests/test_eval_harness.py::test_frozen_items_deterministic tests/test_eval_harness.py::test_run_candidate_creds_free</automated>
  </verify>
  <acceptance_criteria>
    - `eval_harness.py` imports cleanly with NO Langfuse keys set; `python -c "import agent_mesh.services.eval_harness"` exits 0.
    - `held_out_item_ids()` returns a deterministic non-empty set; importable from another module.
    - `test_frozen_items_deterministic` proves identical inputs across two builds (SI-01c).
    - `test_run_candidate_creds_free` passes default lane (no creds).
    - `eval_harness.py` does NOT import `live_creds` and does NOT reference `_dataset_version` (grep -v '^#' | grep -c).
  </acceptance_criteria>
  <done>Creds-free experiment runner, deterministic frozen-item builder, and the importable held-out pool identity exist and are tested in the default lane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: no-regression gate + golden baseline + evaluate_proposal body swap</name>
  <files>src/agent_mesh/services/eval_harness.py, src/agent_mesh/services/self_improvement.py, tests/test_eval_harness.py, tests/fixtures/holdout_baseline.json</files>
  <read_first>
    - src/agent_mesh/services/self_improvement.py (evaluate_proposal :149-194 — what to KEEP vs swap)
    - tests/test_self_improvement.py (existing 11 tests, esp. test_pending_evaluation_does_not_pass — must stay green)
    - .planning/phases/06-self-improvement/06-PATTERNS.md (Pattern 2 no-regression gate; baseline-storage note)
  </read_first>
  <behavior>
    - passes_no_regression: candidate aggregate < baseline aggregate -> (False, [{"reason":"aggregate_below_baseline"}]).
    - passes_no_regression: any item where (baseline - candidate) > item_threshold -> (False, [regression dicts]).
    - passes_no_regression: candidate >= baseline aggregate and all item deltas within threshold -> (True, []).
    - evaluate_proposal with can_run=False still returns pending=True and PENDING_EVALUATION (contract preserved).
    - evaluate_proposal default lane runs the harness over held-out items + gate, maps result onto EvaluationResult.passed.
  </behavior>
  <action>
    Add `passes_no_regression(candidate, baseline, *, item_threshold: float) -> tuple[bool, list[dict]]`
    to `eval_harness.py` (pure stdlib — aggregate mean check THEN item-level drill-down; aggregate
    alone hides per-item regressions per D-03). Keep this OUT of run_experiment (must be reproducible
    + creds-free). Add `tests/fixtures/holdout_baseline.json`: a committed golden baseline (per-item
    scores keyed by version) the gate compares against — chosen over durable storage to keep 06-02 off
    repository.py/0004 (RESEARCH Open-Q2). Provide a creds-free loader for it. Then swap the scoring
    engine inside `self_improvement.evaluate_proposal` (:178-194 block ONLY): run the candidate over
    `held_out_pool()` via `run_candidate`, load the golden baseline, call `passes_no_regression`, and
    map (passed, regressions) onto the SAME `EvaluationResult` (set `checks` to the regression detail,
    `evaluator` to "holdout-harness", `passed` accordingly) + `_set_status` flow. Do NOT change the
    signature, the `can_run`/`pending` branch, or `repo.upsert_evaluation`. Preserve a `deterministic_checks=` override path: when explicit checks are passed (as the existing 11 test_self_improvement.py tests do) run them as today; the held-out harness path runs ONLY when no deterministic_checks override is supplied, so legacy tests stay green while the default real-evaluation path uses the harness. Add
    `tests/test_eval_harness.py::test_no_regression_gate` covering the three branches above.
  </action>
  <verify>
    <automated>python -m pytest -q -m "not live" tests/test_eval_harness.py tests/test_self_improvement.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_no_regression_gate` covers aggregate-fail, item-regression-fail, and pass branches (SI-01b).
    - `tests/fixtures/holdout_baseline.json` exists and is loaded creds-free by the gate.
    - All 11 existing `tests/test_self_improvement.py` tests still pass (esp. test_pending_evaluation_does_not_pass — can_run/pending contract intact).
    - `evaluate_proposal` no longer contains the old `non_empty_patch` stub default as its scoring engine; `evaluator` on a real run reads "holdout-harness".
    - Full `make test` green + creds-free.
  </acceptance_criteria>
  <done>The no-regression gate (aggregate + item-level vs golden baseline) is the promotion-eligibility check, wired into a swapped evaluate_proposal body; existing pipeline tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| held-out pool → proposer | The held-out set must never become the proposer's optimization signal (anti-reward-hack) |
| candidate scoring → promotion gate | A candidate that games aggregate metrics must still be caught at item level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03b | Tampering | held_out_pool / held_out_item_ids | mitigate | Held-out pool is a distinct importable surface; proposer (06-03) mines only durable traces, never this pool; zero-overlap test enforced in 06-03 (SI-01a) |
| T-06-04 | Tampering | passes_no_regression (aggregate masking item regressions) | mitigate | Gate requires BOTH aggregate >= baseline AND no item delta > threshold; item-level drill-down mandatory (D-03, SI-01b) |
| T-06-05 | Information Disclosure | eval_harness creds usage | mitigate | Harness imports no live_creds; run_experiment runs creds-free over local data; live-model scoring is 06-04 only (D-14) |
| T-06-06 | Tampering | golden baseline tampering | accept | Baseline is a committed, version-keyed JSON fixture under code review + git history; acceptable for POC (production would sign it) |
</threat_model>

<verification>
- `make test` green + creds-free; `eval_harness.py` imports with no keys.
- evaluate_proposal scores via run_experiment over held-out frozen items, gated by passes_no_regression.
- Existing test_self_improvement.py (11 tests) still green.
</verification>

<success_criteria>
- SI-01: real harness replaces the stub.
- SI-01a: held-out pool is a distinct importable surface (zero-overlap test in 06-03).
- SI-01b: aggregate + item-level no-regression gate is the eligibility check.
- SI-01c: frozen items are deterministic (proven by test).
- Default suite stays creds-free (SI criterion 6, partial — judge in 06-04).
</success_criteria>

<output>
Create `.planning/phases/06-self-improvement/06-02-SUMMARY.md` when done.
Record the held-out pool accessor name + item_id scheme for 06-03's zero-overlap test.
</output>
