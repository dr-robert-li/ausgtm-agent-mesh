---
phase: 06-self-improvement
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/agent_mesh/services/ai_bom.py
  - tests/test_ai_bom.py
autonomous: true
requirements: [SI-02, SI-02a]
must_haves:
  truths:
    - "A CycloneDX ML-BOM (SchemaVersion V1_7) is generated from the deployment + tool-pack manifests, with a machine-learning-model component"
    - "Prompts / dataset version / eval results / model routes are carried as CycloneDX properties (ModelCard gap worked around)"
    - "The generated snapshot persists as an AIBOMSnapshot via repo.upsert_ai_bom(...) and yields its snapshot_id to fill PromotionRecord.ai_bom_snapshot_id"
    - "The generator runs creds-free in the default lane (pure manifest->JSON transform)"
  artifacts:
    - path: "src/agent_mesh/services/ai_bom.py"
      provides: "Manifest-reading CycloneDX V1_7 ML-BOM generator -> AIBOMSnapshot"
      min_lines: 50
    - path: "tests/test_ai_bom.py"
      provides: "Default-lane test: valid CycloneDX 1.7 JSON, ML component, manifest-field mapping"
  key_links:
    - from: "src/agent_mesh/services/ai_bom.py"
      to: "manifests/deployment.manifest.yaml + manifests/tool_pack_manifest.yaml"
      via: "yaml.safe_load manifest read (mirrors gateway.load_tool_pack)"
      pattern: "safe_load"
    - from: "src/agent_mesh/services/ai_bom.py"
      to: "AIBOMSnapshot"
      via: "generated CycloneDX JSON persisted as the snapshot, id linked to PromotionRecord"
      pattern: "AIBOMSnapshot"
---

<objective>
Generate a CycloneDX ML-BOM (ECMA-424 v1.7) snapshot on promotion from the deployment +
tool-pack manifests (SI-02 / SI-02a), filling the existing `PromotionRecord.ai_bom_snapshot_id`
link. Represent the promoted model/prompt bundle as a `machine-learning-model` component and
carry prompts / dataset-version / eval-results / model-routes as CycloneDX `properties`
(the library has NO `ModelCard` model — issue #912; this is the D-11 "keep conformance
pragmatic" posture). Pure manifest->JSON transform — creds-free, default lane.

This plan depends only on 06-01 (the vetted `[aibom]` dependency). It does NOT touch
`self_improvement.py`; the caller wiring that passes the generated id into `promote_proposal`
is done in 06-06 (which owns the `self_improvement.py` edits).

Purpose: SI-02 (AI-BOM snapshot on promotion) + SI-02a (CycloneDX ML-BOM from manifests).
Output: `ai_bom.py` generator + default-lane validity/mapping tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-self-improvement/06-CONTEXT.md
@.planning/phases/06-self-improvement/06-RESEARCH.md
@.planning/phases/06-self-improvement/06-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Use these directly. -->

Manifest-read analog — tools/gateway.py:load_tool_pack (:56-74):
  data = yaml.safe_load(Path(path).read_text()); for raw in data.get("tools", []): ...
Manifests exist at manifests/deployment.manifest.yaml and manifests/tool_pack_manifest.yaml.

AIBOMSnapshot (contracts/models.py :213-226) — the output target:
  snapshot_id (default factory), tenant_id, client_slug, version, agents: list[dict],
  tools: list[dict], skills: list[dict], prompts: list[dict], model_routes: dict, created_at.

PromotionRecord.ai_bom_snapshot_id (contracts/models.py :350) — the link field to fill.

repository (added in 06-01) — the persistence calls to USE (do NOT add repo methods here; 06-01 owns repository.py):
  upsert_ai_bom(snapshot: AIBOMSnapshot) -> AIBOMSnapshot   (persist the snapshot)
  get_ai_bom(snapshot_id: str, tenant_id: str) -> AIBOMSnapshot | None   (tenant-scoped read-back for the test)
  Exact signatures confirmed in the 06-01 SUMMARY.

cyclonedx-python-lib (installed + import paths CONFIRMED in 06-01 SUMMARY — read it):
  from cyclonedx.model.bom import Bom
  from cyclonedx.model.component import Component, ComponentType  # ComponentType.MACHINE_LEARNING_MODEL
  from cyclonedx.model import Property
  from cyclonedx.schema import SchemaVersion, OutputFormat       # SchemaVersion.V1_7
  from cyclonedx.output import make_outputter
  json_str = make_outputter(bom, OutputFormat.JSON, SchemaVersion.V1_7).output_as_string()
  NOTE: there is NO ModelCard model — carry ML metadata as Property entries.

observability.py — the lazy-optional-dep import-and-degrade guard pattern to mirror.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CycloneDX V1_7 ML-BOM generator from manifests -> AIBOMSnapshot</name>
  <files>src/agent_mesh/services/ai_bom.py, tests/test_ai_bom.py</files>
  <read_first>
    - .planning/phases/06-self-improvement/06-01-SUMMARY.md (CONFIRMED cyclonedx output-API import paths — resolves RESEARCH A2)
    - src/agent_mesh/tools/gateway.py (load_tool_pack :56-74 — the yaml manifest-read pattern)
    - src/agent_mesh/contracts/models.py (AIBOMSnapshot :213-226; PromotionRecord.ai_bom_snapshot_id :350)
    - manifests/deployment.manifest.yaml, manifests/tool_pack_manifest.yaml
    - .planning/phases/06-self-improvement/06-PATTERNS.md (ai_bom.py section; Pattern 6 + ModelCard workaround)
    - src/agent_mesh/observability.py (lazy-optional-dep guard pattern)
  </read_first>
  <behavior>
    - generate_ml_bom reads both manifests and emits a CycloneDX V1_7 JSON document.
    - The BOM contains exactly one machine-learning-model component for the promoted bundle, plus tool components from the tool-pack manifest.
    - Prompts, dataset_version, eval_result_id, and model_route_profile appear as CycloneDX Property entries (namespaced, e.g. agentmesh:dataset_version).
    - The output parses as valid JSON and declares specVersion 1.7.
    - generate_ml_bom persists an AIBOMSnapshot via repo.upsert_ai_bom(...) (carrying the JSON + version + tenant/client) and returns its snapshot_id.
  </behavior>
  <action>
    Create `src/agent_mesh/services/ai_bom.py`. Lazily import `cyclonedx` (mirror `observability.py`'s
    feature-gated guard so a default-lane import never hard-crashes if the extra is absent — but in
    the make-test env it IS installed per 06-01). Use the EXACT import paths confirmed in the
    06-01 SUMMARY (do not guess — A2). Provide `generate_ml_bom(repo, *, promoted_version, previous_version,
    dataset_version, evaluation_id, tenant_id, client_slug, deployment_manifest_path, tool_pack_manifest_path,
    route_profile) -> str` (returns the new AIBOMSnapshot.snapshot_id): read both manifests via
    `yaml.safe_load` (mirror `gateway.load_tool_pack`), build a `Bom`, add one
    `Component(type=ComponentType.MACHINE_LEARNING_MODEL, version=promoted_version)` carrying
    prompts/dataset_version/eval_result_id/model_route_profile as `Property` entries (NO ModelCard —
    #912), add tool components from the tool-pack manifest, serialize via
    `make_outputter(bom, OutputFormat.JSON, SchemaVersion.V1_7).output_as_string()`, build an
    `AIBOMSnapshot` populated from the manifests into its structured fields (agents/tools/skills/
    prompts lists + model_routes dict + version + tenant_id + client_slug — the same fields the
    CycloneDX components/properties are built from; AIBOMSnapshot has no free-form JSON column, so
    persist the structured fields, not the serialized string), persist it via
    `repo.upsert_ai_bom(snapshot)` (the method added in 06-01 — do NOT add repo methods here), and
    return `snapshot.snapshot_id`. The CycloneDX V1_7 JSON string is the function's validated output
    (asserted by the test); the persisted AIBOMSnapshot is the durable audit record.
    Pure transform — do NOT import live_creds; no network.
    Create `tests/test_ai_bom.py` asserting the behaviors above (default lane).
  </action>
  <verify>
    <automated>python -m pytest -q -m "not live" tests/test_ai_bom.py</automated>
  </verify>
  <acceptance_criteria>
    - `generate_ml_bom` returns valid JSON with `"specVersion": "1.7"` and a `machine-learning-model` component (test parses + asserts).
    - Prompts / dataset_version / eval_result_id / model_route_profile present as Property entries mapped from the manifests.
    - An `AIBOMSnapshot` is persisted via `repo.upsert_ai_bom(...)` and its `snapshot_id` returned; the test reads it back with `repo.get_ai_bom(snapshot_id, tenant_id)` to prove persistence (suitable for `PromotionRecord.ai_bom_snapshot_id`).
    - `ai_bom.py` does NOT import `live_creds` and makes no network call (grep -v '^#' | grep -c live_creds == 0).
    - Full `make test` green + creds-free.
  </acceptance_criteria>
  <done>A creds-free CycloneDX V1_7 ML-BOM generator reads the manifests, emits a valid 1.7 document with an ML component + property-carried metadata, persists an AIBOMSnapshot, and returns its id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest files → BOM | Manifest contents become the audit record of the approved capability bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Repudiation | promoting an unaudited capability | mitigate | ML-BOM bound to the promoted version, persisted as AIBOMSnapshot, linked from PromotionRecord; 12-month append-only retention (SI-02a) |
| T-06-15 | Tampering | malformed/invalid BOM passing as audit | mitigate | cyclonedx-python-lib guarantees valid V1_7 structure; test parses JSON + asserts specVersion 1.7 + ML component |
| T-06-SC2 | Tampering | cyclonedx import (supply chain) | mitigate | Dep already vetted + installed behind the 06-01 blocking human checkpoint (T-06-SC); confirmed import paths recorded in 06-01 SUMMARY |
</threat_model>

<verification>
- `make test` green + creds-free; `ai_bom.py` imports with the installed extra.
- Generated BOM is valid CycloneDX 1.7 JSON with an ML component + manifest-mapped properties.
- AIBOMSnapshot persisted; id returned for the PromotionRecord link.
</verification>

<success_criteria>
- SI-02: promotion can generate an AI-BOM snapshot (generator exists, returns an id to link).
- SI-02a: snapshot is a CycloneDX ML-BOM (V1_7) from the deployment + tool-pack manifests.
- Default lane creds-free.
</success_criteria>

<output>
Create `.planning/phases/06-self-improvement/06-05-SUMMARY.md` when done.
Record the generate_ml_bom signature for 06-06 to call from promote_proposal wiring.
</output>
