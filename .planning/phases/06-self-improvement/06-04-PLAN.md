---
phase: 06-self-improvement
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/agent_mesh/eval/__init__.py
  - src/agent_mesh/eval/judges.py
  - tests/test_judges_live.py
autonomous: true
requirements: [SI-01d]
must_haves:
  truths:
    - "The LLM-judge runs ONLY in the live opt-in lane behind the live_creds fixture; the default suite imports none of it"
    - "The judge applies position-bias control (order-swap) when comparing candidate vs baseline"
    - "The judge is calibrated against a human-labelled set (TPR/FPR) under a finite-sample Type-I gate, and is never the sole arbiter for close-margin proposals"
  artifacts:
    - path: "src/agent_mesh/eval/judges.py"
      provides: "Live-lane LLM-judge evaluator: order-swap, TPR/FPR calibration, finite-sample Type-I gate, close-margin guard"
      min_lines: 50
    - path: "tests/test_judges_live.py"
      provides: "live-marked tests for order-swap, calibration, Type-I gate"
  key_links:
    - from: "tests/test_judges_live.py"
      to: "src/agent_mesh/eval/judges.py"
      via: "live-marked tests consume the judge behind live_creds"
      pattern: "pytest.mark.live"
---

<objective>
Add the opt-in `live`-lane LLM-judge dimension (SI-01d) as a plug-in `run_evaluators` for
the 06-02 harness: position-bias control via order-swap, calibration against a small
human-labelled set estimating judge TPR/FPR under a statistically valid finite-sample
Type-I gate, and a guard that the judge is NEVER the sole arbiter for close-margin
proposals. ALL of it lives behind the `live_creds` fixture + `live` marker — the default
creds-free suite imports none of it (D-14, SI criterion 6).

Purpose: SI-01d. This is the only Phase-6 surface that touches real models / creds.
Output: `eval/judges.py` + `tests/test_judges_live.py` (live-marked).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-self-improvement/06-CONTEXT.md
@.planning/phases/06-self-improvement/06-RESEARCH.md
@.planning/phases/06-self-improvement/06-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Use these directly. -->

live-lane double gate:
- pyproject.toml :91-92 — the `live` pytest marker.
- tests/conftest.py :114-126 — live_creds fixture (skips unless ANTHROPIC_API_KEY / VERTEX_PROJECT_ID
  / CF_AIG_WRAPPER_URL is set).
- Makefile: `make test` -> pytest -m "not live" (default, creds-free); `make test-live` -> pytest -m live.
- Live-test module shape: tests/test_langfuse_seed_live.py uses module-level
  `pytestmark = pytest.mark.live`.

langfuse Evaluation (live run_evaluators target):
- from langfuse.experiment import Evaluation
- An evaluator fn signature: fn(*, input, output, expected_output, metadata, **_) -> Evaluation.
- The judge plugs into run_candidate(..., run_evaluators=[...]) (the run-level evaluator slot)
  in eval_harness from 06-02 — only when creds exist.

observability.py — the lazy-optional-dep import-and-degrade guard pattern to mirror so the
module never crashes a default-lane import probe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: eval/judges.py — order-swap + calibration + Type-I gate (live-lane)</name>
  <files>src/agent_mesh/eval/__init__.py, src/agent_mesh/eval/judges.py</files>
  <read_first>
    - tests/test_langfuse_seed_live.py (the module-level pytestmark = pytest.mark.live shape)
    - tests/conftest.py (live_creds :114-126)
    - src/agent_mesh/observability.py (lazy-optional-dep guard pattern)
    - .planning/phases/06-self-improvement/06-PATTERNS.md (eval/judges.py section; No Analog Found — judge logic is novel)
    - .planning/phases/06-self-improvement/06-RESEARCH.md (D-05/D-06; position bias worst at close margins; finite-sample Type-I)
  </read_first>
  <action>
    Create `src/agent_mesh/eval/__init__.py` (package marker) and `src/agent_mesh/eval/judges.py`.
    Implement a judge evaluator that: (1) order-swap — scores each candidate-vs-baseline pair in
    BOTH orderings and only counts a win when both orderings agree (position-bias control, D-05);
    (2) `calibrate(judge_labels, human_labels) -> dict` computing TPR/FPR against a small
    human-labelled calibration set (the set must match the promotion task distribution, D-06);
    (3) a finite-sample Type-I gate `passes_type_i(observed, *, alpha, n) -> bool` (a statistically
    valid below-threshold / no-regression test, alpha + sample size CONFIG-DRIVEN, not hardcoded);
    (4) a `close_margin` guard so the judge is NEVER the sole arbiter when candidate and baseline are
    within a configured margin — at close margins the deterministic gate decides. Mirror
    `observability.py`'s lazy-optional-dep import guard so importing the module never crashes a
    default-lane probe, but the judge logic is only EXERCISED behind live_creds. The real reflection/
    judge LLM call goes through the existing model gateway (agents never call providers directly — GW-02).
  </action>
  <verify>
    <automated>python -c "import agent_mesh.eval.judges; print('import-ok')" && grep -c "alpha" src/agent_mesh/eval/judges.py</automated>
  </verify>
  <acceptance_criteria>
    - `eval/judges.py` imports cleanly with NO creds set (lazy-optional-dep guard) — `python -c "import agent_mesh.eval.judges"` exits 0.
    - Module exposes order-swap scoring, `calibrate` (TPR/FPR), `passes_type_i` (finite-sample), and a close-margin guard.
    - alpha / sample-size / close-margin are config-driven (no bare numeric literals as the thresholds; grep -v '^#').
    - The judge calls models only through the gateway path, never a provider SDK directly.
  </acceptance_criteria>
  <done>Live-lane judge with order-swap, calibration, finite-sample Type-I gate, and close-margin non-sole-arbiter guard exists and imports cleanly creds-free.</done>
</task>

<task type="auto">
  <name>Task 2: live-marked judge tests (order-swap, calibration, Type-I)</name>
  <files>tests/test_judges_live.py</files>
  <read_first>
    - tests/test_langfuse_seed_live.py (pytestmark = pytest.mark.live; live_creds consumption)
    - src/agent_mesh/eval/judges.py (the API from Task 1)
    - .planning/phases/06-self-improvement/06-RESEARCH.md (Test Map: SI-01d -> tests/test_judges_live.py -m live)
  </read_first>
  <action>
    Create `tests/test_judges_live.py` with module-level `pytestmark = pytest.mark.live` (exactly like
    `tests/test_langfuse_seed_live.py`) and consuming the `live_creds` fixture. Add tests proving:
    order-swap symmetry (a win counts only when both orderings agree), calibration returns TPR/FPR
    bounded in [0,1] against a labelled fixture, the finite-sample Type-I gate rejects below the
    configured alpha, and the close-margin guard defers to the deterministic gate (judge not sole
    arbiter). These tests SKIP cleanly in the default `make test` lane (no creds) and only run under
    `make test-live` with creds.
  </action>
  <verify>
    <automated>python -m pytest -q -m "not live" tests/test_judges_live.py && python -m pytest -q --collect-only -m live tests/test_judges_live.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_judges_live.py` carries `pytestmark = pytest.mark.live` and consumes `live_creds`.
    - Under `make test` (-m "not live") the file collects ZERO running tests (all live-marked) — default lane stays creds-free (SI-01d / criterion 6).
    - `pytest --collect-only -m live tests/test_judges_live.py` lists the order-swap, calibration, and Type-I tests.
    - Full `make test` stays green and imports no judge code at runtime.
  </acceptance_criteria>
  <done>Live-marked judge tests exist, skip cleanly in the default lane, and exercise order-swap, calibration, and the Type-I gate under live creds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM judge → promotion gate | A biased/gamed judge must not be able to wave a regression through |
| live creds → default suite | Real credentials must never be required by the default creds-free lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | LLM-judge position bias / gaming | mitigate | Order-swap control (win only if both orderings agree); calibration TPR/FPR; never sole arbiter at close margins (D-05/D-06) |
| T-06-12 | Information Disclosure | live creds leaking into default suite | mitigate | All judge code behind live marker + live_creds; default-lane collect-only shows zero running tests; lazy-import guard (D-14) |
| T-06-13 | Spoofing | judge bypassing the gateway to call a provider | mitigate | Judge model calls route through the model gateway only (GW-02 structural chokepoint) |
</threat_model>

<verification>
- `make test` (-m "not live") collects zero running judge tests; imports no judge code.
- `eval/judges.py` imports cleanly with no creds.
- `make test-live` exercises order-swap, calibration, Type-I (when creds present).
</verification>

<success_criteria>
- SI-01d: opt-in live judge with order-swap, calibration (TPR/FPR), finite-sample Type-I gate, close-margin non-sole-arbiter guard.
- Default suite stays creds-free.
</success_criteria>

<output>
Create `.planning/phases/06-self-improvement/06-04-SUMMARY.md` when done.
</output>
