---
phase: 06-self-improvement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0004_active_version.sql
  - src/agent_mesh/services/repository.py
  - tests/conftest.py
  - tests/test_active_version_repo.py
  - pyproject.toml
autonomous: false
requirements: [SI-02b]
must_haves:
  truths:
    - "A new migration 0004_active_version.sql exists, is idempotent, tenant-partitioned, registered in tests/conftest.py _MIGRATIONS, and creates BOTH the self_improvement_active_version pointer table AND the ai_bom_snapshots table"
    - "repository.py exposes current_active_version(tenant_id) and an active-version write across Protocol, InMemoryRepository, and RepositorySQL"
    - "repository.py exposes upsert_ai_bom(snapshot) and a tenant-scoped get_ai_bom(snapshot_id, tenant_id) across Protocol, InMemoryRepository, and RepositorySQL (consumed by 06-05 to persist/read back the ML-BOM)"
    - "cyclonedx-python-lib is installable as the [aibom] extra, vetted via a blocking human-verify checkpoint before any code imports it"
    - "conftest.py provides a stub_reflector fixture and frozen-item fixtures consumed by downstream default-lane tests"
  artifacts:
    - path: "migrations/0004_active_version.sql"
      provides: "Durable active-version pointer (non-hot promotion wiring source for SI-02b) + ai_bom_snapshots table (SI-02 persistence target for 06-05)"
      contains: "IF NOT EXISTS"
    - path: "src/agent_mesh/services/repository.py"
      provides: "current_active_version read + active-version pointer write AND upsert_ai_bom + tenant-scoped get_ai_bom across all three repo layers"
    - path: "pyproject.toml"
      provides: "[aibom] optional extra declaring cyclonedx-python-lib"
      contains: "aibom"
  key_links:
    - from: "tests/conftest.py"
      to: "migrations/0004_active_version.sql"
      via: "_MIGRATIONS tuple registration"
      pattern: "0004_active_version.sql"
    - from: "src/agent_mesh/services/repository.py"
      to: "self_improvement_active_version"
      via: "current_active_version read + active-pointer upsert"
      pattern: "current_active_version"
    - from: "src/agent_mesh/services/repository.py"
      to: "ai_bom_snapshots"
      via: "upsert_ai_bom INSERT + get_ai_bom tenant-scoped SELECT"
      pattern: "upsert_ai_bom"
---

<objective>
Land all Phase-6 shared-file foundations in one wave-1 plan so the downstream feature
plans (06-02..06-06) own zero overlapping files and can run in parallel where their
logic is independent. This mirrors the proven 04-01 / 05-01 "all shared-file edits"
foundation pattern.

This plan owns: the new `0004_active_version.sql` migration (the durable active-version
pointer that SI-02b's non-hot loader + rollback re-point read, AND the `ai_bom_snapshots`
table that 06-05 persists the ML-BOM into), the matching `repository.py`
`current_active_version` read + active-pointer write AND `upsert_ai_bom` + `get_ai_bom`
across all three repo layers, the shared `conftest.py` fixtures (stub reflector + frozen
held-out items) downstream default-lane tests consume, the `[aibom]` optional extra in
`pyproject.toml`, and the BLOCKING human-verify checkpoint that vets + installs
`cyclonedx-python-lib` before any code imports it.

Purpose: dissolve the conftest / repository / migration / dependency collision surface
so waves 2 and 3 stay parallel-safe and the supply-chain gate is isolated.
Output: migration 0004 (two tables), repo active-version + ai-bom methods, conftest
fixtures, pyproject extra, vetted cyclonedx install.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-self-improvement/06-CONTEXT.md
@.planning/phases/06-self-improvement/06-RESEARCH.md
@.planning/phases/06-self-improvement/06-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Executor uses these directly — no exploration needed. -->

repository.py — the tenant-scoped read shape to MIRROR for current_active_version + get_ai_bom
(every method appears in Protocol :31-64, InMemoryRepository, AND RepositorySQL):
- list_evaluations(self, proposal_id: str, tenant_id: str) -> list[EvaluationResult]   (Protocol :51; InMemory :177-185)
- get_promotion(self, promotion_id: str) -> PromotionRecord | None                       (Protocol :55; InMemory :192-194)
- upsert_promotion(self, promotion: PromotionRecord) -> PromotionRecord                  (Protocol :54; InMemory :187-190)
InMemoryRepository __init__ holds dicts keyed by id (e.g. self._promotions :78). Add a
self._active_version: dict[str, tuple[str, str | None]] (tenant_id -> (version, promotion_id))
AND a self._ai_bom_snapshots: dict[str, AIBOMSnapshot] (snapshot_id -> snapshot), both _lock-guarded.

RepositorySQL JSON serialization to MIRROR for ai_bom rich fields — upsert_promotion (:924-953)
shows the INSERT...ON CONFLICT shape; rich dict/list fields are stored with Jsonb(...model_dump(mode="json"))
(see the requester serialization at :625 — `Jsonb(task.requester.model_dump(mode="json"))`). For
ai_bom_snapshots: store agents/tools/skills/prompts as Jsonb list columns and model_routes as a Jsonb
dict column; do NOT design a normalized schema.

AIBOMSnapshot (contracts/models.py :213-226) — the persistence target shape:
  snapshot_id (default factory), tenant_id, client_slug, version, agents: list[dict],
  tools: list[dict], skills: list[dict], prompts: list[dict], model_routes: dict, created_at.

migrations/0002_self_improvement.sql — the CREATE-TABLE convention (idempotent, tenant-
partitioned, 12-month-audit header). migrations/0003_tool_call_fields.sql — the ADD-COLUMN
convention. The new file MUST be 0004 (0003 already exists, Phase-4).

tests/conftest.py — _MIGRATIONS tuple at line 8 (currently ends "0003_tool_call_fields.sql");
_RecordingRouter (:129-170) is the recording-fake-double pattern to mirror for stub_reflector;
live_creds (:114-126) is the live-lane skip gate (do NOT touch).

pyproject.toml — optional-dependencies tools/aggregators extras (lines 53-65) are the
opt-in-extra shape to mirror for aibom.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Vet + install cyclonedx-python-lib (supply-chain gate T-06-SC)</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/06-self-improvement/06-RESEARCH.md (Package Legitimacy Audit — cyclonedx is [ASSUMED], gate first install)
    - pyproject.toml (optional-dependencies block, the tools/aggregators extras analog at lines 53-65)
  </read_first>
  <action>
    Add `aibom = ["cyclonedx-python-lib>=8,<12"]` to `pyproject.toml`
    [project.optional-dependencies], mirroring the opt-in `tools`/`aggregators` extras (lines 53-65).
    Then PAUSE for human verification — this is a blocking supply-chain checkpoint. Do NOT let any
    code import cyclonedx before approval. After human approval, install the extra into the make-test
    env (`python -m pip install -e ".[aibom]"`) and run the import + V1_7 output-API probes below;
    record the confirmed import paths in the SUMMARY (resolves RESEARCH A2 for 06-05).
  </action>
  <what-built>
    Adds an `aibom = ["cyclonedx-python-lib>=8,<12"]` optional extra to `pyproject.toml`
    [project.optional-dependencies], mirroring the opt-in `tools`/`aggregators` extras.
    Does NOT install or import it until the human-gated step is approved.
  </what-built>
  <how-to-verify>
    1. Confirm the package is the OWASP CycloneDX reference library, NOT the CLI tool:
       open https://pypi.org/project/cyclonedx-python-lib/ — verify source repo is
       github.com/CycloneDX/cyclonedx-python-lib, 100+ releases, latest ~11.8.0.
       (The `cyclonedx-bom` package is a DIFFERENT package — the CLI — do not install it.)
    2. On approval, the executor runs: `python -m pip install -e ".[aibom]"` into the env
       that runs `make test`.
    3. Confirm import works creds-free: `python -c "import cyclonedx.model.bom; print('ok')"`.
    4. Confirm the V1_7 output-API symbols exist in the installed version (resolves RESEARCH A2):
       `python -c "from cyclonedx.output import make_outputter; from cyclonedx.schema import SchemaVersion, OutputFormat; print(SchemaVersion.V1_7)"`.
  </how-to-verify>
  <verify>
    <automated>python -c "import cyclonedx.model.bom; from cyclonedx.output import make_outputter; from cyclonedx.schema import SchemaVersion, OutputFormat; print(SchemaVersion.V1_7)"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains an `aibom` extra listing `cyclonedx-python-lib>=8,<12`.
    - `python -c "import cyclonedx.model.bom"` exits 0 in the make-test env.
    - The V1_7 / make_outputter probe in step 4 exits 0 (record the exact import paths in the SUMMARY for 06-05 to consume).
    - This checkpoint is NEVER auto-approved (workflow.auto_advance ignored) — human sign-off recorded for 12-month audit.
  </acceptance_criteria>
  <done>cyclonedx-python-lib declared as the [aibom] extra, human-verified on pypi.org as the OWASP reference lib, installed into the make-test env, and its V1_7 output-API import paths confirmed + recorded for 06-05.</done>
  <resume-signal>Type "approved" after verifying the package on pypi.org and the install + import probes pass, or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: migration 0004 — active-version pointer + ai_bom_snapshots tables + conftest registration</name>
  <files>migrations/0004_active_version.sql, tests/conftest.py</files>
  <read_first>
    - migrations/0002_self_improvement.sql (CREATE TABLE convention: idempotent header, tenant_id, IF NOT EXISTS, JSONB columns for rich fields)
    - migrations/0003_tool_call_fields.sql (ADD COLUMN convention — for reference only)
    - tests/conftest.py (_MIGRATIONS tuple at line 8; _TABLES truncate tuple; _apply_migrations comment-stripping at :29-55)
    - src/agent_mesh/contracts/models.py (AIBOMSnapshot :213-226 — the columns the ai_bom_snapshots table mirrors)
  </read_first>
  <action>
    Create `migrations/0004_active_version.sql` containing TWO tables (keep the filename 0004_active_version.sql
    even though it now holds two tables — the migration ordinal, not the name, is load-bearing).

    Table 1 — `self_improvement_active_version` (the active-version pointer): columns
    `tenant_id TEXT PRIMARY KEY`, `active_version TEXT NOT NULL`, `promotion_id TEXT`,
    `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`.

    Table 2 — `ai_bom_snapshots` (the SI-02 persistence target for 06-05), mirroring the
    `AIBOMSnapshot` model: `snapshot_id TEXT PRIMARY KEY`, `tenant_id TEXT NOT NULL`,
    `client_slug TEXT NOT NULL`, `version TEXT NOT NULL`, `agents JSONB NOT NULL DEFAULT '[]'`,
    `tools JSONB NOT NULL DEFAULT '[]'`, `skills JSONB NOT NULL DEFAULT '[]'`,
    `prompts JSONB NOT NULL DEFAULT '[]'`, `model_routes JSONB NOT NULL DEFAULT '{}'`,
    `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`. Tenant-partitioned per convention
    (tenant_id present; add an index on (tenant_id) for the tenant-scoped get_ai_bom read).

    Both use the 0002 header convention (region australia-southeast1, append-only governance
    note, `CREATE TABLE IF NOT EXISTS`). Plain DDL only — no dollar-quoted bodies, no `--` inside
    string literals (the conftest comment-stripper splits on `--` and `;`). Register
    `"0004_active_version.sql"` as the fourth element of the `_MIGRATIONS` tuple in
    `tests/conftest.py` line 8. Add BOTH table names — `self_improvement_active_version` AND
    `ai_bom_snapshots` — to the `_TABLES` truncate tuple so SQL tests stay isolated.
  </action>
  <verify>
    <automated>grep -c "IF NOT EXISTS" migrations/0004_active_version.sql && grep -c "ai_bom_snapshots" migrations/0004_active_version.sql && grep -c "0004_active_version.sql" tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/0004_active_version.sql` exists, contains `CREATE TABLE IF NOT EXISTS self_improvement_active_version` with `tenant_id TEXT PRIMARY KEY`, AND `CREATE TABLE IF NOT EXISTS ai_bom_snapshots` with `snapshot_id TEXT PRIMARY KEY` + the AIBOMSnapshot JSONB columns.
    - `tests/conftest.py` `_MIGRATIONS` includes `"0004_active_version.sql"` as the 4th element; `_TABLES` includes BOTH `self_improvement_active_version` and `ai_bom_snapshots`.
    - `make test` stays green (in-memory suite does not apply migrations; no regression).
  </acceptance_criteria>
  <done>Migration 0004 created (idempotent, tenant-partitioned) with both the active-version pointer and ai_bom_snapshots tables, registered in the test migration applier + truncate list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: repository active-version + ai-bom read/write across all three layers + conftest fixtures</name>
  <files>src/agent_mesh/services/repository.py, tests/test_active_version_repo.py, tests/conftest.py</files>
  <read_first>
    - src/agent_mesh/services/repository.py (Protocol :31-64; InMemoryRepository list_evaluations :177-185, upsert_promotion :187-190, __init__ dicts :70-80; RepositorySQL upsert_promotion :924-953 INSERT...ON CONFLICT shape; Jsonb model_dump serialization :625)
    - src/agent_mesh/contracts/models.py (AIBOMSnapshot :213-226 — the persisted shape)
    - tests/conftest.py (_RecordingRouter :129-170 — the recording-fake pattern to mirror for stub_reflector)
    - .planning/phases/06-self-improvement/06-PATTERNS.md (repository.py section: "Methods to add (mirror across all three layers)")
  </read_first>
  <behavior>
    - current_active_version returns None for a tenant with no pointer row.
    - After set_active_version(tenant_id, version, promotion_id), current_active_version(tenant_id) returns that version.
    - current_active_version is tenant-scoped: a pointer for tenant A is never returned for tenant B (DUR-02).
    - A second set_active_version for the same tenant overwrites (upsert semantics, mirroring upsert_promotion).
    - upsert_ai_bom(snapshot) persists an AIBOMSnapshot and returns it; get_ai_bom(snapshot_id, tenant_id) reads it back.
    - get_ai_bom is tenant-scoped: a snapshot owned by tenant A is never returned for tenant B (DUR-02), and returns None for an unknown id.
  </behavior>
  <action>
    Add to `repository.py` — BOTH method-pairs, each across the `Repository` Protocol (:31-64), the
    `InMemoryRepository`, AND `RepositorySQL` (never just one layer; mirror the
    upsert_promotion/list_evaluations shapes):

    1. Active-version: a tenant-scoped read `current_active_version(self, tenant_id: str) -> str | None`
       and an active-pointer write `set_active_version(self, tenant_id: str, version: str, promotion_id: str | None = None) -> None`.
       InMemory: back it with a new `self._active_version` dict in `__init__`, `self._lock`-guarded.
       SQL: UPSERT into `self_improvement_active_version` (ON CONFLICT (tenant_id) DO UPDATE) and a
       tenant-scoped SELECT for the read.

    2. AI-BOM persistence (consumed by 06-05): `upsert_ai_bom(self, snapshot: AIBOMSnapshot) -> AIBOMSnapshot`
       and a tenant-scoped read `get_ai_bom(self, snapshot_id: str, tenant_id: str) -> AIBOMSnapshot | None`.
       InMemory: back it with a new `self._ai_bom_snapshots: dict[str, AIBOMSnapshot]` in `__init__`,
       `self._lock`-guarded; get_ai_bom must check the snapshot's tenant_id matches the arg. SQL: INSERT
       into `ai_bom_snapshots` (ON CONFLICT (snapshot_id) DO NOTHING), serializing agents/tools/skills/
       prompts/model_routes with `Jsonb(...model_dump(mode="json"))`-style serialization (mirror the
       requester serialization at :625 and the promotion INSERT at :924-953); get_ai_bom is a
       `WHERE snapshot_id = %s AND tenant_id = %s` SELECT rehydrating an AIBOMSnapshot.

    Create `tests/test_active_version_repo.py` asserting ALL behaviors above against the in-memory repo
    (default lane, creds-free): the four active-version behaviors AND the ai-bom round-trip + tenant
    isolation + unknown-id-None cases. Also add to `tests/conftest.py`: a `stub_reflector` fixture (a
    recording fake mirroring `_RecordingRouter` that records the evidence passed and returns a canned
    non-empty diff string) and a `frozen_holdout_items` fixture returning a small deterministic list
    of LocalExperimentItem-shaped dicts ({"input":..., "expected_output":..., "metadata":{"item_id":...}})
    keyed by stable item_ids — these are consumed by 06-02/06-03 downstream.
  </action>
  <verify>
    <automated>python -m pytest -q -m "not live" tests/test_active_version_repo.py && python -c "from agent_mesh.services.repository import InMemoryRepository as R; assert all(hasattr(R,m) for m in ('current_active_version','set_active_version','upsert_ai_bom','get_ai_bom'))"</automated>
  </verify>
  <acceptance_criteria>
    - `current_active_version`, `set_active_version`, `upsert_ai_bom`, and `get_ai_bom` exist on Protocol, InMemoryRepository, and RepositorySQL (grep all three layers for each).
    - `tests/test_active_version_repo.py` passes default lane, covering active-version (None-default, set+read, tenant isolation, overwrite) AND ai-bom (upsert→get_ai_bom round-trip, tenant isolation, unknown-id→None).
    - `tests/conftest.py` exports `stub_reflector` and `frozen_holdout_items` fixtures.
    - Full `make test` stays green and creds-free.
  </acceptance_criteria>
  <done>Active-version read/write AND upsert_ai_bom/get_ai_bom present in all three repo layers, tenant-scoped, tested; shared downstream fixtures added to conftest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip registry → local env | A new third-party package (cyclonedx-python-lib) is installed into the test/runtime env |
| repo read → caller | Active-version pointer + AI-BOM snapshot cross the tenant boundary on read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | cyclonedx-python-lib install (pip supply chain) | mitigate | Blocking human-verify checkpoint (Task 1); verify on pypi.org it is the OWASP CycloneDX reference lib (not `cyclonedx-bom` CLI); never auto-approved; 12-month audit sign-off |
| T-06-01 | Information Disclosure | repository.current_active_version | mitigate | Read is tenant-scoped (tenant_id arg required); test asserts a tenant-A pointer is never returned for tenant-B (DUR-02) |
| T-06-02 | Elevation of Privilege | set_active_version write | accept | Write is called only from promote/rollback service code (06-06), themselves behind the existing approval chokepoint; no external ingress reaches this method in this phase |
| T-06-03 | Information Disclosure | repository.get_ai_bom | mitigate | Read is tenant-scoped (snapshot_id + tenant_id args; tenant mismatch returns None); test asserts a tenant-A snapshot is never returned for tenant-B (DUR-02) |
</threat_model>

<verification>
- `make test` green + creds-free (in-memory suite, no migration applied by default).
- `migrations/0004_active_version.sql` registered in conftest `_MIGRATIONS` and `_TABLES` (both tables).
- cyclonedx importable in the make-test env after the human-gated install.
- `python -m pytest -q tests/test_active_version_repo.py` passes (active-version + ai-bom round-trip).
</verification>

<success_criteria>
- Migration 0004 (not 0003) created, idempotent, tenant-partitioned, registered — with both the active-version pointer and ai_bom_snapshots tables.
- `current_active_version` + `set_active_version` AND `upsert_ai_bom` + `get_ai_bom` on all three repo layers, tenant-scoped + tested.
- `[aibom]` extra declared; cyclonedx vetted + installed behind a blocking human checkpoint.
- `stub_reflector` + `frozen_holdout_items` fixtures available for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/06-self-improvement/06-01-SUMMARY.md` when done.
Record in the SUMMARY: the exact confirmed cyclonedx output-API import paths (resolves RESEARCH A2 for 06-05),
and the `upsert_ai_bom`/`get_ai_bom` signatures for 06-05 to consume.
</output>
</content>
</invoke>
