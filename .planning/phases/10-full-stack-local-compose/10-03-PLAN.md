---
phase: 10-full-stack-local-compose
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - Makefile
  - RUNBOOK.md
  - .env.example
  - tests/test_compose_config.py
autonomous: true
requirements: [COMPOSE-02, COMPOSE-03]
must_haves:
  truths:
    - "`make compose-up` brings the stack up with one command, passing --profile $(MODEL_PROFILE) --profile langfuse (MODEL_PROFILE ?= vllm; cpu override for non-GPU)"
    - "`make compose-down` tears down all profiles cleanly (enumerated vllm/cpu/langfuse — Pitfall 4)"
    - "compose-up/compose-down are best-effort operator targets, NOT wired into `make test` (P8/P9 convention)"
    - "RUNBOOK documents the one-command full-stack bring-up with the dev-only disclosures (docker-socket host-root, Langfuse UI-minted keys, vLLM-needs-GPU, LiteLLM embedded not a container, # CHANGEME secrets)"
    - "tests/test_compose_config.py statically validates the compose file via `docker compose config` (parses; core services present; litellm absent; pgvector image present; >=4 healthchecks; profiled services appear when named) and loud-skips when docker is absent — the ONLY CI-wired Phase-10 artifact"
    - "A [BLOCKING] phase invariant asserts `git diff <phase-base>..HEAD -- src/` is empty (zero-src-change, mirrors P8/P9)"
  artifacts:
    - path: "Makefile"
      provides: "compose-up / compose-down best-effort operator targets (COMPOSE-02)"
      contains: "compose-up"
    - path: "tests/test_compose_config.py"
      provides: "COMPOSE-03 static-validation test (CI-wired, module-level docker loud-skip)"
      contains: "shutil.which"
    - path: "RUNBOOK.md"
      provides: "Full-stack compose section with all dev-only disclosures (COMPOSE-02)"
      contains: "Full-stack local compose"
  key_links:
    - from: "Makefile compose-up"
      to: "docker compose profiles"
      via: "--profile $(MODEL_PROFILE) --profile langfuse up"
      pattern: "--profile \\$\\(MODEL_PROFILE\\) --profile langfuse"
    - from: "tests/test_compose_config.py"
      to: "docker compose config"
      via: "subprocess module-level skipif(which docker None)"
      pattern: "docker.*compose.*config"
    - from: "tests/test_compose_config.py"
      to: "pgvector image + core services + litellm-absent assertions"
      via: "rendered-config grep + --services"
      pattern: "pgvector/pgvector:pg16"
---

<objective>
Wire the operator and validation surface over the 10-02 compose file: the `make compose-up`/
`compose-down` targets (COMPOSE-02), the RUNBOOK full-stack section with honest dev-only
disclosures (COMPOSE-02), the CI-wired static-validation test (COMPOSE-03), and the
`[BLOCKING]` zero-src-change phase invariant.

Purpose: COMPOSE-02 — one-command bring-up via make + RUNBOOK docs. COMPOSE-03 — the lone
CI-wired piece, a static `docker compose config` assertion that loud-skips when docker is
absent (heavy bring-up stays operator-only by design). The zero-src invariant enforces the
whole-milestone guardrail.

Output: edited `Makefile`, `RUNBOOK.md`, `.env.example` (confirm/comment only),
`tests/test_compose_config.py` (NEW).

HARD GUARDRAIL: config/docker-compose/docs/tests ONLY. ZERO `src/` change. Every `src/`
path referenced below is READ-ONLY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-full-stack-local-compose/10-CONTEXT.md
@.planning/phases/10-full-stack-local-compose/10-RESEARCH.md
@.planning/phases/10-full-stack-local-compose/10-PATTERNS.md
@.planning/phases/10-full-stack-local-compose/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compose-up / compose-down Makefile targets (COMPOSE-02)</name>
  <files>Makefile</files>
  <read_first>
    - Makefile:1 (the `.PHONY` line — append `compose-up compose-down`), :6-25 (the `help:` block — add a help line per target mirroring every existing entry), :72-116 (the P8/P9 best-effort `run-*`/`use-*`/`run-pg` pattern: env-default `VAR ?= default`, operator-only, DELIBERATELY never wired into `test` or CI)
    - .planning/phases/10-full-stack-local-compose/10-RESEARCH.md § "Open discretion items > compose-down scope" (enumerated `--profile vllm --profile cpu --profile langfuse down`, no `-v` by default — Pitfall 4)
    - .planning/phases/10-full-stack-local-compose/10-PATTERNS.md § "Makefile — compose-up / compose-down targets" (the exact target shape; MODEL_PROFILE ?= vllm; D-05)
  </read_first>
  <action>
    Add `MODEL_PROFILE ?= vllm` (values `vllm` | `cpu`; cpu => Ollama — D-05) near the other
    env-default make vars. Add target `compose-up:` running
    `docker compose --profile $(MODEL_PROFILE) --profile langfuse up -d --build` (one command,
    builds the shared image + activates the heavy profiles — L2/D-05). Add `compose-down:`
    running the ENUMERATED teardown `docker compose --profile vllm --profile cpu --profile langfuse down`
    (deterministic across compose versions — Pitfall 4; no `-v` by default to preserve data —
    document the `-v` wipe option in RUNBOOK, not as the default). Add both to the `.PHONY` line
    (Makefile:1) and add a help line for each in the `help:` block (Makefile:6-25) mirroring the
    existing style (e.g. "compose-up   Bring up the full local stack (one command; MODEL_PROFILE=vllm|cpu)").
    Mirror the P8/P9 comment block: these are best-effort, operator-run, require docker, may fail
    on a bare box (acceptable, documented in RUNBOOK), and are DELIBERATELY NOT wired into `make
    test` or any CI path. The ONLY CI-wired Phase-10 piece is `tests/test_compose_config.py` (Task 3).
  </action>
  <acceptance_criteria>
    - `make -n compose-up | grep -q -- '--profile $(MODEL_PROFILE)\|--profile vllm'` AND `make -n compose-up | grep -q -- '--profile langfuse'` AND `make -n compose-up | grep -q 'up'`
    - `make -n compose-down | grep -q -- '--profile vllm --profile cpu --profile langfuse'` AND `make -n compose-down | grep -q 'down'` AND `! make -n compose-down | grep -q -- '-v'` (no volume wipe by default)
    - `grep -q 'MODEL_PROFILE ?= vllm' Makefile` (D-05 default)
    - `head -1 Makefile | grep -q 'compose-up' && head -1 Makefile | grep -q 'compose-down'` (added to .PHONY)
    - `grep -q 'compose-up' Makefile` within the help block (sed -n '6,30p' Makefile shows the help lines)
    - `! grep -E '^test:' -A2 Makefile | grep -q compose` (compose targets NOT folded into `make test`)
    - `git diff --stat -- src/` is EMPTY
  </acceptance_criteria>
  <done>
    `make compose-up`/`compose-down` exist (one-command up with profiles; enumerated teardown),
    in .PHONY + help, best-effort/never-CI. No src/ change.
  </done>
</task>

<task type="auto">
  <name>Task 2: RUNBOOK full-stack compose section + .env.example reconciliation (COMPOSE-02)</name>
  <files>RUNBOOK.md, .env.example</files>
  <read_first>
    - RUNBOOK.md:184-245 (the P8/P9 "Local data & telemetry plane" section style + honest-disclosure tone; the dev-only-creds disclosure at :186-191; the "Self-hosted Langfuse (documentation-only)" block at :219-245 which explicitly DEFERS the multi-container standup "to Phase 10's compose" and notes the server-side `# CHANGEME` secrets are "Phase 10 territory" — THIS plan reconciles that promise)
    - .env.example (existing DATABASE_URL / LANGFUSE_* / MODEL_GATEWAY_BASE_URL / MODEL_PROVIDER_MODE keys — RESEARCH: no new mesh keys expected; compose overrides these inline)
    - .planning/phases/10-full-stack-local-compose/10-PATTERNS.md § "RUNBOOK.md — full-stack compose section" (the mandatory disclosures list: D-02 docker-socket dev-only, D-03a UI-minted keys, D-04/D-05 vLLM-GPU/cpu-override, D-06 LiteLLM embedded, # CHANGEME secrets) + § ".env.example (confirm-no-change)"
    - .planning/phases/10-full-stack-local-compose/10-RESEARCH.md § "Security Domain" (the dev-only disclosure requirements)
  </read_first>
  <action>
    Add a new `## Full-stack local compose` section to RUNBOOK.md AFTER the P9 "Local data &
    telemetry plane" section. Document the one-command bring-up: `make compose-up` (default vLLM;
    needs a GPU) and the `MODEL_PROFILE=cpu make compose-up` override for non-GPU boxes (Ollama),
    plus `make compose-down`. State the operator-only/best-effort nature (requires docker; not CI).
    Carry these MANDATORY honest disclosures (reuse the P9 dev-only tone):
    - **docker-socket mount is DEV-ONLY**: the worker binds `/var/run/docker.sock` so the real P2
      sandbox runs in-stack; this grants the worker effective host-root and is NEVER a production
      posture (production uses Cloud Run Jobs) — D-02.
    - **Langfuse keys are UI-minted**: tracing loud-skips on first bring-up until a human mints
      keys in the Langfuse UI (http://localhost:3000) and sets `LANGFUSE_PUBLIC_KEY`/`SECRET_KEY`
      in an untracked `.env` — D-03a; reconcile with the existing P9 client-vs-server key note.
    - **vLLM is the default and needs a GPU**; non-GPU operators use `MODEL_PROFILE=cpu` (Ollama) — D-04/D-05.
    - **LiteLLM runs EMBEDDED in api/worker, not as a proxy container** — mirrors deploy (no
      litellm image); COMPOSE-03's required-services list omits a litellm container by design — D-06.
    - **Langfuse server `# CHANGEME` secrets** (NEXTAUTH_SECRET/SALT/ENCRYPTION_KEY/clickhouse/
      minio) are local-only dev defaults; replace for any non-local use.
    - Note the `-v` teardown option for a clean volume wipe (data loss) and the `MESH_SBX_DIR`/
      `DOCKER_GID` knobs (dev-only, host-dependent — A1).
    Update the P9 "Self-hosted Langfuse (documentation-only)" block (RUNBOOK.md:219-233) so it
    points at this new section for the actual standup (it currently says "deferred to Phase 10's
    compose").
    `.env.example`: RESEARCH says no new mesh keys — at most add an explanatory comment noting the
    in-stack host overrides (`DATABASE_URL`→`@postgres`, `LANGFUSE_HOST`→`@langfuse-web`) are
    applied by compose `environment:`, and that Langfuse server secrets + `MESH_SBX_DIR`/`DOCKER_GID`
    live compose-inline, not as mesh `.env` keys. Default action is confirm-no-change; add comments
    only if clearer. Do NOT add cloud keys (Phase-11 no-egress assertion depends on a clean .env).
  </action>
  <acceptance_criteria>
    - `grep -q 'Full-stack local compose' RUNBOOK.md` (new section present)
    - `grep -iq 'docker.sock' RUNBOOK.md && grep -iq 'dev-only\|dev only' RUNBOOK.md` (docker-socket dev-only disclosure — D-02)
    - `grep -iq 'UI-minted\|UI-generated\|mint' RUNBOOK.md` (Langfuse keys UI-minted — D-03a)
    - `grep -iq 'MODEL_PROFILE=cpu' RUNBOOK.md` (non-GPU override — D-05)
    - `grep -iq 'embedded' RUNBOOK.md && grep -iq 'no litellm\|not a proxy\|no litellm image' RUNBOOK.md` (LiteLLM embedded, no container — D-06)
    - `grep -q 'make compose-up' RUNBOOK.md && grep -q 'make compose-down' RUNBOOK.md`
    - `! grep -Eq 'ANTHROPIC_API_KEY=.+|VERTEX|sk-' .env.example` (no cloud keys leaked — Phase-11 cleanliness)
    - `git diff --stat -- src/` is EMPTY
  </acceptance_criteria>
  <done>
    RUNBOOK has a Full-stack local compose section with all dev-only disclosures (D-02/D-03a/
    D-04/D-05/D-06 + # CHANGEME); the P9 Langfuse-deferral block points here; .env.example
    reconciled (no cloud keys). No src/ change.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_compose_config.py — COMPOSE-03 static-validation (CI-wired, loud-skip) + [BLOCKING] zero-src invariant</name>
  <files>tests/test_compose_config.py</files>
  <behavior>
    - With docker absent: the WHOLE file skips (module-level skipif), `make test` stays green.
    - With docker present: `docker compose config` parses (exit 0); raises/fails loudly on a malformed file.
    - Core services `{api, worker, gui, postgres, migrate}` are present in `docker compose config --services`; `litellm` is NOT in the set (D-06).
    - `pgvector/pgvector:pg16` string is present in the rendered bare config.
    - `>= 4` `healthcheck:` blocks render (postgres, api, langfuse-web, model backend — across profiles).
    - Profiled `{vllm, langfuse-web}` appear ONLY when `--profile vllm --profile langfuse` is passed; absent from the bare config (L2 profile-gating).
    - (Static-only — does NOT bring the stack up, does NOT validate bind-source existence or api_base correctness; Pitfall 3.)
  </behavior>
  <read_first>
    - tests/conftest.py:75-88 (the `pg_dsn` loud-skip PRINCIPLE — gate cleanly when a dependency is absent)
    - tests/test_local_data_plane.py:1-25 (DSN-gated test FILE shape: collects under plain `make test`, skips cleanly; heavy dep imported inside the test body)
    - .planning/phases/10-full-stack-local-compose/10-RESEARCH.md § "COMPOSE-03 static-validation test" (the empirically-verified recipe: module-level `pytestmark = skipif(which("docker") is None)`, daemon-free `docker compose config`, `--services` for profile-gating, `pgvector/pgvector:pg16` grep; daemon-free even with `build:` services) + § "Open Questions" (assertion shape defaults)
    - .planning/phases/10-full-stack-local-compose/10-PATTERNS.md § "tests/test_compose_config.py" (CRITICAL: skip SHAPE differs from pg_dsn — module-level `pytest.mark.skipif`, NOT a fixture-internal `pytest.skip`; mirrors executor's `shutil.which("docker")` gate)
    - Makefile:38-39 (`test:` runs `pytest -q -m "not live"` with NO path filter → a new `tests/*.py` auto-collects; the test must NOT be `live`-marked and needs NO Makefile test-lane edit)
  </read_first>
  <action>
    Create `tests/test_compose_config.py` mirroring the RESEARCH recipe. Module-level
    `pytestmark = pytest.mark.skipif(shutil.which("docker") is None, reason="docker binary absent;
    compose-config validation requires the docker CLI")` — this is the loud-skip (mirrors the
    executor's own `shutil.which("docker")` gate; do NOT use a fixture-internal `pytest.skip`).
    Do NOT mark it `live` — it must auto-collect under `make test` (`pytest -q -m "not live"`)
    and loud-skip cleanly on a docker-less box.
    A helper runs `docker compose config` (daemon-free even with `build:` services — it only
    resolves/merges/interpolates and prints) with optional `--profile` args, `check=True`,
    `cwd` at the repo root. Author these tests:
    1. bare `docker compose config -q`/`config` parses (exit 0).
    2. `docker compose config --services` ⊇ `{api, worker, gui, postgres, migrate}` AND
       `"litellm" not in services` (D-06; include `migrate` so Pitfall-5's fix is a CI-caught
       presence property even though runtime correctness stays operator-only).
    3. `"pgvector/pgvector:pg16"` present in the bare rendered config.
    4. `rendered.count("healthcheck:") >= 4` (postgres, api, langfuse-web, model backend — pass
       `--profile vllm --profile langfuse --profile cpu` so all healthchecked services render).
    5. profiled `{vllm, langfuse-web}` appear in `--services` ONLY when `--profile vllm
       --profile langfuse` is passed; assert they are ABSENT from the bare `--services` (L2).
    OPTIONAL cheap wins (presence, not correctness — turn gap-fixes into CI-caught properties):
    grep the rendered config (or the raw docker-compose.yml) for the `model_gateway.config.yaml`
    mount target (Finding #1 present) and `/var/run/docker.sock` (Finding #2 present).
    Keep ALL assertions static — do NOT bring the stack up (out of scope; Pitfall 3 boundary).
  </action>
  <acceptance_criteria>
    - `make test` exits 0 on this box (docker may be present or absent; either passes-or-loud-skips). Run: `PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_compose_config.py` (canonical interpreter per Phase-8 note; bare PATH python lacks deps)
    - `grep -q 'pytest.mark.skipif' tests/test_compose_config.py && grep -q 'shutil.which' tests/test_compose_config.py` (module-level docker loud-skip)
    - `! grep -q '@pytest.mark.live\|pytest.mark.live\|"live"' tests/test_compose_config.py` (NOT live-marked → auto-collects under `make test`)
    - `grep -q 'pgvector/pgvector:pg16' tests/test_compose_config.py` (image assertion)
    - `grep -q 'litellm' tests/test_compose_config.py` (D-06 absence assertion present)
    - `grep -q 'migrate' tests/test_compose_config.py` (Pitfall-5 presence assertion)
    - `grep -q 'healthcheck' tests/test_compose_config.py` (>=4 healthcheck assertion)
    - With docker present, `docker compose config -q` exits 0 (the file under test parses)
    - `git diff --stat -- src/` is EMPTY
  </acceptance_criteria>
  <done>
    tests/test_compose_config.py CI-wired (module-level docker loud-skip, not live-marked),
    asserts parse + core-services + litellm-absent + pgvector image + >=4 healthchecks +
    profile-gating + gap-fix presence. Passes-or-loud-skips under `make test`. No src/ change.
  </done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Zero-src-change phase invariant assertion</name>
  <files>tests/test_compose_config.py</files>
  <read_first>
    - .planning/phases/10-full-stack-local-compose/10-PATTERNS.md § "Shared Patterns > Zero-src-change invariant" (assert `git diff <base>..HEAD -- src/` is empty as a phase gate — same invariant P8/P9 enforced)
    - .planning/STATE.md (the v1.1 milestone guardrail: config/compose/docs/tests ONLY, zero src/ change — the deployment-name parity surface)
  </read_first>
  <action>
    [BLOCKING] After Tasks 1-3 land, assert the whole-phase zero-`src/`-change invariant: the
    entire Phase 10 diff against the phase base must touch NO file under `src/`. Compute the phase
    base (the commit before Phase-10 work began, e.g. the Phase-9 completion commit `b53d6b6`, or
    `git merge-base HEAD <phase-base>`), then run `git diff --stat <phase-base>..HEAD -- src/` and
    confirm it is EMPTY. This mirrors the P8/P9 invariant. If ANY `src/` file appears in the diff,
    STOP and report — a src/ edit violates the milestone guardrail and must be reverted before the
    phase can complete (deployment names `low/medium/high-complexity` and the gateway seam must
    stay byte-identical so agent/gateway code is untouched). This is a blocking gate, not a code
    artifact — it does not add to `tests/test_compose_config.py`; it is the phase-completion check.
  </action>
  <acceptance_criteria>
    - `git diff --stat $(git merge-base HEAD b53d6b6)..HEAD -- src/` produces NO output (empty — zero src/ change across the whole phase)
    - Equivalently: `test -z "$(git diff --name-only b53d6b6..HEAD -- src/)"` exits 0
    - The full file set changed by Phase 10 is confined to: Dockerfile, .dockerignore, config/model_gateway.{vllm,cpu}.compose.yaml, docker-compose.yml, Makefile, RUNBOOK.md, .env.example, tests/test_compose_config.py, and .planning/** — `git diff --name-only b53d6b6..HEAD | grep -v '^\.planning/' | grep -qv '^src/'` confirms no src/ path
  </acceptance_criteria>
  <done>
    `git diff <phase-base>..HEAD -- src/` is empty — the zero-src-change milestone guardrail
    holds across all of Phase 10. Blocking gate satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI test → docker CLI | the COMPOSE-03 test shells out to `docker compose config` (daemon-free, client-side only) |
| operator docs → secrets | RUNBOOK discloses dev-only creds/socket; no secret values authored beyond # CHANGEME pointers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Elevation of Privilege | docker-socket mount (disclosed here) | mitigate | RUNBOOK explicitly discloses the `/var/run/docker.sock` mount as DEV-ONLY host-root-equivalent, never a production posture — the disclosure IS the mitigation for the 10-02 surface. |
| T-10-03-02 | Information Disclosure | .env.example cleanliness | mitigate | Assert no cloud keys (ANTHROPIC/VERTEX/sk-) leak into .env.example — keeps the Phase-11 no-egress assertion clean and prevents accidental credential commit. |
| T-10-03-03 | Tampering | unintended src/ edit | mitigate | [BLOCKING] zero-src invariant (Task 4) fails the phase if any `src/` file is touched — enforces the milestone guardrail and deployment-name parity. |

No HIGH-severity threat. Docs/test/config-only; the docker-socket disclosure mitigates the 10-02 surface rather than introducing a new one.
</threat_model>

<verification>
- `make -n compose-up` shows `--profile $(MODEL_PROFILE) --profile langfuse up`; `make -n compose-down` shows the enumerated teardown
- RUNBOOK has the Full-stack local compose section with all five dev-only disclosures
- `make test` passes-or-loud-skips `tests/test_compose_config.py` (canonical: `PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_compose_config.py`)
- `git diff --name-only b53d6b6..HEAD -- src/` is EMPTY ([BLOCKING] invariant)
</verification>

<success_criteria>
- `make compose-up`/`compose-down` exist, best-effort, never-CI; RUNBOOK documents one-command bring-up with all dev-only disclosures (COMPOSE-02).
- `tests/test_compose_config.py` is the CI-wired static-validation test (parses + core services + litellm-absent + pgvector image + >=4 healthchecks + profile-gating), module-level docker loud-skip (COMPOSE-03).
- [BLOCKING] zero-`src/`-change invariant holds across the whole phase.
</success_criteria>

<output>
Create `.planning/phases/10-full-stack-local-compose/10-03-SUMMARY.md` when done.
</output>
