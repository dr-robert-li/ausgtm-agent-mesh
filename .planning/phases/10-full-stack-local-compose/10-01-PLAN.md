---
phase: 10-full-stack-local-compose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - config/model_gateway.vllm.compose.yaml
  - config/model_gateway.cpu.compose.yaml
autonomous: true
requirements: [COMPOSE-01]
must_haves:
  truths:
    - "A single root Dockerfile builds one image installable as `.[runtime,agents,gui]` that any of the three service commands can run"
    - "The shared image carries the docker CLI binary so the worker can shell out to `docker run` (Finding #2 — executor uses subprocess + shutil.which, NO python docker SDK)"
    - "A compose-variant vLLM profile exists whose api_base is the `vllm` service DNS, not localhost (Finding #1)"
    - "A compose-variant CPU/Ollama profile exists named to match the MODEL_PROFILE value `cpu` whose api_base is the `ollama` service DNS (Finding #1 + A5 filename gotcha)"
  artifacts:
    - path: "Dockerfile"
      provides: "Shared D-01 app image (one image, command-parameterized) + docker CLI binary for the worker"
      contains: "FROM"
    - path: ".dockerignore"
      provides: "Lean build context (excludes .venv, .git, .sbxwork, active config swap state)"
      contains: ".venv"
    - path: "config/model_gateway.vllm.compose.yaml"
      provides: "vLLM routing profile with service-DNS api_base for in-stack completions"
      contains: "http://vllm:8000/v1"
    - path: "config/model_gateway.cpu.compose.yaml"
      provides: "Ollama/CPU routing profile with service-DNS api_base; filename follows MODEL_PROFILE value cpu (A5)"
      contains: "http://ollama:11434"
  key_links:
    - from: "Dockerfile"
      to: "pyproject extras [runtime,agents,gui]"
      via: "pip install .[runtime,agents,gui]"
      pattern: "runtime,agents,gui"
    - from: "Dockerfile"
      to: "docker CLI binary (worker DooD, Finding #2)"
      via: "apt-get/apk install docker-cli OR docker.io"
      pattern: "docker"
    - from: "config/model_gateway.vllm.compose.yaml"
      to: "vllm compose service"
      via: "api_base service DNS (NOT localhost)"
      pattern: "vllm:8000/v1"
    - from: "config/model_gateway.cpu.compose.yaml"
      to: "ollama compose service"
      via: "api_base service DNS (NOT localhost)"
      pattern: "ollama:11434"
---

<objective>
Author the shared application image and the two compose-variant model profiles that the
Phase-10 docker-compose stack consumes. This is the "files first" plan: it creates the
build artifact (Dockerfile) and the Finding #1 model-profile variants WITHOUT yet wiring
them into docker-compose.yml (that is plan 10-02).

Purpose: D-01 deploy-fidelity needs ONE shared image (api/worker/gui differ only by
`command:`); the worker additionally needs the docker CLI binary so the real P2 sandbox
runs in-stack (Finding #2). Finding #1: the Phase-8 `model_gateway.{vllm,ollama}.yaml`
bake `localhost`, which inside compose resolves to the container itself — completions
break. NEW `.compose.yaml` variants with service-DNS api_base fix this without editing the
frozen Phase-8 files (which host `make use-vllm`/`run-vllm` and Phase-11's localhost
assertion depend on).

Output: `Dockerfile`, `.dockerignore`, `config/model_gateway.vllm.compose.yaml`,
`config/model_gateway.cpu.compose.yaml`.

HARD GUARDRAIL: config/docker-compose/docs/tests ONLY. ZERO `src/` change. Every `src/`
path referenced below is READ-ONLY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-full-stack-local-compose/10-CONTEXT.md
@.planning/phases/10-full-stack-local-compose/10-RESEARCH.md
@.planning/phases/10-full-stack-local-compose/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the shared root Dockerfile (D-01) carrying the docker CLI binary (Finding #2)</name>
  <files>Dockerfile, .dockerignore</files>
  <read_first>
    - pyproject.toml (lines 1-90 — the `dependencies`, and `runtime`/`agents`/`gui` optional-dependency groups the image must install; jsonschema is a core dep, fastapi/uvicorn are core)
    - src/agent_mesh/sandbox/executor.py:120-145 (the worker shells out via `subprocess.run(_docker_run_argv(...))` + `shutil.which("docker")` at :131 — the image MUST carry the docker CLI binary; NO python docker SDK is imported)
    - scripts/gcp_deploy_core.sh:16-18 (image-name parity reference: `agent-mesh-api`/`-worker`/`-code-executor`; do NOT invent names that break this — compose service names are the parity surface, the Dockerfile is one shared image)
    - .planning/phases/10-full-stack-local-compose/10-RESEARCH.md § "Standard Stack > App image" (install `.[runtime,agents,gui]` so any `command:` boots; worker needs docker CLI binary — A2)
    - .planning/phases/10-full-stack-local-compose/10-PATTERNS.md § "No Analog Found" (Dockerfile is net-new; informed by pyproject extras + deploy image names)
    - .gitignore (starting exclusion list for .dockerignore)
  </read_first>
  <action>
    Author a single root `Dockerfile` building ONE shared image (D-01). Base on an official
    slim Python image satisfying `requires-python = ">=3.11"` (e.g. `python:3.12-slim`).
    Install the project as `pip install .[runtime,agents,gui]` so all three service commands
    (`uvicorn agent_mesh.api.app:app --port 8080`, `python -m agent_mesh.worker.main`,
    `streamlit run src/agent_mesh/gui/admin_app.py`) can boot from the same image — do NOT
    set a single ENTRYPOINT that locks one service; `command:` in compose selects per service.
    Copy the repo (src/, config/, migrations/, pyproject.toml, requirements/) into `/app` and
    set `WORKDIR /app`; ensure `PYTHONPATH` resolves `agent_mesh` (pythonpath=src convention).
    CRITICAL (Finding #2): install the docker CLI client binary (e.g. apt `docker.io` or the
    docker-cli package) so the worker's `shutil.which("docker")` gate passes and `docker run`
    is invocable — the python docker SDK is NOT needed and MUST NOT be added. Do NOT run the
    container as root by default beyond what the docker-socket group access requires; the
    socket gid handling is wired in compose (10-02), not baked here.
    Author `.dockerignore` excluding `.venv`, `.git`, `__pycache__`, test artifacts,
    `.sbxwork/`, and the active `config/model_gateway.config.yaml` swap state, to keep the
    build context lean. Use the `.gitignore` exclusions as the starting list.
    Pin no `:latest` floating tags for the base image — use a concrete minor (e.g. `3.12-slim`).
  </action>
  <acceptance_criteria>
    - `test -f Dockerfile && test -f .dockerignore` (both exist)
    - `grep -q 'runtime,agents,gui' Dockerfile` (installs the three extra groups in one image — D-01)
    - `grep -Eiq 'docker\.io|docker-ce-cli|docker-cli' Dockerfile` (docker CLI binary present — Finding #2 worker DooD)
    - `grep -qv 'import docker' Dockerfile` AND no python `docker` SDK added to the image (subprocess-only path)
    - `grep -Eq '^FROM python:3\.(1[1-9]|[2-9][0-9])' Dockerfile` (>=3.11 concrete pinned base, no floating `:latest`)
    - `grep -q '.venv' .dockerignore && grep -q '.git' .dockerignore && grep -q '.sbxwork' .dockerignore`
    - `git diff --stat -- src/` is EMPTY (zero src change)
  </acceptance_criteria>
  <done>
    A single root Dockerfile builds the shared D-01 image with the docker CLI binary; a lean
    .dockerignore exists. No src/ change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the two compose-variant model profiles with service-DNS api_base (Finding #1 + A5)</name>
  <files>config/model_gateway.vllm.compose.yaml, config/model_gateway.cpu.compose.yaml</files>
  <read_first>
    - config/model_gateway.vllm.yaml (the WHOLE file — the compose variant is byte-identical EXCEPT the three api_base values; keep frozen deployment names low/medium/high-complexity, the `/v1` suffix, fallbacks, litellm_settings, general_settings verbatim)
    - config/model_gateway.ollama.yaml (the WHOLE file — the cpu variant's source; keep `ollama_chat/` prefix, NO `/v1` suffix, frozen names, settings verbatim)
    - .planning/phases/10-full-stack-local-compose/10-RESEARCH.md § "Pattern 1: Compose-variant model profile mount" + § "Inter-service DNS / env wiring table" (the api_base row is the ONE non-env-override; vLLM keeps /v1, Ollama no /v1)
    - .planning/phases/10-full-stack-local-compose/10-PATTERNS.md § "config/model_gateway.vllm.compose.yaml" + "config/model_gateway.cpu.compose.yaml" (exact-match analog; A5 filename gotcha)
  </read_first>
  <action>
    Create `config/model_gateway.vllm.compose.yaml` as a copy of `config/model_gateway.vllm.yaml`
    with ONLY the three `api_base` values changed from `http://localhost:8000/v1` to
    `http://vllm:8000/v1` (the compose `vllm` service DNS). Keep the `/v1` suffix (LiteLLM
    `hosted_vllm` appends only `chat/completions`). Keep the frozen `low/medium/high-complexity`
    model_name keys, the `hosted_vllm/Qwen/Qwen2.5-7B-Instruct` model strings, router_settings
    fallbacks, litellm_settings, and the inert general_settings verbatim. Update the header
    comment to state this is the IN-COMPOSE variant (service-DNS api_base) and that the
    Phase-8 `vllm.yaml` stays frozen for host/loopback use.
    Create `config/model_gateway.cpu.compose.yaml` as a copy of `config/model_gateway.ollama.yaml`
    with ONLY the three `api_base` values changed from `http://localhost:11434` to
    `http://ollama:11434` (the compose `ollama` service DNS, NO `/v1`). Keep the `ollama_chat/`
    prefix and all other settings verbatim.
    A5 FILENAME GOTCHA (load-bearing): the CPU variant MUST be named `…cpu.compose.yaml` (NOT
    `…ollama.compose.yaml`) — the 10-02 compose mount source interpolates `${MODEL_PROFILE}`
    whose CPU value is `cpu`, so `MODEL_PROFILE=cpu` resolves to `…cpu.compose.yaml`. Naming it
    `.ollama.compose.yaml` would point the interpolation at a non-existent file → mount fails.
    Do NOT edit the Phase-8 `vllm.yaml`/`ollama.yaml` files (anti-pattern: breaks host
    `make use-vllm`/`run-vllm` loopback + Phase-11's localhost api_base assertion).
  </action>
  <acceptance_criteria>
    - `test -f config/model_gateway.vllm.compose.yaml && test -f config/model_gateway.cpu.compose.yaml`
    - `grep -c 'api_base: http://vllm:8000/v1' config/model_gateway.vllm.compose.yaml` returns 3 (all three tiers point at service DNS)
    - `grep -c 'api_base: http://ollama:11434' config/model_gateway.cpu.compose.yaml` returns 3
    - `! grep -q 'localhost' config/model_gateway.vllm.compose.yaml && ! grep -q 'localhost' config/model_gateway.cpu.compose.yaml` (no loopback leaked into the compose variants)
    - `grep -c 'model_name: low-complexity\|model_name: medium-complexity\|model_name: high-complexity' config/model_gateway.vllm.compose.yaml` returns 3 (frozen deployment names preserved)
    - `grep -q 'ollama_chat/' config/model_gateway.cpu.compose.yaml` (chat-variant prefix preserved)
    - `git diff --stat -- config/model_gateway.vllm.yaml config/model_gateway.ollama.yaml` is EMPTY (frozen Phase-8 files untouched)
    - `git diff --stat -- src/` is EMPTY (zero src change)
  </acceptance_criteria>
  <done>
    Two compose-variant model profiles exist with service-DNS api_base (vllm:8000/v1,
    ollama:11434), frozen names preserved, Phase-8 files untouched, CPU variant named for the
    `cpu` MODEL_PROFILE value (A5). No src/ change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build context → image | repo files copied into the shared image; .dockerignore limits what crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Information Disclosure | Dockerfile build context | mitigate | `.dockerignore` excludes `.venv`, `.git`, `.env`, `.sbxwork`, active config swap state so secrets/local state never enter the image layer |
| T-10-01-02 | Tampering | docker CLI binary in image | accept | Installing the docker CLI is required for the worker DooD path (Finding #2); the privileged socket that makes it dangerous is wired (and disclosed dev-only) in 10-02, not here. Image itself only carries the client binary. |
| T-10-01-SC | Tampering | pip/apt installs in Dockerfile | accept | No NEW Python packages introduced (RESEARCH Package Legitimacy Audit: zero new pip/npm; all extras pre-vetted Phases 1–6). docker CLI is an OS package from the base distro repo. No blocking supply-chain checkpoint required. |

No HIGH-severity threat. Config/build-artifact-only scope, local-only posture.
</threat_model>

<verification>
- `test -f Dockerfile && test -f .dockerignore && test -f config/model_gateway.vllm.compose.yaml && test -f config/model_gateway.cpu.compose.yaml`
- `grep -q 'runtime,agents,gui' Dockerfile` and `grep -Eiq 'docker\.io|docker-cli|docker-ce-cli' Dockerfile`
- `grep -c 'vllm:8000/v1' config/model_gateway.vllm.compose.yaml` == 3; `grep -c 'ollama:11434' config/model_gateway.cpu.compose.yaml` == 3
- `git diff --stat -- src/` empty
</verification>

<success_criteria>
- The shared D-01 image Dockerfile installs `.[runtime,agents,gui]` and carries the docker CLI binary (Finding #2).
- Two compose-variant model profiles exist with service-DNS api_base (Finding #1), CPU variant named `cpu` (A5), Phase-8 files frozen.
- Zero `src/` change.
</success_criteria>

<output>
Create `.planning/phases/10-full-stack-local-compose/10-01-SUMMARY.md` when done.

CROSS-PLAN PATH CONTRACT (load-bearing for Finding #1 — record explicitly in the SUMMARY
so 10-02's mount target is copy-paste, not re-derived): state the exact `WORKDIR` the
Dockerfile sets and the resulting ABSOLUTE path of the active config the container reads
(`<WORKDIR>/config/model_gateway.config.yaml`, e.g. `/app/config/model_gateway.config.yaml`).
`build_router` reads the relative `config/model_gateway.config.yaml`, which resolves only
when the container CWD == WORKDIR and `config/` was copied under it. 10-02 mounts the
compose-variant onto THIS exact absolute path; a drift (different WORKDIR / config copied
elsewhere) makes the mount silently miss and `docker compose config` (Pitfall 3) will NOT
catch it — a green COMPOSE-03 over a hollow SC-1.
</output>
