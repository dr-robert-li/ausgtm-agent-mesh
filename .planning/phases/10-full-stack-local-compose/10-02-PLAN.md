---
phase: 10-full-stack-local-compose
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - docker-compose.yml
autonomous: true
requirements: [COMPOSE-01]
must_haves:
  truths:
    - "`docker compose config` parses the single-file stack; core api/worker/gui/postgres are non-profiled (always-on)"
    - "Postgres is `pgvector/pgvector:pg16` with creds/port/db matching the Phase-9 pinned DATABASE_URL (postgres:postgres@postgres:5432/agent_mesh)"
    - "api AND worker mount the compose-variant model profile onto config/model_gateway.config.yaml via ${MODEL_PROFILE} interpolation (Finding #1) — completions reach the backend service DNS, not localhost"
    - "The worker mounts the docker socket AND an identical-path host bind with TMPDIR redirect so the real P2 sandbox runs in-stack (Finding #2)"
    - "A one-shot `migrate` core service applies migrations 0001-0004 over mesh postgres before api/worker/gui start (Pitfall 5) — the composed stack comes up with a schema"
    - "Langfuse v3 (6 containers) is declared inline but profile-gated [langfuse]; its OWN postgres is `langfuse-postgres` (Pitfall 1 collision avoided)"
    - "vLLM (profiles:[vllm]) and Ollama (profiles:[cpu]) model backends are declared with pinned concrete tags; NO standalone litellm container (D-06)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Single-file profile-gated full-stack compose (core + langfuse + vllm/cpu profiles + migrate)"
      contains: "pgvector/pgvector:pg16"
  key_links:
    - from: "docker-compose.yml api/worker services"
      to: "config/model_gateway.${MODEL_PROFILE}.compose.yaml (Finding #1)"
      via: "volume mount onto config/model_gateway.config.yaml"
      pattern: "model_gateway.\\$\\{MODEL_PROFILE.*\\.compose.yaml.*model_gateway.config.yaml"
    - from: "docker-compose.yml worker service"
      to: "host docker daemon + identical-path sandbox dir (Finding #2)"
      via: "docker.sock mount + source==target bind + TMPDIR"
      pattern: "/var/run/docker.sock:/var/run/docker.sock"
    - from: "docker-compose.yml worker service"
      to: "executor tempdir (Finding #2 TMPDIR)"
      via: "TMPDIR env pointing at the shared identical-path bind"
      pattern: "TMPDIR"
    - from: "docker-compose.yml migrate service"
      to: "migrations/0001-0004 over mesh postgres (Pitfall 5)"
      via: "psql -v ON_ERROR_STOP=1 -f over ./migrations bind"
      pattern: "ON_ERROR_STOP"
    - from: "docker-compose.yml api/worker/gui"
      to: "migrate service (Pitfall 5)"
      via: "depends_on service_completed_successfully"
      pattern: "service_completed_successfully"
    - from: "docker-compose.yml langfuse-web"
      to: "langfuse-postgres (Pitfall 1)"
      via: "renamed Langfuse pg (NOT the mesh postgres)"
      pattern: "langfuse-postgres"
---

<objective>
Author the single-file `docker-compose.yml` that assembles the whole mesh: non-profiled
core (api, worker, gui, postgres, migrate) + `profiles:[langfuse]` (Langfuse v3, 6
containers) + `profiles:[vllm]`/`profiles:[cpu]` model backends. This plan threads the
THREE load-bearing functional gaps the static COMPOSE-03 test cannot catch:
- Finding #1: mount the compose-variant model profile so api_base is service-DNS, not localhost.
- Finding #2: worker docker-socket mount + identical-path host bind + TMPDIR for the real DooD sandbox.
- Pitfall 5: a one-shot `migrate` service so a fresh `make compose-up` comes up with a schema.

Purpose: COMPOSE-01 — one file declares the required service set. D-06: NO standalone
litellm container (the in-process Router in api/worker is the control plane). D-03: full
Langfuse v3 inlined but profile-gated. D-04/D-05: vLLM default profile, Ollama under `cpu`.

Output: `docker-compose.yml` (the ONLY file this plan touches — clean ownership for parallel
waves).

HARD GUARDRAIL: config/docker-compose/docs/tests ONLY. ZERO `src/` change. Every `src/`
path referenced below is READ-ONLY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-full-stack-local-compose/10-CONTEXT.md
@.planning/phases/10-full-stack-local-compose/10-RESEARCH.md
@.planning/phases/10-full-stack-local-compose/10-PATTERNS.md
@.planning/phases/10-full-stack-local-compose/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core services + model backends — api/worker/gui/postgres + vllm/ollama profiles, with Finding #1 + Finding #2 wiring</name>
  <files>docker-compose.yml</files>
  <read_first>
    - Makefile:63-70 (the exact `command:` strings the three services run on the shared image — D-01: `uvicorn agent_mesh.api.app:app --port 8080`, `python -m agent_mesh.worker.main`, `streamlit run src/agent_mesh/gui/admin_app.py`; drop `--reload`)
    - Makefile:109-116 (run-pg: pgvector/pgvector:pg16, POSTGRES_USER/PASSWORD/DB=postgres/postgres/agent_mesh, port 5432, named volume — the mesh `postgres` service MUST match for DSN parity)
    - src/agent_mesh/sandbox/executor.py:75-98,120-145 (Finding #2 evidence: `-v {snippet_dir}:/snippet:ro` where snippet_dir is the `tempfile.TemporaryDirectory` workdir with NO `dir=`, honors TMPDIR; `shutil.which("docker")` gate at :131)
    - config/model_gateway.vllm.compose.yaml + config/model_gateway.cpu.compose.yaml (the 10-01 variants mounted here onto config/model_gateway.config.yaml)
    - .planning/phases/10-full-stack-local-compose/10-RESEARCH.md § "vLLM / Ollama compose service" (images, GPU `deploy.resources.reservations.devices`, ipc:host/shm_size, Pitfall-2 healthchecks — Ollama `ollama list`, vLLM python urllib probe, start_period) + § "Pattern 1" + § "Pattern 2" (the VERIFIED Finding #1/#2 yaml shapes) + § "Inter-service DNS / env wiring table"
    - .planning/phases/10-full-stack-local-compose/10-PATTERNS.md § "docker-compose.yml (composite)" + § "Shared Patterns" (Finding #1, Finding #2 apply-to targets)
    - scripts/gcp_deploy_core.sh:16-18 (deployment-name parity: agent-mesh-api/-worker/-code-executor — do not break service-name parity)
  </read_first>
  <action>
    Begin `docker-compose.yml` (single file, compose v2 — no obsolete `version:` key needed).
    Declare CORE (no `profiles:` key → always-on): `api`, `worker`, `gui`, `postgres`.
    All three app services `build: .` (the 10-01 Dockerfile, one image — D-01) and differ ONLY
    by `command:` (the exact Makefile:63-70 strings, no `--reload`; api on container port 8080).
    Wire env overrides per the RESEARCH wiring table: `DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agent_mesh`
    (creds/port/db identical to Phase-9; host localhost→`postgres`), `LANGFUSE_HOST=http://langfuse-web:3000`,
    `LANGFUSE_PUBLIC_KEY`/`SECRET_KEY` blank (UI-minted, D-03a). Map `api` port `8080:8080`.
    `postgres` service: image `pgvector/pgvector:pg16`, env POSTGRES_USER/PASSWORD/DB=postgres/postgres/agent_mesh,
    a NAMED volume (e.g. `mesh_pgdata:/var/lib/postgresql/data`), healthcheck `pg_isready -U postgres`.
    FINDING #1 (apply to api AND worker): add a read-only volume mounting the compose-variant
    profile onto the active config path, source interpolated by MODEL_PROFILE:
    `./config/model_gateway.${MODEL_PROFILE:-vllm}.compose.yaml:/app/config/model_gateway.config.yaml:ro`.
    (Adjust the container-side target to the WORKDIR the 10-01 Dockerfile set, e.g. `/app/config/...`.)
    FINDING #2 (apply to worker ONLY): add `volumes: [/var/run/docker.sock:/var/run/docker.sock,
    ${MESH_SBX_DIR:-/tmp/agent-mesh-sbx}:${MESH_SBX_DIR:-/tmp/agent-mesh-sbx}]` (the second bind
    source==target IDENTICAL absolute string — a mismatched bind fails loudly "Mounts denied"),
    `environment: TMPDIR=${MESH_SBX_DIR:-/tmp/agent-mesh-sbx}`, and `group_add: ["${DOCKER_GID:-999}"]`
    (non-root socket gid; host-dependent, dev-only — A1). The real P2 executor runs UNMODIFIED.
    MODEL BACKENDS: `vllm` service (`profiles: [vllm]`, image `vllm/vllm-openai` PINNED to a
    concrete tag — replace `:latest`; command `--model Qwen/Qwen2.5-7B-Instruct
    --enable-auto-tool-choice --tool-call-parser hermes`; port 8000; `ipc: host` or
    `shm_size: "8gb"`; `deploy.resources.reservations.devices` nvidia/count:all/capabilities:[gpu];
    healthcheck python urllib probe of `/health` with start_period >=120s — Pitfall 2). `ollama`
    service (`profiles: [cpu]`, image `ollama/ollama` PINNED concrete tag; port 11434; named
    volume `ollama_data:/root/.ollama`; healthcheck `ollama list || exit 1` — image ships no curl;
    start_period >=60s). Do NOT add any `litellm` service (D-06).
    Declare the named volumes (`mesh_pgdata`, `ollama_data`) in a top-level `volumes:` block.
  </action>
  <acceptance_criteria>
    - `docker compose config -q` exits 0 (parses) — loud-skip the assertion if `docker` absent
    - `docker compose config --services | sort | tr '\n' ' '` contains `api gui postgres worker` (core non-profiled present)
    - `! docker compose config --services | grep -qx litellm` (NO standalone litellm container — D-06)
    - `docker compose config --profile vllm --profile cpu | grep -q 'pgvector/pgvector:pg16'`
    - `grep -E 'model_gateway\.\$\{MODEL_PROFILE.*\}\.compose\.yaml.*model_gateway\.config\.yaml' docker-compose.yml` (Finding #1 mount on api/worker — appears twice)
    - `grep -c 'model_gateway.config.yaml:ro' docker-compose.yml` returns 2 (api + worker both mount the variant)
    - `grep -q '/var/run/docker.sock:/var/run/docker.sock' docker-compose.yml` (Finding #2 socket mount)
    - `grep -q 'TMPDIR' docker-compose.yml` AND `grep -q 'MESH_SBX_DIR' docker-compose.yml` (Finding #2 identical-path bind + redirect)
    - `grep -Eq 'image: *vllm/vllm-openai:[^l]' docker-compose.yml` (vLLM tag pinned, not `:latest`) AND `grep -Eq 'image: *ollama/ollama:[^l]' docker-compose.yml`
    - `grep -q 'POSTGRES_DB: *agent_mesh\|POSTGRES_DB=agent_mesh' docker-compose.yml` (DSN parity)
    - `git diff --stat -- src/` is EMPTY
  </acceptance_criteria>
  <done>
    Core api/worker/gui/postgres + vllm/cpu model backends declared; Finding #1 (variant mount on
    api+worker) and Finding #2 (worker socket + identical-path bind + TMPDIR) wired; no litellm
    container; pinned model tags. `docker compose config` parses. No src/ change.
  </done>
</task>

<task type="auto">
  <name>Task 2: One-shot `migrate` core service applying migrations 0001-0004 (Pitfall 5)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - tests/conftest.py:8-12,36-66 (the migration file list + 0001→0004 lexical order + the proof the repo NEVER self-applies migrations outside the test path; `migrations/` dir holds the four .sql files)
    - migrations/0001_init.sql, 0002_self_improvement.sql, 0003_tool_call_fields.sql, 0004_active_version.sql (the four files; CONFIRMED dollar-quote-free so `psql -f` is safe — do NOT need the conftest comment-strip/split applier)
    - .planning/phases/10-full-stack-local-compose/10-RESEARCH.md § "Pitfall 5" (the one-shot migrate-service fix: psql -v ON_ERROR_STOP=1 -f over ordered files; api/worker depends_on service_completed_successfully) + § "Open discretion items > mesh-DB migration applier"
    - .planning/phases/10-full-stack-local-compose/10-PATTERNS.md § "migrate one-shot service" (analog = conftest file-list+order ONLY; mechanism is NEW psql -f, not psycopg)
  </read_first>
  <action>
    Add a one-shot `migrate` CORE service (no `profiles:` key → runs every `make compose-up`).
    Base it on `pgvector/pgvector:pg16` (it ships `psql`). Bind-mount the repo migrations
    read-only: `./migrations:/migrations:ro` (the pgvector image has psql but NOT the project's
    SQL files). Run an entrypoint/command that applies each file in lexical order with
    `psql -v ON_ERROR_STOP=1 -f /migrations/0001_init.sql` … `0004_active_version.sql` against
    the mesh postgres (host `postgres`, db `agent_mesh`, user/pass postgres/postgres, e.g. via
    `PGPASSWORD` + `-h postgres -U postgres -d agent_mesh`), then exit 0. The four files are
    dollar-quote-free (verified) so straight `psql -f` is safe — do NOT reimplement the conftest
    comment-strip/split logic. `depends_on: { postgres: { condition: service_healthy } }` so the
    DB is ready first. Then add to api, worker, AND gui:
    `depends_on: { migrate: { condition: service_completed_successfully } }` (merge with each
    service's existing postgres dependency) so app services never boot against a schema-less DB.
    This blocks SC-1-as-functional; the static COMPOSE-03 test will NOT catch its absence, so it
    must be present and correct here.
  </action>
  <acceptance_criteria>
    - `docker compose config --services | grep -qx migrate` (migrate is a declared core/non-profiled service)
    - `grep -q './migrations:/migrations:ro' docker-compose.yml` (migrations bind-mounted into the applier)
    - `grep -c 'ON_ERROR_STOP' docker-compose.yml` >= 1 AND all four files referenced: `grep -c '0001_init.sql\|0002_self_improvement.sql\|0003_tool_call_fields.sql\|0004_active_version.sql' docker-compose.yml` returns 4
    - `grep -c 'service_completed_successfully' docker-compose.yml` >= 3 (api + worker + gui depend on migrate completing)
    - `grep -q 'migrate:' docker-compose.yml && grep -A6 'migrate:' docker-compose.yml | grep -q 'service_healthy'` (migrate waits for postgres healthy)
    - `docker compose config -q` exits 0 (still parses with the migrate service added)
    - `git diff --stat -- src/` is EMPTY
  </acceptance_criteria>
  <done>
    A one-shot `migrate` service applies 0001-0004 via psql -f over a migrations bind before
    api/worker/gui start (depends_on service_completed_successfully). Composed stack comes up
    with a schema. No src/ change.
  </done>
</task>

<task type="auto">
  <name>Task 3: Langfuse v3 self-host service set (6 containers) behind profiles:[langfuse] (D-03, Pitfall 1)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - .planning/phases/10-full-stack-local-compose/10-RESEARCH.md § "Langfuse v3 self-host service set" (the VERIFIED 6-container upstream lift: langfuse-web, langfuse-worker, langfuse-postgres, clickhouse, redis, minio; exact image tags, the secret list NEXTAUTH_SECRET/SALT/ENCRYPTION_KEY/clickhouse/redis/minio, healthchecks, depends_on service_healthy graph) + the "re-verify env-key spellings vs upstream main at plan time" note (A4)
    - .planning/phases/10-full-stack-local-compose/10-RESEARCH.md § "Pitfall 1" (mesh postgres vs Langfuse postgres name+volume collision: rename Langfuse's to `langfuse-postgres`, give it its OWN named volume, point its DATABASE_URL at `langfuse-postgres:5432` — the mesh DSN points ONLY at the pgvector `postgres`)
    - .planning/phases/10-full-stack-local-compose/10-PATTERNS.md § "No Analog Found" (Langfuse block is net-new — upstream lift, no repo analog)
  </read_first>
  <action>
    Add the Langfuse v3 self-host service set inline, EVERY service carrying `profiles: [langfuse]`
    so raw `docker compose up` stays lean and only `make compose-up`'s `--profile langfuse`
    activates them. Lift the RESEARCH § "Langfuse v3 self-host service set" block: `langfuse-web`
    (`docker.io/langfuse/langfuse:3`, port `3000:3000`, the depends_on service_healthy graph,
    healthcheck `/api/public/health`), `langfuse-worker` (`docker.io/langfuse/langfuse-worker:3`,
    same backing-store env), `langfuse-postgres` (`docker.io/postgres:17` — RENAMED from upstream
    `postgres` per Pitfall 1, its OWN named volume `langfuse_pgdata`, pg_isready healthcheck),
    `clickhouse` (`clickhouse/clickhouse-server`, named volume `clickhouse_data`, ping healthcheck),
    `redis` (`redis:7`, requirepass, redis-cli ping healthcheck), `minio` (`cgr.dev/chainguard/minio`
    — keep the Chainguard registry path verbatim, do NOT substitute minio/minio; named volume
    `minio_data`). Wire `langfuse-web`/`langfuse-worker` `DATABASE_URL` at `langfuse-postgres:5432`
    (NOT the mesh `postgres`). Server secrets (NEXTAUTH_SECRET/SALT/ENCRYPTION_KEY/clickhouse/redis/
    minio creds) are inline dev defaults each marked `# CHANGEME` (dev-only; disclosed in RUNBOOK
    by 10-03). Add `langfuse_pgdata`, `clickhouse_data`, `minio_data` to the top-level `volumes:`.
    RE-VERIFY the exact env-key spellings against the upstream langfuse `main` docker-compose.yml
    at execution time (A4) — the 6-service SET and the secret LIST are stable; individual env key
    spellings can drift. The mesh telemetry will loud-skip until UI-minted keys are set (D-03a) —
    no action here beyond leaving the mesh `LANGFUSE_PUBLIC_KEY`/`SECRET_KEY` blank (done in Task 1).
  </action>
  <acceptance_criteria>
    - `docker compose config --profile langfuse --services | sort | tr '\n' ' '` contains all six: `clickhouse langfuse-postgres langfuse-web langfuse-worker minio redis`
    - `! docker compose config --services | grep -qx langfuse-web` (langfuse services ABSENT from the bare/non-profiled set — proves profile-gating, L2)
    - `grep -q 'langfuse-postgres' docker-compose.yml && ! grep -q '^  postgres:.*langfuse\|langfuse.*pgvector' docker-compose.yml` (Pitfall 1: Langfuse pg renamed, mesh pg distinct)
    - `grep -q 'cgr.dev/chainguard/minio' docker-compose.yml` (Chainguard registry kept verbatim — not minio/minio)
    - `grep -c 'CHANGEME' docker-compose.yml` >= 4 (dev-default secrets marked for the RUNBOOK disclosure)
    - `grep -q 'langfuse_pgdata' docker-compose.yml && grep -q 'clickhouse_data' docker-compose.yml && grep -q 'minio_data' docker-compose.yml` (distinct named volumes per Langfuse store)
    - `docker compose config -q` exits 0 with all profiles
    - `git diff --stat -- src/` is EMPTY
  </acceptance_criteria>
  <done>
    Langfuse v3 (6 containers) inlined behind profiles:[langfuse]; its postgres renamed
    `langfuse-postgres` with its own volume (Pitfall 1); Chainguard minio kept; dev-default
    secrets marked # CHANGEME. Profile-gated (absent from bare config). No src/ change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker container → host docker daemon | the `/var/run/docker.sock` bind grants the worker effective host-root (DooD) — the one real escalation surface |
| operator → compose dev secrets | Langfuse/clickhouse/minio/postgres default creds are inline in the file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Elevation of Privilege | worker `/var/run/docker.sock` mount (D-02) | accept (dev-only) + mitigate | DooD grants the worker host-root-equivalent. Scope is local-dev-only; production uses Cloud Run Jobs, never a socket mount. RUNBOOK (10-03) discloses it explicitly as dev-only. Not a HIGH blocker given local-only, config-only scope. |
| T-10-02-02 | Information Disclosure | Langfuse `NEXTAUTH_SECRET`/`SALT`/`ENCRYPTION_KEY` + clickhouse/redis/minio default creds | accept (dev-only) + mitigate | Inline dev defaults each marked `# CHANGEME`; RUNBOOK (10-03) notes they are local-only and must be replaced for any non-local use. Mirrors Phase-9 DSN dev-cred handling. |
| T-10-02-03 | Information Disclosure | mesh `postgres` default creds (postgres/postgres) | accept (dev-only) | Phase-9 pinned dev DSN parity; local-only; replaced for real deployment per RUNBOOK. |
| T-10-02-04 | Tampering | mesh pg vs Langfuse pg collision (Pitfall 1) | mitigate | Langfuse pg renamed `langfuse-postgres` with its OWN volume; mesh DSN points ONLY at the pgvector `postgres` — no shared state. |

No HIGH-severity threat. The docker-socket mount is the genuine surface but is dev-only-scoped, RUNBOOK-disclosed, and never in the deploy path.
</threat_model>

<verification>
- `docker compose config -q` exits 0 (parses) — loud-skip when docker absent
- core `{api,worker,gui,postgres,migrate}` present; `litellm` absent; `pgvector/pgvector:pg16` in rendered config
- Finding #1 mount appears twice (api+worker); Finding #2 socket+TMPDIR present; migrate depends_on service_completed_successfully ≥3
- Langfuse 6-service set appears only under `--profile langfuse`; `langfuse-postgres` distinct from mesh `postgres`
- `git diff --stat -- src/` empty
</verification>

<success_criteria>
- Single-file `docker-compose.yml` parses; core always-on, langfuse/vllm/cpu profile-gated; no litellm container (D-06).
- All three functional gaps wired: Finding #1 (variant mount), Finding #2 (DooD socket+TMPDIR), Pitfall 5 (migrate service).
- Langfuse v3 6-container set with renamed `langfuse-postgres` (Pitfall 1); pinned model tags.
- Zero `src/` change.
</success_criteria>

<output>
Create `.planning/phases/10-full-stack-local-compose/10-02-SUMMARY.md` when done.
</output>
