---
phase: 07-e2e-validation-deploy-readiness
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - tests/e2e/test_e2e_mcp_durable_job.py
  - tests/e2e/test_e2e_mcp_durable_job_live.py
  - Makefile
  - RUNBOOK.md
autonomous: true
requirements: [E2E-02]
must_haves:
  truths:
    - "An in-process MCP request drives the PRODUCTION Worker/orchestrator path (Worker.process -> run_mesh -> _run_langgraph -> _select_checkpointer -> _graph_config), pausing at the write_gate interrupt as a durable checkpointed job."
    - "After a simulated process restart (drop the saver + close the sqlite connection, reopen a FRESH file-backed SqliteSaver on the SAME file), the persisted checkpoint is READABLE under the BARE production thread_id (task.task_id) and the reviewer artifact survives the restart-resume."
    - "The artifact-survival assertion is DISCRIMINATING: it reads the reopened checkpointer under _graph_config(task) and goes red when the checkpoint is lost (empty file / :memory:), not merely when the task reaches COMPLETED."
    - "The resume leg is proven to CONSUME the durable checkpoint (post-resume checkpoint decision is True), not merely stash-complete on a fresh graph — completion alone is non-discriminating (all reopen modes reach COMPLETED)."
    - "The tenant-t:: thread_id invention is removed and the false DUR-02 docstring claim is corrected: cross-task isolation comes from task_id being tenant-scoped at load time, keyed BARE on thread_id."
    - "The MCP-transport live test drives the REGISTERED FastMCP create_task tool (server.call_tool) and proves it funnels into the shared TaskService — not just that the server constructs."
    - "The Postgres durable lane has the post-close negative assertion (saver2 is not saver) and a runnable make/runbook hook so it executes on deploy-readiness validation when a DSN is present; loud-skip when absent."
    - "Production orchestrator._graph_config is UNCHANGED (test-side + seam-driven only)."
  artifacts:
    - path: "tests/e2e/test_e2e_mcp_durable_job.py"
      provides: "Default-lane E2E-02 driving the production restart-resume wiring via set_checkpointer_override + file-backed sqlite; discriminating get_tuple artifact-survival assertion under the bare production key."
      contains: "set_checkpointer_override"
    - path: "tests/e2e/test_e2e_mcp_durable_job_live.py"
      provides: "WR-01 fix (registered FastMCP tool via call_tool) + WR-02 fix (saver2 is not saver negative assertion)."
      contains: "saver2 is not saver"
    - path: "Makefile"
      provides: "test-pg target that runs the DSN-gated durable/Postgres lane against TEST_DATABASE_URL on deploy-readiness validation."
      contains: "test-pg"
    - path: "RUNBOOK.md"
      provides: "Documented hook for running the Postgres durable lane (testcontainers / Docker-Compose) on deploy-readiness validation."
      contains: "TEST_DATABASE_URL"
  key_links:
    - from: "tests/e2e/test_e2e_mcp_durable_job.py"
      to: "Worker.process -> orchestrator._run_langgraph -> _graph_config"
      via: "set_checkpointer_override(file-backed SqliteSaver) so _select_checkpointer returns it; bare task.task_id thread_id"
      pattern: "set_checkpointer_override"
    - from: "tests/e2e/test_e2e_mcp_durable_job.py"
      to: "reopened SqliteSaver checkpoint under _graph_config(task)"
      via: "saver2.get_tuple(orchestrator._graph_config(task)) is not None pre-resume; channel_values['review'] survives"
      pattern: "get_tuple"
    - from: "tests/e2e/test_e2e_mcp_durable_job_live.py"
      to: "registered FastMCP create_task tool -> shared TaskService"
      via: "await server.call_tool('create_task', {...}); svc.get_task(task_id) is not None"
      pattern: "call_tool"
---

<objective>
Close the single gap that reopened Phase 07: SC-2 / E2E-02 (CR-01). The default-lane
durable-job test currently invokes `build_graph().compile(saver).invoke(...)` DIRECTLY
under an invented `tenant-t::{task_id}` thread_id, so it proves only the SqliteSaver
drop-then-reopen PRIMITIVE — not the production durable wiring — and keys on a thread_id
production never writes. Production `_graph_config()` writes the BARE `task.task_id` and is
correct as-is.

This plan rewrites the default-lane test to drive the REAL production path
(`Worker.process -> run_mesh -> _run_langgraph -> _select_checkpointer -> _graph_config`)
by injecting a file-backed SqliteSaver through the EXISTING documented seam
`orchestrator.set_checkpointer_override`, simulates process death, drives the resume leg
through the production approval->runner->`resume_mesh` path, and asserts the surviving
artifact with a DISCRIMINATING checkpoint read (goes red on checkpoint loss). It also folds
in the two warnings on the live companion module: WR-01 (drive the registered FastMCP tool)
and WR-02 (add the post-close negative assertion + a runnable hook so the Postgres durable
lane actually executes on deploy-readiness validation).

Purpose: prove production durability through executed test code in the default gate, not by
code-reading or a primitive-only proof. Production code MUST NOT change.

Output: rewritten `tests/e2e/test_e2e_mcp_durable_job.py`, corrected
`tests/e2e/test_e2e_mcp_durable_job_live.py`, a `make test-pg` target, and a RUNBOOK note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-e2e-validation-deploy-readiness/07-CONTEXT.md
@.planning/phases/07-e2e-validation-deploy-readiness/07-VERIFICATION.md
@.planning/phases/07-e2e-validation-deploy-readiness/07-REVIEW.md

<interfaces>
<!-- Production seam + entrypoints the executor drives. Extracted from the codebase. -->
<!-- These are PRODUCTION contracts; the executor uses them directly. DO NOT modify orchestrator.py. -->

From src/agent_mesh/worker/orchestrator.py (DO NOT MODIFY — drive through the seam only):
- set_checkpointer_override(checkpointer) -> None  (line 60) — inject a file-backed SqliteSaver; pass None to clear. NEVER a purely in-memory saver.
- close_checkpointer() -> None  (line 69) — idempotent; release the cached PostgresSaver + clear cache.
- _select_checkpointer()  (line 84) — returns _CHECKPOINTER_OVERRIDE FIRST (lines 100-101) when set, else builds PostgresSaver from DATABASE_URL, else None.
- _graph_config(task) -> dict  (line 305) — returns {"configurable": {"thread_id": task.task_id}} (BARE task.task_id, line 319). The task is loaded tenant-scoped, so this is NOT a bare/arbitrary thread_id; cross-task isolation (DUR-02) comes from task_id being tenant-scoped at load time, keyed bare on thread_id. THERE IS NO tenant-t:: PREFIX in production.
- run_mesh(task) (line 241), _run_langgraph(task) (line 326, compiles with _select_checkpointer() and invokes on _graph_config(task), lines 353-357), resume_mesh(task, decision) (line 383, invokes Command(resume=decision) on _graph_config(task) at line 449).

From src/agent_mesh/worker/runner.py:
- Worker(repo=..., tool_gateway=None, resolver=None) (line 33).
- Worker.process(task_id) -> str  (line 50) — fresh run transitions RUNNING then calls run_mesh; resume path (task.state == APPROVED) calls _resume_after_approval -> resume_mesh(task, True) (line 174). Returns the resulting state value string ("awaiting_approval", "completed", ...).

From src/agent_mesh/services/task_service.py:
- request_from_mcp(tenant_id, client_slug, mcp_subject, text) and TaskService(repo=..., dispatcher=...).create_task(req) -> TaskRecord.

From src/agent_mesh/api/mcp_server.py:
- build_mcp_server(service: TaskService | None = None) returns a FastMCP("agent-mesh") with a registered create_task(prompt, mcp_subject="mcp-user") tool that funnels into the shared service.
- The FastMCP instance exposes async call_tool(name, arguments) -> (content_blocks, structured_dict); the structured dict carries {"task_id", "state"}.

EMPIRICALLY VERIFIED (in the .venv with the agents + mcp + sqlite-backend extras):
- Driving Worker.process(task_id) with a write-trigger prompt + injected file-backed SqliteSaver pauses at the graph interrupt -> returns "awaiting_approval", and the original saver holds a checkpoint under _graph_config(task).
- After drop+reopen on the SAME file + approve, Worker.process(task_id) -> "completed".
- DISCRIMINATION RESULT (the load-bearing finding): Worker.process reaches "completed" EVEN when the checkpoint is LOST (reopen on an empty file or :memory:), because the resume executes the approved write from the AWAITING_APPROVAL stash, not the checkpoint. THEREFORE asserting only on state == completed (or on the executed write) is NON-DISCRIMINATING and reproduces the CR-01 tautology. The DISCRIMINATING assertion is saver2.get_tuple(orchestrator._graph_config(task)) is not None (True for same-file, False for empty-file AND :memory:), and the surviving artifact is tuple.checkpoint["channel_values"]["review"] (non-empty). result_summary is EMPTY on the write-approval resume path (it is only persisted on the read-only completion branch), so do NOT assert on result_summary — assert on the checkpoint review channel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite default-lane E2E-02 to drive the production restart-resume wiring (DECISION 1 / CR-01)</name>
  <files>tests/e2e/test_e2e_mcp_durable_job.py</files>
  <read_first>
    - tests/e2e/test_e2e_mcp_durable_job.py (the file being rewritten — current bypass at lines 90-118; the false DUR-02 docstring at lines 17-19)
    - src/agent_mesh/worker/orchestrator.py (the seam: set_checkpointer_override:60, close_checkpointer:69, _select_checkpointer:84-101, _graph_config:305-319; DO NOT MODIFY)
    - src/agent_mesh/worker/runner.py (Worker.process:50, _resume_after_approval:163-207 — note the resume RETURN VALUE is discarded and result_summary is only persisted on the read-only branch:114-119)
    - tests/test_checkpointer_resume.py (the durability analog: file-backed SqliteSaver drop-then-reopen lines 45-83 — mirror the restart mechanism, but route it through the PRODUCTION path, not an inline compiled graph)
    - tests/conftest.py (agents_stack:90-103, repo:30-33 fixtures; the _backend_available skip pattern)
    - tests/smoke.py (request_from_mcp in-process MCP entry + approval->resume->complete chain shape)
  </read_first>
  <behavior>
    - Test PASSES (not skips) with the agents stack + sqlite backend present: the MCP request creates a task, Worker.process pauses it at the write_gate interrupt as a durable checkpointed job (returns "awaiting_approval"), and after a simulated restart on the SAME file the run resumes through the production path and the reviewer artifact survives in the durable checkpoint.
    - Test SKIPS LOUDLY (never fails, never errors at collection) when langgraph.checkpoint.sqlite is absent OR the agents_stack fixture skips — preserve the deferred-import + _backend_available pattern so a minimal env stays green.
    - The checkpoint is written/read under the BARE production thread_id (task.task_id), sourced from orchestrator._graph_config(task), never an inline tenant-t:: string.
    - The artifact-survival assertion is DISCRIMINATING: it reads the reopened saver under _graph_config(task) and would go RED on checkpoint loss (empty file / :memory:) — it does NOT rely on state == completed.
  </behavior>
  <action>
    Rewrite tests/e2e/test_e2e_mcp_durable_job.py so the single default-lane test drives the
    PRODUCTION path end-to-end and the durability proof is discriminating.

    Keep the module gating identical to today: NO live marker; gate on the agents_stack fixture
    plus _backend_available("langgraph.checkpoint.sqlite") (keep the existing _backend_available
    helper and the deferred-import pattern so the module stays collectable and skippable in a
    minimal env). Keep the write-trigger prompt constant
    _PROMPT = "create a new HubSpot deal for ACME Corp" so the graph pauses at the write_gate.

    Use fixtures agents_stack, repo, tmp_path. Inside the test:

    (1) Create the task IN-PROCESS via the real MCP ingress: svc = TaskService(repo=repo,
    dispatcher=InProcessDispatcher()); task = svc.create_task(request_from_mcp(tenant_id="t",
    client_slug="c", mcp_subject="svc", text=_PROMPT)). This part of the original test was
    already correct.

    (2) Build a FILE-backed SqliteSaver on a tempfile path under tmp_path (e.g.
    db_path = str(tmp_path / "checkpoints.sqlite")), open a sqlite3.connect(db_path,
    check_same_thread=False), wrap in SqliteSaver(conn), and INJECT it via
    orchestrator.set_checkpointer_override(saver). This is the documented seam that
    _select_checkpointer() returns first — NO new production seam is created. NEVER :memory:.

    (3) Compute the production config ONCE: cfg = orchestrator._graph_config(task) and assert
    cfg["configurable"]["thread_id"] == task.task_id (this kills the tenant-t:: invention by
    construction and pins the bare production key).

    (4) Drive the PAUSE leg through production: w = Worker(repo=repo); assert
    w.process(task.task_id) == "awaiting_approval". This routes through run_mesh ->
    _run_langgraph -> _select_checkpointer (returns the override) -> _graph_config, writing the
    checkpoint under the BARE task.task_id. Assert the checkpoint exists in the ORIGINAL saver:
    saver.get_tuple(cfg) is not None.

    (5) Simulate PROCESS DEATH: orchestrator.set_checkpointer_override(None); del saver;
    conn.close(). Then reopen a FRESH saver on the SAME file: conn2 = sqlite3.connect(db_path,
    check_same_thread=False); saver2 = SqliteSaver(conn2);
    orchestrator.set_checkpointer_override(saver2).

    (6) DISCRIMINATING pre-resume assertion: assert saver2.get_tuple(cfg) is not None (the
    persisted checkpoint is readable from the reopened store under the production key). Then
    assert the surviving artifact is already in the checkpoint:
    saver2.get_tuple(cfg).checkpoint["channel_values"].get("review") is NON-EMPTY (the reviewer
    artifact survived the restart). This is the assertion that flips red on checkpoint loss — it
    is what makes the proof non-tautological. (EMPIRICALLY VERIFIED: present for same-file,
    absent for empty-file AND :memory:.)

    (7) Drive the RESUME leg through production: approve via the shared ledger, then
    Worker.process. Find the opened approval (repo.list_approvals(task.task_id, "t")[0]), approve
    through the service (svc.submit_approval_decision(record.approval_record_id,
    ApprovalDecision.APPROVED, "mcp:svc", "mcp")), then assert w.process(task.task_id) ==
    "completed". This routes the resume through _resume_after_approval -> resume_mesh(task, True)
    -> Command(resume=True) on _graph_config(task) — the production resume path on the
    BARE key.

    (7b) DISCRIMINATING resume-CONSUMPTION assertion (mandatory — completion alone is NOT
    enough): after the resume reaches "completed", assert the resume actually CONSUMED the
    durable checkpoint rather than stash-completing on a fresh graph:
    assert saver2.get_tuple(cfg).checkpoint["channel_values"].get("decision") is True. EMPIRICALLY
    VERIFIED present (True) for the same-file restart but None for an empty-file / :memory: reopen
    (the fresh graph never resumed). The pre-resume get_tuple read (step 6) proves the checkpoint
    SURVIVED; THIS read proves the resume leg CONSUMED it — keep BOTH (they flip red on different
    failures: survival loss vs stash-completion-without-resume). Note: post-resume "review" is
    NON-discriminating (present in all modes) — do NOT substitute it for the decision read.

    (8) Wrap the saver lifecycle in try/finally with TEARDOWN HYGIENE so the override never leaks
    into other tests: in finally, call orchestrator.set_checkpointer_override(None) and
    conn2.close() (and guard the original conn close). The override-leak guard is mandatory — a
    leaked override would corrupt unrelated agents-gated tests.

    DELETE the tenant-t:: thread_id construction entirely. REWRITE the module + test docstrings:
    remove the false DUR-02 claim that tenant-t:: is the production form; state accurately that
    the resume keys on the BARE tenant-scoped task.task_id (production _graph_config) and that
    cross-task isolation comes from task_id being tenant-scoped at LOAD time, keyed bare on
    thread_id. State that this test now drives the production Worker/orchestrator wiring (not an
    inline compiled graph) and that the artifact-survival assertion reads the reopened
    checkpointer (discriminating on checkpoint loss). Do NOT inline any fenced code in the
    docstring; keep it prose.

    DO NOT modify any file under src/. orchestrator._graph_config MUST be unchanged.
  </action>
  <verify>
    <automated>cd /Users/robertli/Desktop/consulting/ausgtm-agent-mesh && PYTHONPATH=src .venv/bin/python -m pytest tests/e2e/test_e2e_mcp_durable_job.py -m "not live" -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "tenant-t::" tests/e2e/test_e2e_mcp_durable_job.py == 0  (invention removed)
    - grep -c "set_checkpointer_override" tests/e2e/test_e2e_mcp_durable_job.py >= 2  (inject + clear via the production seam)
    - grep -c "Worker(" tests/e2e/test_e2e_mcp_durable_job.py >= 1  (production entrypoint driven)
    - grep -c "_graph_config" tests/e2e/test_e2e_mcp_durable_job.py >= 1  (bare production key sourced from production)
    - grep -c "get_tuple" tests/e2e/test_e2e_mcp_durable_job.py >= 1  (discriminating checkpoint read)
    - grep -c ":memory:" tests/e2e/test_e2e_mcp_durable_job.py == 0 AND grep -Ec "\.sqlite" tests/e2e/test_e2e_mcp_durable_job.py >= 1  (SPOF not masked; file-backed)
      EXECUTOR NOTE: this grep runs against the TEST FILE — do NOT write the literal token `:memory:` in any test docstring/comment or this criterion self-fails. Express the prohibition as "never an in-memory connection target".
    - grep -c "build_graph().compile" tests/e2e/test_e2e_mcp_durable_job.py == 0  (no direct-invoke bypass remains)
    - The test asserts BOTH the pre-resume survival read AND the post-resume consumption read:
      grep -Ec 'get_tuple\(cfg\)' tests/e2e/test_e2e_mcp_durable_job.py >= 2 (survival + consumption),
      AND grep -c '"decision"' tests/e2e/test_e2e_mcp_durable_job.py >= 1 with a post-resume
      `... .get("decision") is True` assertion (resume CONSUMED the checkpoint, not stash-completed).
    - PYTHONPATH=src .venv/bin/python -m pytest tests/e2e/test_e2e_mcp_durable_job.py -m "not live" -q exits 0 with "1 passed" (stack present); in a minimal env it LOUD-SKIPS (never fails/errors).
    - git diff --stat src/agent_mesh/worker/orchestrator.py shows NO change (production wiring untouched).
  </acceptance_criteria>
  <done>
    The default-lane E2E-02 test drives Worker.process -> orchestrator._run_langgraph ->
    _select_checkpointer (override) -> _graph_config on both the pause and resume legs, keys on
    the BARE production thread_id, simulates restart on a file-backed sqlite store, and asserts
    the reviewer artifact survives via a discriminating reopened-checkpoint read. The tenant-t::
    invention and the false DUR-02 docstring are gone. Production code is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-01 — drive the REGISTERED FastMCP create_task tool in the live companion (DECISION 2)</name>
  <files>tests/e2e/test_e2e_mcp_durable_job_live.py</files>
  <read_first>
    - tests/e2e/test_e2e_mcp_durable_job_live.py (the file being edited — current WR-01 offender test_e2e_mcp_transport_create_task lines 121-153 asserts only server is not None then calls svc.create_task directly)
    - src/agent_mesh/api/mcp_server.py (build_mcp_server:60-115; the registered create_task tool:76-87 funnels into the shared TaskService; the FastMCP instance exposes async call_tool)
    - tests/conftest.py (repo fixture:30-33; _backend_available skip pattern reused in this module:39-45)
  </read_first>
  <action>
    Fix test_e2e_mcp_transport_create_task so it actually invokes the REGISTERED FastMCP tool
    rather than re-deriving the in-process path. EMPIRICALLY CONFIRMED feasible in the .venv: the
    mcp extra is installed and FastMCP.call_tool(name, arguments) is an async coroutine returning
    a (content_blocks, structured_dict) tuple whose structured dict carries {"task_id", "state"};
    the task lands in the shared TaskService's repo.

    Implement option (a) (drive the registered tool), since the harness exists:
    - Keep the loud skip gate: if not _backend_available("mcp"): pytest.skip(...).
    - Build the server over a shared service: svc = TaskService(repo=repo,
      dispatcher=InProcessDispatcher()); server = build_mcp_server(svc).
    - Invoke the REGISTERED tool in-process. call_tool is async — run it via
      asyncio.run(server.call_tool("create_task", {"prompt": "summarize notes"})) (or wrap a
      small local async helper). Unpack the result tuple; read task_id from the structured dict
      (the second tuple element), e.g. result[1]["task_id"]. If the FastMCP version returns only
      content blocks, parse task_id out of the JSON text in the first block — but prefer the
      structured dict, which this version provides.
    - Assert the task FUNNELS INTO THE SHARED SERVICE: fetched = svc.get_task(task_id); assert
      fetched is not None; assert fetched.task_id == task_id. A broken/misrouted tool
      registration now fails this test (the original could not).
    - Update the docstring: the test now drives the REGISTERED transport create_task tool via the
      FastMCP in-process call_tool harness and proves it funnels into the shared TaskService —
      drop any wording implying the tool is exercised when only the server is constructed. No
      fenced code in the docstring.

    Keep test_e2e_postgres_durable_restart untouched in THIS task (Task 3 edits it). Do NOT
    modify any file under src/.
  </action>
  <verify>
    <automated>cd /Users/robertli/Desktop/consulting/ausgtm-agent-mesh && PYTHONPATH=src .venv/bin/python -m pytest tests/e2e/test_e2e_mcp_durable_job_live.py::test_e2e_mcp_transport_create_task -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "call_tool" tests/e2e/test_e2e_mcp_durable_job_live.py >= 1  (registered tool driven, not bypassed)
    - grep -c "server is not None" tests/e2e/test_e2e_mcp_durable_job_live.py == 0  (the construction-only sole assertion is removed/superseded)
    - PYTHONPATH=src .venv/bin/python -m pytest tests/e2e/test_e2e_mcp_durable_job_live.py::test_e2e_mcp_transport_create_task -q exits 0 with "1 passed" when the mcp extra is present; LOUD-SKIPS when absent.
    - git diff --stat src/ shows NO change.
  </acceptance_criteria>
  <done>
    The MCP-transport test invokes the registered FastMCP create_task tool via call_tool, reads
    back the created task through the shared TaskService, and would fail on a broken tool
    registration. The false "funnels into" claim over a construction-only assertion is removed.
  </done>
</task>

<task type="auto">
  <name>Task 3: WR-02 — Postgres-lane negative assertion + runnable deploy-readiness hook (DECISION 2)</name>
  <files>tests/e2e/test_e2e_mcp_durable_job_live.py, Makefile, RUNBOOK.md</files>
  <read_first>
    - tests/e2e/test_e2e_mcp_durable_job_live.py (test_e2e_postgres_durable_restart:53-118; the close_checkpointer + re-select at lines 106-110 is missing the saver2 is not saver assertion; the tenant-t:: thread_id at line 92 must also be corrected to the bare production key)
    - src/agent_mesh/worker/orchestrator.py (close_checkpointer:69-81, _select_checkpointer:84-125 — close drops the cache so a fresh select builds a NEW saver object; _graph_config:305-319 for the bare key)
    - tests/test_checkpointer_resume.py (the Postgres analog lines 86-125 — same close_checkpointer/_select_checkpointer lifecycle to mirror)
    - tests/conftest.py (pg_dsn fixture:74-87 — TEST_DATABASE_URL gated, applies migrations, skips when unset)
    - Makefile (test:31, test-live:34 targets — add a sibling test-pg target)
    - RUNBOOK.md (local smoke/test section — add the TEST_DATABASE_URL durable-lane note)
  </read_first>
  <action>
    Two parts. Do NOT modify any file under src/. _graph_config MUST stay unchanged.

    Part A — test_e2e_postgres_durable_restart (the DSN-gated Postgres lane):
    - Add the MISSING post-close negative assertion: after orchestrator.close_checkpointer() and
      saver2 = orchestrator._select_checkpointer(), add
      assert saver2 is not saver, "close_checkpointer must drop cache; new saver expected"
      (keep the existing assert saver2 is not None). This proves the cache is dropped and a fresh
      saver is built (WR-02 b).
    - While here, correct the tenant-t:: thread_id at line 92 to the BARE production key: replace
      the inline thread_id = f"tenant-t::{task.task_id}" + manual config with
      config = orchestrator._graph_config(task) so the Postgres lane also keys on the bare
      production thread_id (consistent with Task 1 and with production _graph_config). Update the
      DUR-02 docstring lines (25-26) to the accurate bare-key wording (tenant-scoped at load time,
      not a tenant-t:: prefix).
    - Keep the lane DSN-gated and loud-skip when TEST_DATABASE_URL is unset / the postgres
      backend is absent (do not add a live marker — it is DSN-gated, not creds-gated).

    Part B — make the Postgres durable lane RUN on deploy-readiness (WR-02 a):
    - Add a test-pg target to the Makefile that runs the DSN-gated durable lane, e.g. it runs
      pytest -q -m "not live" over tests/e2e/test_e2e_mcp_durable_job_live.py and
      tests/test_checkpointer_resume.py with TEST_DATABASE_URL expected in the environment. Mirror
      the existing test / test-live target style (PYTHONPATH=$(PYTHONPATH), $(PY) -m pytest). Add
      a test-pg entry to the help recipe and the .PHONY line. The target must LOUD-SKIP (via the
      existing pg_dsn fixture) when TEST_DATABASE_URL is unset — it must not hard-fail on a
      machine with no Postgres.
    - Add a RUNBOOK.md note under the test/validation section documenting that the Postgres
      durable checkpointer lane is DSN-gated and skipped by default, and that deploy-readiness
      validation MUST run it against a real Postgres by exporting TEST_DATABASE_URL (point at a
      pgvector-enabled Postgres — e.g. a testcontainers or Docker-Compose pgvector/pgvector:pg16
      instance, matching the conftest migration note) and running make test-pg. Keep it loud about
      the skip-when-absent behavior so the gap is never silent.
  </action>
  <verify>
    <automated>cd /Users/robertli/Desktop/consulting/ausgtm-agent-mesh && grep -c "saver2 is not saver" tests/e2e/test_e2e_mcp_durable_job_live.py && grep -c "test-pg" Makefile && PYTHONPATH=src .venv/bin/python -m pytest tests/e2e/test_e2e_mcp_durable_job_live.py -m "not live" -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "saver2 is not saver" tests/e2e/test_e2e_mcp_durable_job_live.py >= 1  (post-close negative assertion present)
    - grep -c "tenant-t::" tests/e2e/test_e2e_mcp_durable_job_live.py == 0  (invention removed from the Postgres lane too)
    - grep -c "test-pg" Makefile >= 1 (target exists) AND test-pg appears in the .PHONY line and the help recipe.
    - grep -c "TEST_DATABASE_URL" RUNBOOK.md >= 1  (deploy-readiness hook documented)
    - make test-pg on a machine WITHOUT TEST_DATABASE_URL exits 0 with the Postgres lane LOUD-SKIPPED (the pg_dsn fixture skip message) — it does NOT hard-fail.
    - PYTHONPATH=src .venv/bin/python -m pytest tests/e2e/test_e2e_mcp_durable_job_live.py -m "not live" -q exits 0 (Postgres lane loud-skips without a DSN; MCP-transport test passes with the mcp extra).
    - git diff --stat src/ shows NO change.
  </acceptance_criteria>
  <done>
    The Postgres durable lane carries the saver2 is not saver negative assertion, keys on the bare
    production thread_id via _graph_config, and is runnable on deploy-readiness via make test-pg
    with a documented RUNBOOK note (testcontainers / pgvector Docker-Compose). It loud-skips
    without a DSN. Production code is unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> production checkpointer seam | The test injects a saver via set_checkpointer_override; the override must never leak into other tests (teardown hygiene) or it would corrupt unrelated agents-gated runs. |
| reopened checkpoint store -> resume decision | The durable checkpoint is the resume authority for the graph leg; a lost/empty checkpoint that still reaches COMPLETED would silently mask a durability SPOF (the exact CR-01 failure). |
| TEST_DATABASE_URL (Postgres lane) | A real DSN crosses into the test process; it must never be logged and must loud-skip when absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Spoofing (durability SPOF masking) | E2E-02 artifact-survival assertion | mitigate | Assertion reads the reopened checkpointer via saver2.get_tuple(_graph_config(task)) and asserts the review channel survived — EMPIRICALLY VERIFIED to go red on empty-file/:memory: checkpoint loss. :memory: is grep-forbidden; file-backed sqlite required. Asserting on state == completed is explicitly insufficient (non-discriminating). |
| T-07-05-02 | Tampering (cross-task checkpoint resume) | thread_id key | mitigate | Tests key on the BARE production task.task_id sourced from orchestrator._graph_config(task) (tenant-scoped at load time). The tenant-t:: invention is removed (grep == 0) so the test cannot drift from the production key contract. |
| T-07-05-03 | Tampering (production contract drift) | src/agent_mesh/worker/orchestrator.py | mitigate | The plan is test-side + seam-driven only. git diff --stat src/ must show NO change; _graph_config MUST be unchanged. Acceptance criteria assert no production diff. |
| T-07-05-04 | Repudiation / leaked state | set_checkpointer_override teardown | mitigate | try/finally clears the override (set_checkpointer_override(None)) and closes the sqlite connection so no saver leaks across tests; the Postgres lane uses close_checkpointer() in finally. |
| T-07-05-05 | Information disclosure | TEST_DATABASE_URL | accept | DSN sourced from env only in the opt-in Postgres lane and never logged (existing T-07-06 posture); default lane has no DB. Low risk for a local/CI test DSN. |
| T-07-05-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed by this plan; all deps (langgraph sqlite backend, mcp extra) are already pinned in the agents/runtime extras and present in the validated .venv. No package legitimacy gate required. |
</threat_model>

<verification>
Run after all three tasks:

1. Default lane stays green and E2E-02 now drives production wiring:
   PYTHONPATH=src .venv/bin/python -m pytest -q -m "not live" — full default lane PASSES;
   tests/e2e/test_e2e_mcp_durable_job.py shows "1 passed" (not skipped) in the .venv.
2. Lint clean:
   PYTHONPATH=src .venv/bin/python -m ruff check tests/e2e/test_e2e_mcp_durable_job.py tests/e2e/test_e2e_mcp_durable_job_live.py exits 0.
3. Invention removed, production path + discriminating read present:
   grep -c "tenant-t::" tests/e2e/test_e2e_mcp_durable_job.py == 0;
   grep -c "set_checkpointer_override" tests/e2e/test_e2e_mcp_durable_job.py >= 2;
   grep -c "get_tuple" tests/e2e/test_e2e_mcp_durable_job.py >= 1;
   grep -c ":memory:" tests/e2e/test_e2e_mcp_durable_job.py == 0 and a .sqlite file path present
   (do NOT write the literal `:memory:` token in the test file — phrase the prohibition as "never an in-memory connection target").
4. Live companion fixes:
   grep -c "call_tool" tests/e2e/test_e2e_mcp_durable_job_live.py >= 1;
   grep -c "saver2 is not saver" tests/e2e/test_e2e_mcp_durable_job_live.py >= 1.
5. Deploy-readiness hook:
   grep -c "test-pg" Makefile >= 1; grep -c "TEST_DATABASE_URL" RUNBOOK.md >= 1;
   make test-pg loud-skips (exit 0) without a DSN.
6. Production untouched:
   git diff --stat src/agent_mesh/worker/orchestrator.py shows NO change;
   git diff --stat src/ shows NO change.
</verification>

<success_criteria>
- E2E-02 / SC-2 default-lane test drives the production Worker/orchestrator restart-resume wiring
  (Worker.process -> run_mesh -> _run_langgraph -> _select_checkpointer -> _graph_config) via the
  existing set_checkpointer_override seam on a file-backed sqlite store, keyed on the BARE
  production task.task_id.
- The artifact-survival proof is DISCRIMINATING (reopened-checkpoint get_tuple read of the review
  channel; goes red on checkpoint loss), not a state == completed tautology.
- The tenant-t:: invention and the false DUR-02 docstring are removed from BOTH E2E-02 modules.
- WR-01: the MCP-transport test drives the registered FastMCP create_task tool and proves it
  funnels into the shared TaskService.
- WR-02: the Postgres lane has the saver2 is not saver negative assertion and a runnable
  make test-pg + RUNBOOK hook so it executes on deploy-readiness validation; loud-skip absent.
- Production _graph_config (and all of src/) is unchanged.
- Full default lane (pytest -m "not live") stays green; both edited modules pass ruff.
</success_criteria>

<output>
Create .planning/phases/07-e2e-validation-deploy-readiness/07-05-SUMMARY.md when done.
</output>
