---
phase: 07-e2e-validation-deploy-readiness
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/e2e/test_e2e_failure_modes.py
  - tests/e2e/test_e2e_failure_modes_live.py
autonomous: true
requirements: [E2E-03]
must_haves:
  truths:
    - "D-05: E2E-03 is a SINGLE combined run driving, in order, model fallback -> (in-cascade) retry-recovery -> budget-limit halt -> FAILED terminal, composing existing Phase-3 seams with NO new fault-injection harness"
    - "D-06: an opt-in `live` variant of E2E-03 exercises a real model fallback + real budget-halt, bounded by D-08"
    - "D-08: the live E2E-03 variant sets a few-cents budget routed to the cheapest model tier so the real budget-halt fires for pennies"
    - "RESOLVED (open decision 1) — the deterministic 'job retry' leg is read as litellm IN-CASCADE recovery folded into the fallback seam (option b); the literal Pub/Sub job-redelivery notion is asserted as deploy-config consistency in DEP-01 (plan 07-04), NOT via a new fault harness (honors D-05)"
    - "A failure E2E demonstrates model fallback, job retry, and budget-limit halt together (ROADMAP SC-3) — fallback+retry as the mid-run recovery, then the governed halt"
  artifacts:
    - path: "tests/e2e/test_e2e_failure_modes.py"
      provides: "E2E-03 default-lane combined-run proof: fallback+retry-recovery then budget_halt -> FAILED"
      contains: "budget_halt"
    - path: "tests/e2e/test_e2e_failure_modes_live.py"
      provides: "E2E-03 opt-in live variant: real fallback + cents-cap real budget-halt"
      contains: "pytestmark"
  key_links:
    - from: "tests/e2e/test_e2e_failure_modes.py"
      to: "litellm Router fallback cascade"
      via: "build_router() + mock_testing_fallbacks=True (the in-cascade retry-recovery leg)"
      pattern: "mock_testing_fallbacks"
    - from: "tests/e2e/test_e2e_failure_modes.py"
      to: "agent_mesh.worker.graph budget halt"
      via: "over-cap BudgetEvent -> Worker.process -> FAILED + budget_halt gateway_event"
      pattern: "model_route\\s*==\\s*[\"']budget_halt"
---

<objective>
Prove E2E-03 end-to-end: ONE combined run that drives, in order, model fallback ->
in-cascade retry-recovery -> budget-limit halt -> governed FAILED terminal (D-05),
composing the existing Phase-3 seams (GW-03 cascade, budget cents-cap) with NO new
fault-injection harness. Plus an opt-in `live` variant (D-06) that fires a REAL
budget-halt for pennies (D-08).

RESOLVES open design decision 1 (the retry leg has no deterministic analog): the
deterministic "job retry" element is read as litellm IN-CASCADE recovery (the cascade IS
the mid-run retry-then-recover), folded into the fallback seam — this needs no new harness
and honors D-05's "no new fault-injection harness." The literal Pub/Sub job-redelivery
notion (`--max-delivery-attempts=5`) is asserted as deploy-config consistency in DEP-01
(plan 07-04). Both readings together cover the named three-part criterion; neither is
silently dropped.

Purpose: Proves the platform fails SAFE and OBSERVABLE — fallback recovers mid-run, then a
budget breach halts to a governed FAILED terminal with an auditable budget_halt event.
Output: E2E-03 default-lane combined proof + its live cents-cap variant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-e2e-validation-deploy-readiness/07-CONTEXT.md
@.planning/phases/07-e2e-validation-deploy-readiness/07-PATTERNS.md

<interfaces>
<!-- Extracted from analogs; the executor uses these directly. -->

OPEN-DECISION RESOLUTION (retry leg): the three-part SC-3 ("fallback, job retry, budget-halt
together") is covered as:
  - fallback + retry-recovery = ONE seam: the litellm cascade. mock_testing_fallbacks=True forces the
    primary to raise InternalServerError -> the cascade recovers on the fallback deployment. That
    recovery IS the mid-run retry. (option b — no new harness; honors D-05.)
  - literal job-redelivery = asserted in DEP-01 (plan 07-04) as `--max-delivery-attempts=5` +
    `--dead-letter-topic` config consistency in gcp_bootstrap.sh:74-79. (option a, deploy-config only.)
DO NOT build an emulator-gated Pub/Sub redelivery test (that would breach D-05's "no new fault harness").

FALLBACK seam (analog tests/test_cascade.py:51-80, creds-free):
  - build the real Router from yaml via `build_router()`; skip cleanly if the litellm runtime extra is
    absent (test_cascade.py:42-48).
  - resp = router.completion(model="low-complexity", messages=[{"role":"user","content":"hi"}],
      mock_testing_fallbacks=True,            # primary raises -> cascade
      mock_response="[fallback served]")      # fallback serves deterministically
  - assert resp.model == "gemini-1.5-pro"     # the FALLBACK deployment served, not the primary
    (i.e. recovery happened — this is the retry-recovery assertion).

BUDGET-HALT seam (analog tests/test_budget_halt_governed.py, creds-free):
  - the `governed_env` fixture pattern (lines 44-71): InMemoryRepository pinned as the process
    singleton (`monkeypatch.setattr(repo_module, "_SINGLETON", repo)`) + TENANT_ID/CLIENT_SLUG pinned
    so task tenant, breach-ledger row, budget owner, and gateway_event scope are ALL the same tenant
    ("t"). CRITICAL: a split-brain (fixture repo vs singleton) green-on-a-lie risk.
  - _force_over_budget: repo.record_budget_event(BudgetEvent(... budget_owner="t",
      estimated_cost_usd=100.0))  # past the $50 cap (lines 73-90)
  - monkeypatch.setattr(graph_module, "_model_credentials_present", lambda: True)  # force real _delegate
  - state = Worker(repo=repo).process(task.task_id)
  - assert state == TaskState.FAILED.value
  - halt = [e for e in repo.list_gateway_events(task.task_id, "t") if e.model_route == "budget_halt"]
  - assert len(halt) == 1 and halt[0].provider_status is None   # exactly one halt event, no provider leak
  - leak guard (lines 164-187): make get_chat_model `_boom` to PROVE the halt short-circuits before any provider.

ORDERING (forced by seam semantics): fallback+retry are mid-run recovery and must SUCCEED first; the
budget then exhausts and the run halts to FAILED. PATTERNS notes the seams live in separate tests —
E2E-03 composes them into one combined run (staged asserts within one test are acceptable; the
contract is "exercised together in order").

LIVE variant gating (PATTERNS SP-2 row 3): `pytestmark = pytest.mark.live` + the `live_creds` fixture
(conftest.py:123-135 — ANTHROPIC_API_KEY / VERTEX_PROJECT_ID / CF_AIG_WRAPPER_URL). Set a few-cents
budget routed to the cheapest tier (D-08).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2E-03 default-lane combined run — fallback + retry-recovery then governed budget_halt</name>
  <files>tests/e2e/test_e2e_failure_modes.py</files>
  <read_first>
    - tests/test_cascade.py (the fallback seam lines 42-48 extra-skip + 51-80 build_router + mock_testing_fallbacks + the `resp.model == "gemini-1.5-pro"` recovery assertion)
    - tests/test_budget_halt_governed.py (the `governed_env` fixture lines 44-71, `_force_over_budget` lines 73-90, the FAILED + single budget_halt + no-provider-leak assertions lines 96-115 & 147-187, the `_boom` leak guard lines 164-187)
    - tests/conftest.py (the `repo` fixture; note the budget test pins its OWN singleton repo — mirror that, do NOT use the bare `repo` fixture for the halt leg)
    - src/agent_mesh/worker/graph.py (_delegate, _model_credentials_present)
    - src/agent_mesh/worker/runner.py (Worker.process FAILED terminal path)
    - config/model_gateway.config.yaml (the low->medium->high cascade map build_router() reads)
  </read_first>
  <action>
    Create `tests/e2e/test_e2e_failure_modes.py` with ONE combined-run test (staged
    asserts) that drives the three SC-3 elements IN ORDER, creds-free. STAGE A
    (fallback + retry-recovery): build the real Router via `build_router()` (skip cleanly
    if the litellm extra is absent, copy test_cascade.py:42-48), call
    `router.completion(model="low-complexity", messages=[...], mock_testing_fallbacks=True,
    mock_response="[fallback served]")`, and assert the FALLBACK deployment served
    (`resp.model == "gemini-1.5-pro"`) — this is the mid-run retry-recovery. STAGE B
    (governed halt): replicate the `governed_env` setup from test_budget_halt_governed.py
    (pin a fresh InMemoryRepository as `repo_module._SINGLETON`, set TENANT_ID/CLIENT_SLUG
    to "t"/"c"), create a QUEUED task, `_force_over_budget` (record an over-cap BudgetEvent
    with budget_owner="t", estimated_cost_usd=100.0), monkeypatch
    `graph_module._model_credentials_present` -> True, monkeypatch `model_gateway.get_chat_model`
    to a `_boom` that raises if reached (leak guard), then `Worker(repo=repo).process(task_id)`
    and assert `state == TaskState.FAILED.value`, exactly ONE budget_halt gateway_event
    (`model_route == "budget_halt"`, provider_status is None), and `leaked["called"] is False`.
    Add an explicit comment block documenting the retry-leg resolution (option b in-cascade
    recovery here; literal Pub/Sub redelivery asserted in DEP-01 / plan 07-04). NO `live`
    marker on this module.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/e2e/test_e2e_failure_modes.py -m "not live" -q</automated>
  </verify>
  <acceptance_criteria>
    - With the litellm extra installed: `PYTHONPATH=src python -m pytest tests/e2e/test_e2e_failure_modes.py -m "not live" -q` exits 0 with the test PASSED; without it, the test SKIPS loudly (reason names the missing litellm extra), never fails.
    - The test asserts the fallback deployment served (`resp.model == "gemini-1.5-pro"`) via `mock_testing_fallbacks=True` — grep confirms `mock_testing_fallbacks` present.
    - The test asserts `TaskState.FAILED` AND exactly one `model_route == "budget_halt"` gateway_event with `provider_status is None` — grep confirms `budget_halt` present.
    - The test includes the no-provider-leak guard (a `_boom`/get_chat_model patch asserting it is never reached) — grep confirms the leak-guard assertion.
    - The halt leg pins its OWN singleton repo (`repo_module._SINGLETON`) and tenant — grep confirms `_SINGLETON` present (no split-brain).
    - The module documents the retry-leg resolution (in-cascade recovery here; Pub/Sub redelivery -> DEP-01) — grep confirms a comment referencing DEP-01 / 07-04.
    - `PYTHONPATH=src python -m ruff check tests/e2e/test_e2e_failure_modes.py` exits 0; `grep -E "^pytestmark *= *pytest\.mark\.live" tests/e2e/test_e2e_failure_modes.py` returns 0 (statement-anchored, comment-immune).
  </acceptance_criteria>
  <done>E2E-03 default-lane combined proof passes (or skips loudly): one run shows fallback+retry-recovery succeeding, then a governed budget halt to FAILED with exactly one observable budget_halt event and no provider leak; the retry-leg resolution is documented.</done>
</task>

<task type="auto">
  <name>Task 2: E2E-03 opt-in live variant — real fallback + cents-cap real budget-halt (D-06/D-08)</name>
  <files>tests/e2e/test_e2e_failure_modes_live.py</files>
  <read_first>
    - tests/test_cascade_live.py (the live cascade shape, if present) and tests/test_cascade.py (the deterministic shape to mirror live)
    - tests/conftest.py (the `live_creds` fixture lines 123-135 — the model/gateway creds axis)
    - tests/e2e/test_e2e_failure_modes.py (the default-lane combined run from Task 1 — the live variant mirrors its stages with real seams)
    - .planning/phases/07-e2e-validation-deploy-readiness/07-CONTEXT.md (D-06 real fallback+halt; D-08 few-cents budget + cheapest tier)
  </read_first>
  <action>
    Create `tests/e2e/test_e2e_failure_modes_live.py`. Set `pytestmark =
    pytest.mark.live` (whole-module). Depend on the `live_creds` fixture (SP-2 row 3 —
    model/gateway creds: ANTHROPIC_API_KEY / VERTEX_PROJECT_ID / CF_AIG_WRAPPER_URL). The
    test exercises a REAL model fallback and a REAL budget-halt, bounded by D-08: set a
    FEW-CENTS budget cap and route to the CHEAPEST model tier so the real halt fires for
    pennies (configure the cents-cap via the same budget-ledger / settings mechanism the
    default-lane halt uses, but with a real provider call this time and a tiny cap). Assert
    the real fallback recovered, then the real budget breach produced a FAILED terminal +
    a budget_halt gateway_event. Keep total live spend bounded to pennies; do NOT burn
    toward the $50 cap.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/e2e/test_e2e_failure_modes_live.py -m live -q</automated>
  </verify>
  <acceptance_criteria>
    - With no model/gateway creds exported: `PYTHONPATH=src python -m pytest tests/e2e/test_e2e_failure_modes_live.py -m live -q` exits 0 with the test SKIPPED loudly (via `live_creds`), never failed.
    - The module is whole-module `live`-marked (grep confirms `pytestmark = pytest.mark.live`) and depends on `live_creds` (grep confirms `live_creds`), NOT on a provider-specific token.
    - The default suite is unaffected: `PYTHONPATH=src python -m pytest tests/e2e/ -m "not live" -q` still green.
    - The test sets a few-cents budget cap and routes to the cheapest tier (grep confirms a small/cents cap value and a low-complexity/cheapest route reference) — D-08.
    - `PYTHONPATH=src python -m ruff check tests/e2e/test_e2e_failure_modes_live.py` exits 0.
  </acceptance_criteria>
  <done>The live E2E-03 variant is `live`+`live_creds`-gated and (when creds present) fires a real fallback + a real cents-cap budget-halt for pennies; the default suite stays green and creds-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> litellm Router (mock hooks) | In-process; `mock_testing_fallbacks` / `mock_response` keep the default lane network-free and creds-free. |
| live variant -> real model provider | Crossed ONLY under `-m live` with `live_creds`; spend bounded to a few cents (D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Denial of Service | live variant burning toward the full $50 budget / runaway spend | mitigate | D-08: few-cents cap + cheapest tier; the real halt fires for pennies; bound into acceptance criteria. |
| T-07-08 | Information Disclosure | provider leak past the budget halt (cost/audit gap) | mitigate | `_boom` leak guard asserts get_chat_model is never reached past the halt; budget_halt event has provider_status is None. |
| T-07-09 | Repudiation | split-brain repo (fixture vs singleton) passing the halt assertion green-on-a-lie | mitigate | Pin one InMemoryRepository as `_SINGLETON` + same tenant for task/ledger/event scope (governed_env pattern); bound into acceptance criteria. |
| T-07-SC | Tampering | npm/pip installs | n/a | No new packages installed (reuses existing extras). |

No HIGH-severity threats: test-only plan composing already-hardened Phase-3 seams. The elevated concerns (runaway live spend, provider leak) are directly mitigated by the cents-cap and the existing leak guard.
</threat_model>

<verification>
- Default lane: `PYTHONPATH=src python -m pytest tests/e2e/test_e2e_failure_modes.py -m "not live" -q` green (or loud skip without the litellm extra).
- Live lane no creds: `PYTHONPATH=src python -m pytest tests/e2e/test_e2e_failure_modes_live.py -m live -q` shows the live variant SKIPPED loudly.
- `make test` (`-m "not live"`) stays green with both modules present.
</verification>

<success_criteria>
- E2E-03 (ROADMAP SC-3) proven: fallback, retry-recovery, and budget-limit halt exercised together in one run, halting to a governed observable FAILED terminal.
- D-05, D-06, D-08 each observably satisfied; the retry-leg open decision is resolved (in-cascade recovery here; Pub/Sub redelivery in DEP-01) with no new fault harness.
- Default suite green and creds-free; live variant `live_creds`-gated and cents-bounded.
</success_criteria>

<output>
Create `.planning/phases/07-e2e-validation-deploy-readiness/07-03-SUMMARY.md` when done.
</output>
