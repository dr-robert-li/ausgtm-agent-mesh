---
phase: 07-e2e-validation-deploy-readiness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/deploy/__init__.py
  - tests/deploy/bin/gcloud
  - tests/deploy/bin/wrangler
  - tests/deploy/test_gcloud_idempotency.py
  - tests/deploy/test_manifest_consistency.py
autonomous: true
requirements: [DEP-01, DEP-02]
must_haves:
  truths:
    - "D-09: gcloud/wrangler idempotency LOGIC is proven via a fake `gcloud`/`wrangler` shim on PATH that records describe->create branch decisions; a pytest harness drives the real scripts and asserts reuse-vs-create (resource detection short-circuits creation when a resource 'exists')"
    - "D-10: shellcheck (lint) and `wrangler --dry-run` run when the tool is installed, else the check SKIPs with a LOUD logged notice (never a silent gap); a `bash -n` syntax floor runs always-on"
    - "D-11 RESOLVED: a NEW dedicated validator/test asserts deployment-manifest + tool-pack-manifest schema-consistency (env-var contract vs scripts; manifest fields vs declared schemas; required-stack assertions) — NOT an extension of export_schemas, which only does Pydantic->JSON-Schema and never reads the YAML manifests"
    - "The literal Pub/Sub 'job retry' leg of E2E-03 is asserted HERE as deploy-config consistency: gcp_bootstrap.sh declares `--dead-letter-topic` + `--max-delivery-attempts=5` on the worker subscription (completes ROADMAP SC-3's job-retry element without a fault harness; cross-ref plan 07-03)"
    - "The gcloud/wrangler scripts pass syntax checks and their resource-detection branches are unit-tested with a mocked gcloud; the deployment + tool-pack manifests are schema-consistent (ROADMAP SC-4)"
  artifacts:
    - path: "tests/deploy/test_gcloud_idempotency.py"
      provides: "DEP-01: PATH-shim subprocess harness proving reuse-vs-create logic + bash -n floor + loud-skip shellcheck"
      contains: "subprocess"
    - path: "tests/deploy/bin/gcloud"
      provides: "catch-all fake gcloud shim recording every argv, returning exists/absent per fixture state"
    - path: "tests/deploy/test_manifest_consistency.py"
      provides: "DEP-02/D-11: new validator asserting manifest<->script<->schema consistency + required-stack"
      contains: "deployment.manifest.yaml"
  key_links:
    - from: "tests/deploy/test_gcloud_idempotency.py"
      to: "scripts/gcp_bootstrap.sh"
      via: "subprocess.run with PATH=shim_dir prepended; assert describe->|| create short-circuit"
      pattern: "subprocess\\.run"
    - from: "tests/deploy/test_manifest_consistency.py"
      to: "manifests/deployment.manifest.yaml + scripts/*.sh + schemas/"
      via: "env-var contract + schema-coverage + required-stack cross-checks"
      pattern: "deployment\\.manifest\\.yaml"
---

<objective>
Prove DEP-01 + DEP-02 locally (deploy-ready ceiling, NO live provisioning): the
`gcloud`/`wrangler` scripts pass syntax checks and their resource-detection (reuse-vs-create)
LOGIC is proven via a PATH-shimmed fake binary (D-09); shellcheck + `wrangler --dry-run`
run when installed else SKIP loudly (D-10) with an always-on `bash -n` floor; and a NEW
dedicated validator asserts the deployment + tool-pack manifests are schema-consistent with
the scripts and schemas (D-11). Also assert the Pub/Sub `--max-delivery-attempts=5` /
`--dead-letter-topic` config as the literal job-retry evidence for E2E-03 (cross-ref 07-03).

Purpose: Confirms the platform is genuinely deploy-ready — the idempotency the scripts claim
by-design is now PROVEN by test, and the manifest/script/schema contract cannot silently drift.
Output: `tests/deploy/` harness — fake shims + idempotency-logic test + manifest-consistency validator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-e2e-validation-deploy-readiness/07-CONTEXT.md
@.planning/phases/07-e2e-validation-deploy-readiness/07-PATTERNS.md
@RUNBOOK.md

<interfaces>
<!-- Extracted from drive-surfaces; the executor uses these directly. NO existing analog for the shim
     harness or the manifest validator — build new (PATTERNS "No Analog Found"). -->

D-09 SHIM CONTRACT: a fake `gcloud` (and `wrangler`) executable on PATH that (1) records EVERY argv
invocation to a file, and (2) exits 0 ("exists") or non-0 ("absent") per a test-controlled fixture
state. CRITICAL (correctness): gcloud's presence on this machine is NOT confirmed — the shim MUST be
a CATCH-ALL: record-and-exit for ANY argv (`projects describe/create`, `services enable`, `artifacts`,
`pubsub`, `iam`, `sql`, `config set`, `billing`, ...). An unhandled subcommand falling through to a
real gcloud would corrupt the test. Drive the script with
`subprocess.run([...], env={"PATH": shim_dir + os.pathsep + os.environ["PATH"], "PROJECT_ID": "...", ...})`
so the shim shadows any real binary ONLY inside the subprocess env, never the session PATH.

gcp_bootstrap.sh describe-before-create guards to exercise (drive-surface, read-only):
  - project: lines 25-40 (`gcloud projects describe` -> create only if CREATE_PROJECT=true)
  - Artifact Registry: lines 58-62 (`repositories describe ... || create`)
  - Pub/Sub topics + subs + DLQ: lines 64-98 (`--dead-letter-topic`, `--max-delivery-attempts=5` — the
    E2E-03 job-retry deploy-config evidence)
  - service accounts: lines 100-104; Cloud SQL instance + db: lines 130-145
  - required env contract: line 14 (`: "${PROJECT_ID:?...}"`) + REGION/CREATE_PROJECT/SQL_* defaults 15-22
gcp_deploy_core.sh: deploy_job create-vs-update branch lines 72-90 (`run jobs describe ... && update` else create); env contract 9-19.
cf_deploy_ai_gateway_worker.sh: env contract lines 12-15; reuse-gateway branch + `wrangler deploy` lines 22-30.

D-10 LINT/DRY-RUN (loud skip — PATTERNS SP-5; shellcheck + wrangler CONFIRMED ABSENT here):
  - `shellcheck scripts/*.sh` -> run if on PATH else `pytest.skip("shellcheck not installed; gcloud-script lint skipped")`.
  - `wrangler --dry-run` -> run if on PATH else loud skip.
  - ALWAYS-ON FLOOR: `bash -n scripts/*.sh` (syntax check) runs wherever bash exists — this is the
    real DEP-01 "syntax check" that must NOT be skip-gated, so the default lane proves something real.

D-11 MANIFEST-CONSISTENCY (NEW validator — export_schemas does NOT fit: it only does
Pydantic CONTRACT_MODELS -> schemas/contracts/*.schema.json, never reads YAML manifests). Three cross-checks:
  1. Env-var contract: script `${VAR:?}` requirements (gcp_bootstrap.sh:14, gcp_deploy_core.sh:9-19,
     cf_deploy_ai_gateway_worker.sh:12-15: PROJECT_ID, REGION, TENANT_ID, CLIENT_SLUG, MODEL_ROUTE_PROFILE,
     AR_REPO, SQL_INSTANCE, CLOUDFLARE_ACCOUNT_ID, CLOUDFLARE_AI_GATEWAY_ID, WRANGLER_ENV,
     REUSE_EXISTING_CLOUDFLARE_GATEWAY, CREATE_PROJECT/BILLING_ACCOUNT_ID) must each be supplied by
     deployment.manifest.yaml fields (project_id/region/tenant_id/client_slug :8-12;
     model_routes.default_profile :65; gcp.artifact_registry_repo :20; gcp.cloud_sql.instance_name :22;
     cloudflare.account_id/ai_gateway_id/wrangler_env/reuse_existing_gateway :41-44;
     deployment.create_project_if_missing/billing_account_id :11-12). Assert no orphan on either side.
  2. Tool-pack pointer + schema coverage: deployment.manifest.yaml:114-115
     tool_packs.manifest_path "manifests/tool_pack_manifest.yaml" must RESOLVE; each tool in
     tool_pack_manifest.yaml `tools:` should have matching schemas/<tool>.input/output.schema.json
     (file-relative resolution, PATTERNS SP-4); integration_styles (:118) must be a known set.
  3. Required-stack assertions (CLAUDE.md §6 AC-1/AC-2): deployment.manifest.yaml:57-62
     agent_framework "langchain+langgraph+deepagents", observability "langfuse",
     langsmith "optional-alternative-only" (LangSmith never required).

Path resolution (SP-4): resolve manifest + schema dirs from `__file__` parents, never cwd.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/deploy package + catch-all fake gcloud/wrangler shims + idempotency-logic harness (DEP-01)</name>
  <files>tests/deploy/__init__.py, tests/deploy/bin/gcloud, tests/deploy/bin/wrangler, tests/deploy/test_gcloud_idempotency.py</files>
  <read_first>
    - scripts/gcp_bootstrap.sh (the describe-before-create guards: project 25-40, Artifact Registry 58-62, Pub/Sub+DLQ 64-98 incl. --max-delivery-attempts=5, service accounts 100-104, Cloud SQL 130-145, env contract 14-22)
    - scripts/gcp_deploy_core.sh (deploy_job create-vs-update branch lines 72-90; env contract 9-19)
    - tests/conftest.py (the loud skip-if-absent shape — `pg_dsn` lines 74-87 — to mirror for shellcheck/wrangler skips)
    - .planning/phases/07-e2e-validation-deploy-readiness/07-PATTERNS.md (SP-5 loud-skip; the "No Analog Found" note — shim+subprocess harness is genuinely new)
  </read_first>
  <action>
    Create `tests/deploy/__init__.py` (empty). Create executable CATCH-ALL fake binaries
    `tests/deploy/bin/gcloud` and `tests/deploy/bin/wrangler` (bash; `chmod +x`): each
    records its full argv (append to a file named by an env var the test sets, e.g.
    `$SHIM_LOG`) and exits 0 or non-0 per a test-controlled state file/env var so the
    fixture can toggle "resource exists" vs "absent". They MUST handle ANY argv
    (record-and-exit), never let an unhandled subcommand fall through. Create
    `tests/deploy/test_gcloud_idempotency.py` that, per script, runs
    `subprocess.run([...path to script...], env={PATH: shim_dir + os.pathsep + real PATH,
    plus all required `${VAR:?}` env vars})` and asserts: (EXISTS state) when describe
    returns 0, the corresponding `create` is NEVER recorded in the shim log (the
    `describe ... || create` short-circuits); (ABSENT state) when describe returns non-0,
    `create` IS recorded. Cover at minimum the Artifact Registry, Pub/Sub
    topic/subscription, service-account, and Cloud SQL branches of gcp_bootstrap.sh, and
    the deploy_job create-vs-update branch of gcp_deploy_core.sh. ALSO add: an always-on
    `bash -n scripts/*.sh` syntax-floor assertion (runs wherever bash exists, NOT
    skip-gated); a `shellcheck scripts/*.sh` leg that runs if shellcheck is on PATH else
    SKIPS LOUDLY (named reason); and an assertion that gcp_bootstrap.sh declares
    `--dead-letter-topic` and `--max-delivery-attempts=5` on the worker subscription (the
    literal Pub/Sub job-retry evidence for E2E-03 — add a comment cross-referencing plan
    07-03). NO `live` marker.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/deploy/test_gcloud_idempotency.py -m "not live" -q</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=src python -m pytest tests/deploy/test_gcloud_idempotency.py -m "not live" -q` exits 0; the bash -n floor + the reuse-vs-create logic tests PASS (not skipped); shellcheck/wrangler legs SKIP loudly when absent.
    - The shims are executable and catch-all: `test -x tests/deploy/bin/gcloud && test -x tests/deploy/bin/wrangler` succeeds; the shim source records argv for any subcommand (grep confirms an append-to-log on the default/catch-all path).
    - The harness drives the real scripts via `subprocess.run` with a prepended-PATH shim dir scoped to the subprocess env (grep confirms `subprocess.run` and a `PATH` env key); it never mutates the session PATH.
    - The test asserts BOTH reuse (create NOT invoked when resource exists) AND create (invoked when absent) for >=2 distinct resource branches — grep confirms both assertion directions.
    - The test asserts gcp_bootstrap.sh contains `--max-delivery-attempts=5` and `--dead-letter-topic` (E2E-03 job-retry evidence) with a comment referencing plan 07-03.
    - An always-on `bash -n` syntax check over `scripts/*.sh` is present and NOT skip-gated — grep confirms `bash` + `-n`.
    - `PYTHONPATH=src python -m ruff check tests/deploy/test_gcloud_idempotency.py` exits 0.
  </acceptance_criteria>
  <done>DEP-01 proven locally: the scripts pass the bash -n syntax floor; a PATH-shim subprocess harness proves the reuse-vs-create idempotency logic for multiple resource branches; shellcheck/wrangler legs skip loudly when absent; the Pub/Sub redelivery config (E2E-03 job-retry) is asserted.</done>
</task>

<task type="auto">
  <name>Task 2: manifest-consistency validator (DEP-02 / D-11) + wrangler dry-run loud-skip leg</name>
  <files>tests/deploy/test_manifest_consistency.py</files>
  <read_first>
    - manifests/deployment.manifest.yaml (env-supplying fields: deployment 5-12, gcp 19-22, cloudflare 40-44, stack 57-62, model_routes 64-67, tool_packs 114-118)
    - manifests/tool_pack_manifest.yaml (the `tool_pack:` header + `tools:` list — schema coverage source)
    - scripts/gcp_bootstrap.sh, scripts/gcp_deploy_core.sh, scripts/cf_deploy_ai_gateway_worker.sh (the `${VAR:?}` env-var requirements to reconcile against the manifest)
    - src/agent_mesh/contracts/export_schemas.py (lines 13,17-31 — to CONFIRM it only does Pydantic->JSON-Schema and never reads YAML manifests, justifying the NEW validator per D-11)
    - .planning/phases/07-e2e-validation-deploy-readiness/07-PATTERNS.md (SP-4 file-relative path resolution; the three cross-checks; the "No Analog Found" justification for a new validator)
  </read_first>
  <action>
    Create `tests/deploy/test_manifest_consistency.py` as a NEW dedicated validator/test
    (resolving D-11 in favor of a new validator — document in a module docstring WHY:
    export_schemas only emits Pydantic contract schemas and never touches the YAML
    manifests). Resolve `manifests/deployment.manifest.yaml`,
    `manifests/tool_pack_manifest.yaml`, `schemas/`, and `scripts/*.sh` from `__file__`
    parents (SP-4, cwd-independent). Implement the three cross-checks: (1) ENV-VAR
    CONTRACT — parse the `${VAR:?...}` required vars from the three scripts and assert each
    maps to a supplying field in deployment.manifest.yaml (PROJECT_ID->project_id,
    REGION->region, TENANT_ID->tenant_id, CLIENT_SLUG->client_slug,
    MODEL_ROUTE_PROFILE->model_routes.default_profile, AR_REPO->gcp.artifact_registry_repo,
    SQL_INSTANCE->gcp.cloud_sql.instance_name, CLOUDFLARE_ACCOUNT_ID->cloudflare.account_id,
    CLOUDFLARE_AI_GATEWAY_ID->cloudflare.ai_gateway_id, WRANGLER_ENV->cloudflare.wrangler_env,
    REUSE_EXISTING_CLOUDFLARE_GATEWAY->cloudflare.reuse_existing_gateway,
    CREATE_PROJECT->deployment.create_project_if_missing,
    BILLING_ACCOUNT_ID->deployment.billing_account_id) with NO orphan on either side; (2)
    TOOL-PACK POINTER + SCHEMA COVERAGE — assert tool_packs.manifest_path resolves, each
    tool in tool_pack_manifest.yaml `tools:` has matching schemas/<tool>.input/output
    schema files, and integration_styles is a subset of the known set
    {direct_api, mcp_server, aggregate_mcp, nango_aggregator, composio_aggregator}; (3)
    REQUIRED-STACK — assert stack.agent_framework == "langchain+langgraph+deepagents",
    stack.observability == "langfuse", stack.langsmith == "optional-alternative-only"
    (CLAUDE.md §6 AC-1/AC-2; LangSmith never required). ALSO add a `wrangler --dry-run`
    leg that runs if wrangler is on PATH else SKIPS LOUDLY (D-10). NO `live` marker.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/deploy/test_manifest_consistency.py -m "not live" -q</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=src python -m pytest tests/deploy/test_manifest_consistency.py -m "not live" -q` exits 0; the three cross-check tests PASS; the wrangler --dry-run leg SKIPS loudly when wrangler is absent.
    - The validator is a NEW module that reads the YAML manifests (grep confirms `deployment.manifest.yaml` and `tool_pack_manifest.yaml`); it does NOT extend export_schemas (no import of export machinery for the consistency logic) and documents the D-11 decision in its docstring.
    - The env-var cross-check asserts no orphan in BOTH directions (every required script var has a manifest field; flagged if a manifest-declared deploy var is missing) — grep confirms a bidirectional/orphan assertion.
    - The schema-coverage check resolves tool_packs.manifest_path and matches each tool to its input/output schemas via file-relative paths (grep confirms `__file__` / `parents` resolution).
    - The required-stack check asserts the three stack fields (grep confirms `langchain+langgraph+deepagents`, `langfuse`, `optional-alternative-only`).
    - The wrangler dry-run leg uses the loud-skip shape (named reason) — grep confirms a `wrangler` skip path.
    - `PYTHONPATH=src python -m ruff check tests/deploy/test_manifest_consistency.py` exits 0.
  </acceptance_criteria>
  <done>DEP-02/D-11 proven: a new dedicated validator asserts the deployment + tool-pack manifests are schema-consistent with the scripts and schemas (env-var contract, schema coverage, required-stack), and the wrangler dry-run runs when installed else skips loudly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> deploy scripts via subprocess | The scripts run under a shimmed PATH; NO real gcloud/wrangler/cloud API is ever reached. |
| fake shim on PATH | Shadows real `gcloud`/`wrangler` ONLY inside `subprocess.run(env=...)`; never alters the session/parent PATH. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering / Elevation | shim fails to cover an argv -> unhandled subcommand reaches a REAL gcloud -> real cloud call | mitigate (HIGH-watch) | Shim is CATCH-ALL: record-and-exit for ANY argv; PATH override scoped to the subprocess env only; bound into acceptance criteria (catch-all path + scoped PATH). EXPECTED severity LOW once the catch-all is enforced. |
| T-07-11 | Spoofing | a real gcloud/wrangler on PATH shadowing the shim (wrong precedence) | mitigate | shim_dir is PREPENDED to PATH so it wins; verified by asserting the shim log captured the invocations. |
| T-07-12 | Repudiation | silent gap — shellcheck/wrangler quietly not run, masking a lint failure | mitigate | D-10 loud skip (named reason) + always-on `bash -n` floor so the default lane proves real syntax; "no silent caps". |
| T-07-SC | Tampering | npm/pip installs | n/a | No new packages installed; shims are local bash files, not dependencies. |

HIGH called out explicitly: T-07-10 (unhandled-argv leaking to a real binary) is the only candidate for HIGH and is reduced to LOW by the catch-all shim + subprocess-scoped PATH override (both bound into acceptance criteria). No residual HIGH-severity threats.
</threat_model>

<verification>
- `PYTHONPATH=src python -m pytest tests/deploy/ -m "not live" -q` green: bash -n floor + reuse-vs-create logic + manifest-consistency cross-checks PASS; shellcheck/wrangler legs skip loudly.
- `make test` (`-m "not live"`) stays green with the new package included.
- No real cloud call is made (the shim log is the only record of gcloud/wrangler invocations).
</verification>

<success_criteria>
- DEP-01 (ROADMAP SC-4 first half): scripts pass syntax checks; resource-detection branches unit-tested with a mocked gcloud proving idempotency logic locally.
- DEP-02 (ROADMAP SC-4 second half): wrangler dry-run runs when installed else loud-skip; deployment + tool-pack manifests are schema-consistent.
- D-09, D-10, D-11 each observably satisfied; the literal Pub/Sub job-retry leg of E2E-03 is asserted as deploy-config consistency (cross-ref 07-03).
</success_criteria>

<output>
Create `.planning/phases/07-e2e-validation-deploy-readiness/07-04-SUMMARY.md` when done.
</output>
