---
phase: 05-reference-adapter-breadth
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/agent_mesh/tools/adapters/clockify.py
  - tests/test_clockify_adapter.py
  - tests/test_clockify_live.py
  - docs/credentials/clockify.md
autonomous: true
user_setup:
  - service: clockify
    why: "Clockify direct adapter live lane — read time entries (read-only)"
    env_vars:
      - name: CLOCKIFY_API_KEY
        source: "Clockify -> Profile Settings -> API -> Generate. Subdomain workspaces need a subdomain-specific key. Header X-Api-Key."
requirements: [TOOL-03]

must_haves:
  truths:
    - "clockify_read_time_entries (read) executes a real Clockify API v1 call through the gateway with CLOCKIFY_API_KEY resolved at execution time (D-02), authenticated via the X-Api-Key header (NOT Bearer)"
    - "the read endpoint needs BOTH workspaceId AND userId; the adapter resolves them from spec.resource_bindings (workspace_id + user_id) — GET /workspaces/{workspaceId}/user/{userId}/time-entries"
    - "ONE register('clockify', dispatcher) routing by spec.name via _CLOCKIFY_OPS (single op now, but the dispatcher shape is preserved for future ops) — NOT a bare register-per-op pattern"
    - "credential absent -> the adapter returns None so the engine degrades to the stub; make test stays green and creds-free (D-11); stub fallback driven by credential is None (httpx core, no ImportError branch)"
  artifacts:
    - path: "src/agent_mesh/tools/adapters/clockify.py"
      provides: "Clockify direct adapter: clockify dispatcher routing read_time_entries by spec.name over httpx with X-Api-Key"
      contains: "X-Api-Key"
    - path: "docs/credentials/clockify.md"
      provides: "Clockify API key mint + X-Api-Key header + workspace_id/user_id bindings + subdomain caveat + CLOCKIFY_API_KEY"
      contains: "CLOCKIFY_API_KEY"
  key_links:
    - from: "tools/adapters/clockify.py"
      to: "tools/adapters registry"
      via: "ONE register('clockify', dispatcher); execute() dispatches get_adapter(spec.provider)='clockify'"
      pattern: "register"
    - from: "tools/adapters/clockify.py"
      to: "Clockify API v1 (api.clockify.me/api/v1)"
      via: "httpx X-Api-Key call; creds-absent -> stub"
      pattern: "httpx"
---

<objective>
Ship the Clockify direct adapter (TOOL-03) — `clockify_read_time_entries` (read-only). The
adapter calls Clockify API v1 over core httpx (no SDK, no new dependency) and degrades to the
gateway stub when `CLOCKIFY_API_KEY` is absent (D-11). Clockify stays read-only this phase
(05-01 froze: no clockify write op).

Critical: the read endpoint needs BOTH workspaceId AND userId —
`GET /workspaces/{workspaceId}/user/{userId}/time-entries` — resolved from
`spec.resource_bindings` (`workspace_id` + `user_id`); auth is the `X-Api-Key` header (NOT
Bearer).

This plan touches ONLY `tools/adapters/clockify.py` + its two tests + its doc. It does NOT edit
the manifest, schemas, pyproject, the registry, gateway.py, or the README / credential-docs
guard (05-01 / 05-07) — so it runs parallel with the other four wave-2 adapter plans.

Op-name contract (05-01): register EXACTLY ONE dispatcher under `clockify` routing by `spec.name`
via `_CLOCKIFY_OPS = {"clockify_read_time_entries": _read}`. Even with a single op, keep the
dispatcher+map shape (consistency with the other adapters; future-proofs a second op without a
register-per-op collision). The key MUST match the manifest op name 05-01 froze.

Purpose: prove the direct_api style on a read-only provider with a two-segment path key
(workspace + user).
Output: registered Clockify adapter (one dispatcher), default-lane unit test (fake httpx +
path/header assertion), opt-in live test, credential doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reference-adapter-breadth/RESEARCH.md
@.planning/phases/05-reference-adapter-breadth/05-01-PLAN.md

<interfaces>
<!-- adapter call contract (04-03): adapter(spec, params, *, credential) -> dict|None ; register(key, fn) -->
<!-- ADAPTER KEY = spec.provider ("clockify"). ONE dispatcher routing by spec.name via _CLOCKIFY_OPS. -->
<!-- httpx is CORE — stub fallback = `if credential is None: return None`; no ImportError branch. -->
<!-- Clockify API v1 base: https://api.clockify.me/api/v1 . Auth header: {"X-Api-Key": credential} (NOT Bearer). -->
<!-- READ read_time_entries: GET /workspaces/{workspaceId}/user/{userId}/time-entries -> array of {id, description, timeInterval:{start,end,duration}, projectId, ...} -->
<!-- workspace_id + user_id from spec.resource_bindings. The endpoint REQUIRES both segments. -->
<!-- output mapping: -> schemas/clockify_read_time_entries.output.schema.json — an OBJECT {entries:[...]} (NOT a bare list; build the entries key). -->
<!-- token via `credential` arg — do NOT read os.getenv. -->
<!-- live-lane: pytestmark live + inline skip on CLOCKIFY_API_KEY absence. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clockify dispatcher (read_time_entries) over httpx X-Api-Key + default & live tests + doc</name>
  <files>src/agent_mesh/tools/adapters/clockify.py, tests/test_clockify_adapter.py, tests/test_clockify_live.py, docs/credentials/clockify.md</files>
  <read_first>
    - src/agent_mesh/tools/adapters/hubspot.py (single-dispatcher template: _OPS map, ONE register, creds-absent->None)
    - src/agent_mesh/tools/adapters/__init__.py (register/get_adapter/adapter_key_for; one-dispatcher-per-provider invariant)
    - src/agent_mesh/tools/adapters/nango.py (the httpx call idiom; NB Clockify must NOT read os.getenv — token via `credential`)
    - tests/test_hubspot_adapter.py (default-lane structure: _isolate_registry, output-schema conformance)
    - tests/test_hubspot_live.py (live-lane structure: pytestmark live, inline token skip, gateway.execute, stub assertion)
    - schemas/clockify_read_time_entries.output.schema.json (05-01 — the exact output shape to map to; match whatever wrap the schema declares)
    - RESEARCH.md "Clockify" section (endpoint needs workspaceId+userId, X-Api-Key header, subdomain key caveat, mint steps)
  </read_first>
  <action>
    Create `tools/adapters/clockify.py`. One op fn `_read_time_entries(spec, params, *, credential) -> dict|None` returning `None` when `credential is None`. Resolve `workspace_id = spec.resource_bindings["workspace_id"]` and `user_id = spec.resource_bindings["user_id"]`; `httpx.get(f"https://api.clockify.me/api/v1/workspaces/{workspace_id}/user/{user_id}/time-entries", headers={"X-Api-Key": credential}, params=...)`; raise_for_status; map the raw JSON array into the OBJECT shape `{"entries": [...]}` declared by `schemas/clockify_read_time_entries.output.schema.json` (the schema is an object wrapping the array under `entries` — never return a bare list; the live-lane stub assertion does `result.get("stub")` which raises on a list). Build `_CLOCKIFY_OPS = {"clockify_read_time_entries": _read_time_entries}` and a single `clockify_adapter(spec, params, *, credential)` doing `return _CLOCKIFY_OPS[spec.name](spec, params, credential=credential)`. Register EXACTLY ONCE: `register("clockify", clockify_adapter)`. Do NOT read os.getenv.
    `tests/test_clockify_adapter.py` (default lane): `_isolate_registry`; creds-absent -> op returns None; a fake-httpx mapping test asserting (a) the result conforms to the output schema and (b) the request URL contains BOTH the workspace_id and user_id segments and the `X-Api-Key` header (not Bearer) — capture from the fake transport; a `get_adapter("clockify")` reachability test. (Single op -> a routing-distinctness guard is not required, but assert the dispatcher rejects/handles an unknown spec.name gracefully if you add a second op later — optional.) NO ImportError-branch test.
    `tests/test_clockify_live.py`: `pytestmark = pytest.mark.live`; inline skip when `CLOCKIFY_API_KEY` unset; one real `clockify_read_time_entries` through `gateway.execute(...)`; assert `result.get("stub") is not True` + output-schema conformance.
    `docs/credentials/clockify.md`: mint the API key (Clockify -> Profile Settings -> API -> Generate); the `X-Api-Key` header (not Bearer); the `workspace_id` + `user_id` resource bindings (and that `GET /user` returns the caller's id if you need to resolve it); the subdomain-specific-key caveat; env var `CLOCKIFY_API_KEY`.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest -q -m "not live" tests/test_clockify_adapter.py -x && PYTHONPATH=src python -m pytest -q -m live tests/test_clockify_live.py --co -q && PYTHONPATH=src python -m pytest -q -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - EXACTLY ONE register() under "clockify": `grep -v '^#' src/agent_mesh/tools/adapters/clockify.py | grep -c 'register(' ` == 1 and `grep -c 'register("clockify"' src/agent_mesh/tools/adapters/clockify.py` == 1
    - `_CLOCKIFY_OPS` names the op: `grep -c "clockify_read_time_entries" src/agent_mesh/tools/adapters/clockify.py` >= 1
    - X-Api-Key auth (not Bearer): `grep -c "X-Api-Key" src/agent_mesh/tools/adapters/clockify.py` >= 1 and the adapter sends NO Bearer auth header: `grep -cE '"Authorization".*Bearer|Bearer \{' src/agent_mesh/tools/adapters/clockify.py` == 0 (matches the auth-usage form, not a rationale comment)
    - The path uses both segments: `grep -c "workspace_id" src/agent_mesh/tools/adapters/clockify.py` >= 1 and `grep -c "user_id" src/agent_mesh/tools/adapters/clockify.py` >= 1
    - No os.getenv READ in the adapter (token via `credential`): `grep -cE 'os\.(getenv|environ)\(|os\.environ\[' src/agent_mesh/tools/adapters/clockify.py` == 0 (matches the call form, not a rationale comment)
    - A default-lane test asserts the request URL contains both workspace_id and user_id segments and the X-Api-Key header
    - `PYTHONPATH=src python -m pytest -q -m live tests/test_clockify_live.py` SKIPS when CLOCKIFY_API_KEY is unset
    - `grep -c "CLOCKIFY_API_KEY" docs/credentials/clockify.md` >= 1
    - `PYTHONPATH=src python -m pytest -q -m "not live"` exits 0 (default suite green)
  </acceptance_criteria>
  <done>The Clockify adapter registers ONE clockify dispatcher routing read_time_entries by spec.name over httpx X-Api-Key, builds the two-segment workspace/user path, degrades to the stub without the key, ships default + live tests and a credential doc — default suite green and creds-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> Clockify API v1 | X-Api-Key + agent params cross to a real time-tracking surface |
| adapter -> key | CLOCKIFY_API_KEY resolved by the gateway, passed in; must not be logged/leaked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Information disclosure | the API key leaking into logs/results | mitigate | key resolved only inside execute() (04-03), passed as `credential`, never returned; the adapter reads no os.getenv |
| T-05-05-02 | Information disclosure | reading another user's/workspace's time entries via spoofed ids | accept | workspace_id/user_id come from the deployment's resource_bindings (operator-controlled), not free agent input; read-only, low value |

No high-severity threat open: read-only adapter, ids operator-bound, the key never leaks.
</threat_model>

<verification>
- `make test` exits 0 (adapter unit test green; module imports with no SDK)
- A default-lane test proves the two-segment path + X-Api-Key header
- `pytest -m live tests/test_clockify_live.py` skips cleanly without the key
- The Clockify credential/scope doc exists with the X-Api-Key header + subdomain caveat
</verification>

<success_criteria>
Clockify read_time_entries runs through the gateway via ONE clockify dispatcher with
execution-time key resolution and the correct two-segment workspace/user path; the adapter
degrades to the stub without the key; the credential doc is shipped — default suite green and
creds-free.
</success_criteria>

<output>
Create `.planning/phases/05-reference-adapter-breadth/05-05-SUMMARY.md` when done
</output>
