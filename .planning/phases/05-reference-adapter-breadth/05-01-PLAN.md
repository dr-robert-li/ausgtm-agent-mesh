---
phase: 05-reference-adapter-breadth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifests/tool_pack_manifest.yaml
  - schemas/webflow_list_cms_items.input.schema.json
  - schemas/webflow_list_cms_items.output.schema.json
  - schemas/webflow_create_cms_item.input.schema.json
  - schemas/webflow_create_cms_item.output.schema.json
  - schemas/bitscale_list_grids.input.schema.json
  - schemas/bitscale_list_grids.output.schema.json
  - schemas/bitscale_get_workspace.input.schema.json
  - schemas/bitscale_get_workspace.output.schema.json
  - schemas/bitscale_run_grid.input.schema.json
  - schemas/bitscale_run_grid.output.schema.json
  - schemas/calcom_list_bookings.input.schema.json
  - schemas/calcom_list_bookings.output.schema.json
  - schemas/calcom_create_booking.input.schema.json
  - schemas/calcom_create_booking.output.schema.json
  - schemas/clockify_read_time_entries.input.schema.json
  - schemas/clockify_read_time_entries.output.schema.json
  - schemas/beehiiv_create_post.input.schema.json
  - schemas/beehiiv_create_post.output.schema.json
autonomous: true
requirements: [TOOL-03]

must_haves:
  truths:
    - "Every Phase-5 direct manifest entry (webflow, bitscale, calcom, clockify, beehiiv) carries BOTH input_schema_ref + output_schema_ref so D-04 fail-closed never blocks a declared direct tool for lack of a schema, and a creds-absent tool degrades cleanly to the stub instead of input_rejected"
    - "Manifest op `name` fields EXACTLY match the `_<PROVIDER>_OPS` keys each wave-2 adapter will route by spec.name — bitscale's stale `bitscale_enrich` entry is reconciled to the real live-verified ops (bitscale_list_grids, bitscale_get_workspace reads + bitscale_run_grid write); webflow gains a read op (webflow_list_cms_items)"
    - "Xero's two entries are flipped from nango_aggregator to composio_aggregator with tool_slug/user_id resource_bindings and credential_secret_name COMPOSIO_API_KEY, so Xero rides the EXISTING composio.py adapter with zero adapter code (no new Xero module, no nango.py edit)"
    - "Output schemas match the REAL provider response shapes (Beehiiv {data:{id}}, Cal.com {status,data,pagination}, Webflow create 202 {id,fieldData,...}, Bitscale {grids:[...]}) so live calls do not output_quarantine"
    - "make test stays green and creds-free; NO new pyproject.toml extra is added (all five direct providers use core httpx; Xero rides the existing composio extra); the credential-docs guard is NOT touched"
  artifacts:
    - path: "manifests/tool_pack_manifest.yaml"
      provides: "schema_refs on every Phase-5 direct entry + reconciled bitscale ops + webflow read op + Xero composio_aggregator flip"
      contains: "bitscale_list_grids"
    - path: "schemas/bitscale_list_grids.output.schema.json"
      provides: "output schema matching the live-verified {\"grids\":[...]} response shape"
      contains: "json-schema.org/draft/2020-12"
    - path: "schemas/beehiiv_create_post.output.schema.json"
      provides: "output schema matching the real nested {data:{id}} response (NOT flat {id})"
      contains: "json-schema.org/draft/2020-12"
  key_links:
    - from: "manifests/tool_pack_manifest.yaml direct entries"
      to: "schemas/<provider>_<op>.{input,output}.schema.json"
      via: "input_schema_ref / output_schema_ref point at on-disk files (D-04 reachability)"
      pattern: "input_schema_ref"
    - from: "manifests/tool_pack_manifest.yaml op name fields"
      to: "wave-2 adapter _<PROVIDER>_OPS dispatch keys (05-02..05-06)"
      via: "spec.name dispatch contract — names must match exactly or the op is silently unreachable"
      pattern: "bitscale_run_grid"
    - from: "manifests/tool_pack_manifest.yaml xero_* entries"
      to: "src/agent_mesh/tools/adapters/composio.py (existing)"
      via: "integration_style composio_aggregator -> adapter_key_for -> 'composio' (reuse, no new module)"
      pattern: "composio_aggregator"
---

<objective>
Foundation plan: own ALL shared-file edits for Phase 5 so the wave-2 per-adapter plans
are genuinely isolated (each touches only its own `adapters/<provider>.py` + tests + doc).
Three concerns, all on shared surfaces, deliberately grouped here to prevent parallel
collisions (the Phase-4 04-01 pattern):

1. **Schema refs + schema files for every Phase-5 direct op.** The manifest already
   declares the Phase-5 providers but carries NO `input_schema_ref`/`output_schema_ref` and
   NO schema files exist. Under D-04 fail-closed, `validate_tool_input` BLOCKS a `direct_api`
   tool with no schema (`input_rejected`) at execute time — so each direct tool is unreachable
   until its refs + files land. This plan authors every referenced schema file (Draft 2020-12)
   and wires both refs onto every Phase-5 direct entry.

2. **Reconcile the bitscale entry + add the webflow read op (op-name contract).** Dispatch
   routes by `spec.name`, so the manifest `name` must equal the adapter's `_<PROVIDER>_OPS`
   key. Today's `bitscale_enrich` entry maps to NONE of the live-verified endpoints; it is
   replaced by the real ops. This plan FREEZES the final op-name set per provider (below) so
   each wave-2 adapter's `_OPS` keys match exactly.

3. **Flip Xero to Composio (shared-manifest edit).** Change both `xero_*` entries from
   `nango_aggregator` to `composio_aggregator`, add `resource_bindings.tool_slug` + `user_id`,
   set `credential_secret_name: COMPOSIO_API_KEY`. Xero then rides the EXISTING `composio.py`
   adapter (verb-agnostic `session.execute`) — no new Xero module, no `nango.py` edit. Aggregate
   tools get NO schema files (D-04 permissive/runtime branch).

DOES NOT touch `tests/test_credential_docs.py` (the guard) or `docs/credentials/README.md` —
those have NO degradation and are owned by the FINAL plan 05-07 (see the boxed warning).
Adds NO new `pyproject.toml` extra (all five direct providers use core httpx; Xero rides the
existing `composio` extra).

FROZEN OP-NAME SET (each wave-2 adapter's `_<PROVIDER>_OPS` keys MUST match these exactly,
and the live-test filenames 05-07 enumerates MUST match the `tests/test_<provider>_live.py`
names):

| Provider | Ops (manifest `name` == `_OPS` key) | Live test file | New env var |
|----------|--------------------------------------|----------------|-------------|
| webflow  | `webflow_list_cms_items` (read), `webflow_create_cms_item` (publishing write) | `tests/test_webflow_live.py` | `WEBFLOW_API_TOKEN` |
| bitscale | `bitscale_list_grids` (read), `bitscale_get_workspace` (read), `bitscale_run_grid` (write) | `tests/test_bitscale_live.py` (reads only) | `BITSCALE_API_KEY` |
| calcom   | `calcom_list_bookings` (read), `calcom_create_booking` (write) | `tests/test_calcom_live.py` | `CALCOM_API_KEY` |
| clockify | `clockify_read_time_entries` (read) | `tests/test_clockify_live.py` | `CLOCKIFY_API_KEY` |
| beehiiv  | `beehiiv_create_post` (publishing write) | `tests/test_beehiiv_live.py` | `BEEHIIV_API_KEY` |
| xero     | `xero_read_invoices` (read), `xero_create_invoice` (financial write) — via Composio | `tests/test_xero_live.py` (05-07) | reuses `COMPOSIO_API_KEY` |

Optional-op decisions (FROZEN — do not add others): Webflow read = YES. Clockify write = NO
(read-only stays). Beehiiv read = NO (create-only stays). This keeps the surface minimal and
every op reachable.

Purpose: give wave-2 stable, schema-backed, op-name-exact manifest surfaces so each adapter
plan is a pure own-file add.
Output: schema-backed manifest entries for all six providers, every Draft-2020-12 schema file,
the Xero composio flip — default suite green and creds-free, no new dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reference-adapter-breadth/RESEARCH.md
@.planning/phases/04-tool-gateway-framework-first-adapters-aggregators/04-01-PLAN.md

<interfaces>
<!-- Template schema files to copy exactly (Draft 2020-12 + x-tool-category/x-approval-required): -->
<!-- schemas/hubspot_lookup_company.input.schema.json (read input: $schema, x-tool-category, required, enum, additionalProperties:false) -->
<!-- schemas/hubspot_lookup_company.output.schema.json (output: additionalProperties:false top-level, required, nested additionalProperties:true) -->
<!-- D-05 permissive-by-design: inputs allow additionalProperties EXCEPT tighten (false + enums/bounds) on cost/security-critical write fields. Reads may set additionalProperties:false. -->

<!-- Real response shapes the OUTPUT schemas MUST match (else output_quarantine — RESEARCH Pitfall 2): -->
<!-- webflow_list_cms_items: { "items":[{id, isDraft, fieldData{...}, lastPublished}], "pagination":{...} } -->
<!-- webflow_create_cms_item: HTTP 202 { id, lastPublished, lastUpdated, createdOn, fieldData{...} } -->
<!-- bitscale_list_grids: { "grids":[{id, name, description, row_count, column_count, columns:[{id,name,type,dependencies}]}] }  [live-curl verified] -->
<!-- bitscale_get_workspace: workspace details {plan, credits, member counts}  [live-curl verified] -->
<!-- bitscale_run_grid: write; route inferred POST /api/v1/grids/{grid_id}/run (RESEARCH A1) — keep output shape minimal/permissive (run id / status); never probed -->
<!-- calcom_list_bookings: { "status":"success", "data":[{id,uid,title,status,start,end,attendees[...],eventTypeId}], "pagination":{nextCursor,hasMore} } -->
<!-- calcom_create_booking: { "status", "data":{id,uid,...} } -->
<!-- clockify_read_time_entries: array of {id, description, timeInterval:{start,end,duration}, projectId, ...} -->
<!-- beehiiv_create_post: HTTP 201 { "data":{ "id": <prefixed-post-id> } }  (NESTED under data, NOT flat {id}) -->

<!-- gateway loader sanity (04-01 pattern): load_tool_pack(manifest) must not raise the write-class approval invariant -->
<!-- ToolSpec.validate enforces approval_required:true for write/external_send/financial/publishing/admin categories at load -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manifest reconcile — bitscale real ops, webflow read op, Xero->Composio flip, schema_refs on every direct entry</name>
  <files>manifests/tool_pack_manifest.yaml</files>
  <read_first>
    - manifests/tool_pack_manifest.yaml (full file): the existing Phase-5 entries (webflow_create_cms_item:204-215, bitscale_enrich:218-228, calcom_list_bookings/create_booking:231-251, clockify_read_time_entries:254-264, beehiiv_create_post:299-309) and the xero_* entries (178-201) to flip; the GWS/HubSpot entries are the schema_ref + approval pattern to copy
    - RESEARCH.md "Per-Provider API Reference" (every op's category, approval, endpoint, resource_bindings) + "Xero" section (composio flip: tool_slug XERO_LIST_INVOICES / XERO_CREATE_INVOICE, user_id, COMPOSIO_API_KEY)
    - RESEARCH.md "Bitscale" section (replace bitscale_enrich; bindings: grid_id for grid-detail/run_grid; reads are credit-free)
  </read_first>
  <action>
    Edit `manifests/tool_pack_manifest.yaml` ONLY (no schema files in this task). (1) WEBFLOW: add both `input_schema_ref`/`output_schema_ref` to the existing `webflow_create_cms_item` entry (refs `schemas/webflow_create_cms_item.{input,output}.schema.json`), and ADD a new READ entry `webflow_list_cms_items` (provider webflow, direct_api, category read, approval_required false, credential_secret_name WEBFLOW_API_TOKEN, resource_bindings site_id + collection_id, both schema refs). (2) BITSCALE: REPLACE the stale single `bitscale_enrich` entry with THREE entries matching the frozen op names — `bitscale_list_grids` (read, approval false), `bitscale_get_workspace` (read, approval false), `bitscale_run_grid` (write, approval_required true); all provider bitscale, direct_api, credential_secret_name BITSCALE_API_KEY; resource_bindings.grid_id (REPLACE_WITH_BITSCALE_GRID_ID) on list_grids+run_grid; both schema refs on each. (3) CALCOM: add both schema refs to `calcom_list_bookings` and `calcom_create_booking`. (4) CLOCKIFY: add both schema refs to `clockify_read_time_entries`; add `user_id` to its resource_bindings (REPLACE_WITH_CLOCKIFY_USER_ID — the read needs both workspaceId+userId). (5) BEEHIIV: add both schema refs to `beehiiv_create_post`. (6) XERO FLIP: change `xero_read_invoices` and `xero_create_invoice` from `integration_style: nango_aggregator` to `composio_aggregator`; set `credential_secret_name: COMPOSIO_API_KEY`; add `resource_bindings.tool_slug` (XERO_LIST_INVOICES / XERO_CREATE_INVOICE — confirm exact slug at impl) and `resource_bindings.user_id` (mesh-tenant-example); KEEP NO `input_schema_ref`/`output_schema_ref` on the xero entries (aggregate = runtime/permissive, D-04); preserve `approval_required: true` on `xero_create_invoice` (financial). Every write-class entry (webflow create=publishing, bitscale run_grid=write, calcom create=write, beehiiv=publishing, xero create=financial) MUST keep `approval_required: true` or load raises. (Task ordering note: this manifest task may run before Task 2 writes the schema files — `load_tool_pack` stores schema refs as strings and reads the files LAZILY only at execute() time, NOT at load, so a ref pointing at a not-yet-written file does not break `load_tool_pack` or the default suite; the files land in Task 2 of this same plan.)
  </action>
  <verify>
    <automated>PYTHONPATH=src python -c "from agent_mesh.tools.gateway import load_tool_pack; s={x.name:x for x in load_tool_pack('manifests/tool_pack_manifest.yaml')}; assert {'bitscale_list_grids','bitscale_get_workspace','bitscale_run_grid','webflow_list_cms_items'} <= set(s), sorted(s); assert 'bitscale_enrich' not in s; assert s['xero_read_invoices'].integration_style=='composio_aggregator' and s['xero_create_invoice'].integration_style=='composio_aggregator'; assert s['xero_create_invoice'].credential_secret_name=='COMPOSIO_API_KEY'; print('ok', len(s))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bitscale_list_grids\|bitscale_get_workspace\|bitscale_run_grid" manifests/tool_pack_manifest.yaml` >= 3 and `grep -c "bitscale_enrich" manifests/tool_pack_manifest.yaml` == 0
    - `grep -c "webflow_list_cms_items" manifests/tool_pack_manifest.yaml` == 1
    - `grep -c "composio_aggregator" manifests/tool_pack_manifest.yaml` >= 4 (2 existing composio reads + 2 flipped xero) and `grep -c "nango_aggregator" manifests/tool_pack_manifest.yaml` == 1 (only nango_hubspot_list_contacts remains)
    - Every Phase-5 direct entry (webflow ×2, bitscale ×3, calcom ×2, clockify ×1, beehiiv ×1) has BOTH input_schema_ref and output_schema_ref; the xero entries have NEITHER
    - `PYTHONPATH=src python -m pytest -q -m "not live"` exits 0 (manifest loads; write-class approval invariant holds; default suite green)
  </acceptance_criteria>
  <done>The manifest declares every frozen Phase-5 op with both schema refs (direct) or composio bindings (xero), bitscale_enrich is reconciled to the three real ops, webflow gains its read op, Xero is flipped to composio_aggregator, and the manifest loads without raising.</done>
</task>

<task type="auto">
  <name>Task 2: Author every Phase-5 direct schema file (Draft 2020-12, output shapes matched to real responses)</name>
  <files>schemas/webflow_list_cms_items.input.schema.json, schemas/webflow_list_cms_items.output.schema.json, schemas/webflow_create_cms_item.input.schema.json, schemas/webflow_create_cms_item.output.schema.json, schemas/bitscale_list_grids.input.schema.json, schemas/bitscale_list_grids.output.schema.json, schemas/bitscale_get_workspace.input.schema.json, schemas/bitscale_get_workspace.output.schema.json, schemas/bitscale_run_grid.input.schema.json, schemas/bitscale_run_grid.output.schema.json, schemas/calcom_list_bookings.input.schema.json, schemas/calcom_list_bookings.output.schema.json, schemas/calcom_create_booking.input.schema.json, schemas/calcom_create_booking.output.schema.json, schemas/clockify_read_time_entries.input.schema.json, schemas/clockify_read_time_entries.output.schema.json, schemas/beehiiv_create_post.input.schema.json, schemas/beehiiv_create_post.output.schema.json</files>
  <read_first>
    - schemas/hubspot_lookup_company.input.schema.json + .output.schema.json (the EXACT template: $schema Draft 2020-12, title, x-tool-category, x-approval-required, required, enum/bounds, additionalProperties policy)
    - schemas/google_calendar_create_event.input.schema.json + .output.schema.json (a write-op template with tightened critical fields)
    - RESEARCH.md "Per-Provider API Reference" — the documented real response shapes for each op (copy them into the OUTPUT schemas; see the <interfaces> block above)
  </read_first>
  <action>
    Author all 18 schema files copying the `hubspot_lookup_company` template (Draft 2020-12 `$schema`, `title`, `x-tool-category`, `x-approval-required`). INPUTS (D-05 permissive-by-design): set `required` for the fields the adapter truly needs; allow `additionalProperties` by default; set `additionalProperties:false` + enums/bounds ONLY on cost/security-critical write fields. OUTPUTS: shape EACH to the REAL provider response so live calls do not quarantine —
    • `webflow_list_cms_items.output`: top-level `{items:[{id, ...}], pagination:{...}}`, items `additionalProperties:true` required `[id]`.
    • `webflow_create_cms_item.output`: `{id, fieldData, ...}` (202 body) — required `[id]`, `additionalProperties:true`. Input: required `fieldData` (object), default `isDraft:true` documented; keep `additionalProperties:true`.
    • `bitscale_list_grids.output`: `{grids:[{id,name,description,row_count,column_count,columns:[{id,name,type}]}]}` matching the live-verified shape; required `[grids]`. Input: no required fields (lists all), `additionalProperties:true`.
    • `bitscale_get_workspace.output`: `{...}` permissive object (plan/credits/members), `additionalProperties:true`, no strict required. Input: empty/permissive.
    • `bitscale_run_grid.input`: required `[grid_id]` plus `inputs` (object); WRITE — tighten grid_id to string. Output: minimal/permissive `{...}` (run id/status) `additionalProperties:true` (route inferred; never probed).
    • `calcom_list_bookings.output`: `{status, data:[{id,uid,title,status,start,end,...}], pagination:{...}}` required `[status,data]`. Input permissive.
    • `calcom_create_booking.input`: required `[start]` (+ attendee object); WRITE. Output: `{status, data:{id,uid,...}}` required `[status,data]`.
    • `clockify_read_time_entries.output`: MUST be an OBJECT wrapping the array under `entries` (`{entries:[{id,description,timeInterval:{start,end,duration},projectId},...]}`, required `[entries]`). Do NOT use a top-level `type:array` — adapters return dicts and the live-lane stub assertion does `result.get("stub")`, which raises on a bare list; an object output keeps every lane safe. The 05-05 adapter maps the raw array into `{"entries": [...]}`. Input permissive.
    • `beehiiv_create_post.input`: required `[title]`; document `status:"draft"` default (never auto-publish); WRITE. Output: `{data:{id}}` NESTED — required `[data]`, data required `[id]` (NOT a flat {id}).
    Every file's first non-empty content line references `https://json-schema.org/draft/2020-12/schema`. Do NOT write any adapter code.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -c "
import json,glob
need=['webflow_list_cms_items','webflow_create_cms_item','bitscale_list_grids','bitscale_get_workspace','bitscale_run_grid','calcom_list_bookings','calcom_create_booking','clockify_read_time_entries','beehiiv_create_post']
from jsonschema import Draft202012Validator
for n in need:
  for k in ('input','output'):
    s=json.load(open(f'schemas/{n}.{k}.schema.json')); Draft202012Validator.check_schema(s); assert s['\$schema'].endswith('2020-12/schema'), n
# beehiiv output must be nested under data
bo=json.load(open('schemas/beehiiv_create_post.output.schema.json')); assert 'data' in bo.get('properties',{}), 'beehiiv output must nest data'
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All 18 schema files exist and each validates as a legal Draft 2020-12 schema (`Draft202012Validator.check_schema`)
    - `grep -l "draft/2020-12/schema" schemas/{webflow,bitscale,calcom,clockify,beehiiv}_*.schema.json | wc -l` == 18
    - `beehiiv_create_post.output.schema.json` nests `id` under a `data` object (NOT a flat top-level `id`)
    - `bitscale_list_grids.output.schema.json` has a top-level `grids` array (matches the live-verified shape)
    - `PYTHONPATH=src python -m pytest -q -m "not live"` exits 0 (every direct tool now loads with a resolvable schema; default suite green — adapters absent still stub via import-miss)
  </acceptance_criteria>
  <done>Every Phase-5 direct op has on-disk input+output schema files in Draft 2020-12 form, output schemas mirror the real provider response shapes (no quarantine risk), and the default suite is green with adapters still absent (import-miss -> stub).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest -> gateway loader | tool declarations (categories, approval flags, schema refs, op names) become runtime authority |
| manifest op name -> wave-2 adapter _OPS key | a name mismatch silently makes an op unreachable (no load-time error) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | a write-class entry (bitscale run_grid / webflow create / beehiiv / calcom create / xero create) with approval_required:false | mitigate | ToolSpec.validate raises at load if a write-class entry omits approval_required:true; Task 1 acceptance + the load assertion catch it |
| T-05-01-02 | Spoofing | a manifest op name diverging from the adapter _OPS key -> silently unreachable op | mitigate | this plan FREEZES the op-name set (objective table); 05-07 verifies _LIVE_TEST_FILES matches; wave-2 plans cite the frozen names as read_first |
| T-05-01-03 | Tampering | Xero financial write routed through nango.py would require editing the shared aggregator | mitigate | flip to composio_aggregator (verb-agnostic session.execute); nango.py untouched; xero create stays approval_required:true |
| T-05-01-SC | Tampering | pip installs (supply chain) | mitigate | this plan installs NOTHING and adds NO new pyproject extra — all five direct providers use core httpx; Xero rides the existing operator-approved composio extra; no [ASSUMED]/[SUS] package introduced |
| T-05-01-04 | Information disclosure | schema files leaking real resource ids | accept | schemas use generic shapes / REPLACE_WITH placeholders live in the manifest only; no live ids committed |

No high-severity threat open: this plan adds no execution path, handles no credential, and installs nothing.
</threat_model>

<verification>
- `make test` exits 0 (default lane green, creds-free, no new SDK installed)
- `load_tool_pack` loads the manifest without raising the write-class approval invariant
- Every Phase-5 direct entry resolves both schema refs to on-disk Draft 2020-12 files
- Xero entries are composio_aggregator with COMPOSIO_API_KEY + tool_slug/user_id and NO schema refs
- `tests/test_credential_docs.py` is UNCHANGED (guard deferred to 05-07) and still passes its OLD 4-provider lists
- No new `[project.optional-dependencies]` extra in pyproject.toml
</verification>

<success_criteria>
All shared-file Phase-5 foundation work lands here: schema refs + 18 schema files for every
direct op, bitscale reconciled to its real ops, webflow read op added, Xero flipped to
composio_aggregator — default suite green and creds-free, no new dependency, guard untouched.
</success_criteria>

<output>
Create `.planning/phases/05-reference-adapter-breadth/05-01-SUMMARY.md` when done
</output>
