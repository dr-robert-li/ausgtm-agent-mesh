---
phase: 05-reference-adapter-breadth
plan: 07
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified:
  - tests/test_xero_live.py
  - docs/credentials/xero.md
  - docs/credentials/README.md
  - tests/test_credential_docs.py
autonomous: true
user_setup:
  - service: xero
    why: "Xero via the Composio aggregator (financial) — list invoices (read) + create invoice (approval-gated DRAFT). Composio brokers the Xero OAuth; reuses the existing COMPOSIO_API_KEY (no new env var)."
    env_vars:
      - name: COMPOSIO_API_KEY
        source: "Composio dashboard -> connect the Xero toolkit (Composio handles the Xero OAuth + tenant). Already provisioned by the operator; Xero rides the SAME key."
requirements: [TOOL-03]

must_haves:
  truths:
    - "xero_read_invoices executes through the EXISTING composio.py adapter (no new Xero module) — its specs resolve via adapter_key_for to the 'composio' adapter; xero_create_invoice (financial write) stays approval-gated upstream (D-07) and is DRAFT-only (never auto-finalised)"
    - "the credential-docs guard (tests/test_credential_docs.py) is extended ATOMICALLY in the same merge as the README index + xero.md so all its assertions pass on this merge and only this merge: _MIN_ENV_VARS 7->12, _ANCHOR_ENV_VARS +5, _PROVIDER_DOCS +6 (5 direct + xero), _LIVE_TEST_FILES +6"
    - "docs/credentials/README.md links all six new per-provider docs (webflow, bitscale, calcom, clockify, beehiiv, xero) + adds skip-matrix rows + documents the five new env vars (BITSCALE_API_KEY, WEBFLOW_API_TOKEN, CALCOM_API_KEY, CLOCKIFY_API_KEY, BEEHIIV_API_KEY); Xero reuses COMPOSIO_API_KEY (no new env var)"
    - "make test stays green and creds-free; the guard's own self-test test_derived_env_var_set_is_complete_and_non_vacuous passes against the new floor/anchors"
  artifacts:
    - path: "docs/credentials/xero.md"
      provides: "Composio Xero-toolkit connection note (draft-only financial write, reuses COMPOSIO_API_KEY)"
      contains: "COMPOSIO_API_KEY"
    - path: "docs/credentials/README.md"
      provides: "the shared index: 6 new doc links + skip-matrix rows + 5 new env-var rows"
      contains: "BITSCALE_API_KEY"
    - path: "tests/test_credential_docs.py"
      provides: "extended guard enumeration (10 provider docs, 10 live-test files, 9 anchors, floor 12)"
      contains: "test_bitscale_live.py"
  key_links:
    - from: "tests/test_credential_docs.py guard lists"
      to: "the new docs/credentials/<provider>.md + tests/test_<provider>_live.py artifacts (05-02..05-06)"
      via: "guard assertions reference artifacts that now ALL exist (depends_on 05-02..06)"
      pattern: "_PROVIDER_DOCS"
    - from: "tests/test_xero_live.py + a default-lane assertion"
      to: "src/agent_mesh/tools/adapters/composio.py (existing)"
      via: "flipped xero specs resolve via adapter_key_for to the 'composio' adapter (no new module)"
      pattern: "composio"
---

<objective>
Final Phase-5 plan: land Xero-via-Composio's live/default coverage + ALL the artifact-dependent
shared-file edits that 05-01 deliberately deferred. Three concerns:

1. **Xero through the EXISTING composio adapter (no new module).** 05-01 flipped the two
   `xero_*` manifest entries to `composio_aggregator` with `tool_slug`/`user_id`/`COMPOSIO_API_KEY`.
   This plan ships `tests/test_xero_live.py` (opt-in) exercising `xero_read_invoices` through the
   gateway, and — because `test_aggregator_adapters.py` covers the composio path generically by
   registration key (NOT by the specific `composio_gmail_list_messages` tool) — a default-lane
   assertion that the flipped Xero specs resolve via `adapter_key_for` to the `composio` adapter,
   so "default coverage rides existing" is real for Xero. `xero_create_invoice` (financial write)
   stays approval-gated upstream and DRAFT-only; its mapping rides Composio's verb-agnostic
   `session.execute` (no adapter code).

2. **The shared credential index + xero.md.** Update `docs/credentials/README.md` (the single
   serialization point) to link all SIX new per-provider docs, add per-provider skip-matrix rows,
   and document the FIVE new live-lane env vars. Write `docs/credentials/xero.md` (Composio
   toolkit connection; reuses `COMPOSIO_API_KEY`).

3. **The credential-docs guard extension (the ONE shared file 05-01 must NOT touch).** Extend the
   four hardcoded lists + the floor in `tests/test_credential_docs.py` ATOMICALLY with the README +
   xero.md so every guard assertion passes on THIS merge — and only this merge (the boxed warning
   in RESEARCH). Because this plan `depends_on` ALL of 05-02..05-06, every referenced
   `test_<provider>_live.py` + `<provider>.md` already exists.

This plan is the SOLE owner of `docs/credentials/README.md` and `tests/test_credential_docs.py`.
It does NOT edit the manifest, schemas, pyproject, any adapter module, or `composio.py` (Xero
reuses it unchanged).

GUARD ARITHMETIC (verified against the current guard — must be internally consistent or the
guard's own self-test reddens):
- `_MIN_ENV_VARS`: 7 -> 12 (5 new provider keys: WEBFLOW_API_TOKEN, BITSCALE_API_KEY,
  CALCOM_API_KEY, CLOCKIFY_API_KEY, BEEHIIV_API_KEY; Xero reuses COMPOSIO_API_KEY -> adds none)
- `_ANCHOR_ENV_VARS`: 4 -> 9 (add the five new provider keys)
- `_PROVIDER_DOCS`: 4 -> 10 (add webflow.md, bitscale.md, calcom.md, clockify.md, beehiiv.md, xero.md)
- `_LIVE_TEST_FILES`: 4 -> 10 (add test_webflow_live.py, test_bitscale_live.py, test_calcom_live.py,
  test_clockify_live.py, test_beehiiv_live.py, test_xero_live.py)

Purpose: enforce success criterion 4 (docs extended) and land Xero's coverage without ever
reddening an intermediate merge.
Output: Xero live test + default-lane composio-resolution assertion, xero.md, the README index
update, and the atomically-consistent guard extension — default suite green and creds-free.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reference-adapter-breadth/RESEARCH.md
@.planning/phases/05-reference-adapter-breadth/05-01-PLAN.md

<interfaces>
<!-- Xero rides the EXISTING composio.py adapter (verb-agnostic session.execute). NO new Xero module. -->
<!-- adapter_key_for(spec): composio_aggregator -> "composio". So get_adapter for a flipped xero spec returns the composio adapter. -->
<!-- composio.py registers under "composio" (NOT "composio_aggregator") — see tests/test_aggregator_adapters.py:46-52. -->
<!-- test_aggregator_adapters.py asserts composio registers under 'composio' GENERICALLY (by key), not via composio_gmail_list_messages -> add a Xero-specific default-lane assertion here. -->
<!-- Xero tool_slugs: XERO_LIST_INVOICES / XERO_CREATE_INVOICE (confirm at impl, RESEARCH A2). DRAFT-only create (Status: DRAFT). -->
<!-- live-lane: pytestmark live + inline skip on COMPOSIO_API_KEY absence; read-only (xero_read_invoices). -->

<!-- GUARD (tests/test_credential_docs.py) current state to extend:
     _LIVE_TEST_FILES (4): test_hubspot_live.py, test_gws_live.py, test_composio_live.py, test_nango_live.py
     _PROVIDER_DOCS  (4): hubspot.md, google_workspace.md, composio.md, nango.md
     _ANCHOR_ENV_VARS (4): HUBSPOT_PRIVATE_APP_TOKEN, GOOGLE_WORKSPACE_OAUTH, COMPOSIO_API_KEY, NANGO_SECRET_KEY
     _MIN_ENV_VARS = 7
     NB: the guard DERIVES the env-var set from os.getenv literals in the live-test files + os.getenv reads
     in adapter source. Phase-5 direct adapters/live tests gate via os.getenv("<PROVIDER>_*") in the LIVE TEST
     files (e.g. test_webflow_live.py reads os.getenv("WEBFLOW_API_TOKEN")), so each new key appears in the
     derived union -> the README MUST document all five or test_every_live_lane_env_var_is_documented fails.
     Confirm each new live-test file reads its env var via an all-caps underscore literal so the scan picks it up. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xero live test (via existing composio) + default-lane composio-resolution assertion + xero.md</name>
  <files>tests/test_xero_live.py, docs/credentials/xero.md</files>
  <read_first>
    - src/agent_mesh/tools/adapters/composio.py (the existing adapter Xero rides; session.execute(tool=tool_slug, arguments=params); registers under "composio")
    - tests/test_composio_live.py (the existing composio live-lane shape to mirror for Xero — pytestmark live, COMPOSIO_API_KEY skip, gateway.execute)
    - tests/test_aggregator_adapters.py (the generic registration-key assertion; add a Xero spec-resolution assertion either here in a new default-lane test inside test_xero_live.py guarded `-m "not live"` OR as a small default-lane test — keep it in this plan's files only)
    - src/agent_mesh/tools/adapters/__init__.py (adapter_key_for: composio_aggregator -> "composio")
    - manifests/tool_pack_manifest.yaml (the flipped xero_read_invoices / xero_create_invoice entries from 05-01)
    - RESEARCH.md "Xero" section (Composio toolkit, draft-only create, tool_slug, reuses COMPOSIO_API_KEY)
  </read_first>
  <action>
    Create `tests/test_xero_live.py`. Include a DEFAULT-LANE assertion (not marked live, or a separate unmarked test fn in the same file) that loads the manifest and proves the flipped Xero specs resolve to the composio adapter: `from agent_mesh.tools.adapters import adapter_key_for; assert adapter_key_for(spec_for("xero_read_invoices")) == "composio"` and likewise for `xero_create_invoice` — closing the "default coverage rides existing" gap (test_aggregator_adapters covers composio only generically). Then the LIVE-LANE portion: `pytestmark`/marker `live`, inline skip when `COMPOSIO_API_KEY` unset (read it via `os.getenv("COMPOSIO_API_KEY")` so the guard scan still sees the anchor), one real `xero_read_invoices` through `gateway.execute(call, resolver=EnvCredentialResolver())`, assert `result.get("stub") is not True`. (xero_create_invoice is the approval-gated draft financial write — NOT exercised live; it rides Composio's verb-agnostic execute, proven structurally.) NOTE on guard interaction (structural, not discipline): the guard derives the env-var set from ALL all-caps underscore literals in the live-test files. So `test_xero_live.py` MUST contain NO all-caps underscore literal except `COMPOSIO_API_KEY` — in particular keep the Xero `tool_slug`s (`XERO_LIST_INVOICES` / `XERO_CREATE_INVOICE`) in the manifest `resource_bindings` ONLY and reference specs by `spec.name`/manifest, never as bare `"XERO_..."` literals in this test file. (If an unavoidable literal appears, add it to the guard `_NON_ENV` set in Task 2 — but the clean fix is to not write it here.) This keeps the derived floor at exactly 12.
    Write `docs/credentials/xero.md`: how to connect the Xero toolkit in the Composio dashboard (Composio brokers the Xero OAuth + tenant); that Xero reuses the SAME `COMPOSIO_API_KEY` (no new env var); the `tool_slug` (XERO_LIST_INVOICES / XERO_CREATE_INVOICE) + `user_id` resource_bindings; the DRAFT-only financial-write guarantee (Status: DRAFT, never auto-finalised, approval-gated upstream).
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest -q -m "not live" tests/test_xero_live.py -x && PYTHONPATH=src python -m pytest -q -m live tests/test_xero_live.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - A default-lane test proves `adapter_key_for` maps both flipped Xero specs to "composio" (no new Xero module)
    - The live portion reads COMPOSIO_API_KEY via os.getenv (so the guard's anchor scan still sees it) and skips when unset
    - `tests/test_xero_live.py` introduces NO new all-caps env literal beyond COMPOSIO_API_KEY (so the derived env-var floor stays at the planned 12)
    - `grep -c "COMPOSIO_API_KEY" docs/credentials/xero.md` >= 1 and `grep -ci "draft" docs/credentials/xero.md` >= 1
    - `PYTHONPATH=src python -m pytest -q -m live tests/test_xero_live.py` SKIPS when COMPOSIO_API_KEY is unset
    - No new Xero adapter module exists: `ls src/agent_mesh/tools/adapters/xero.py` returns "No such file"
  </acceptance_criteria>
  <done>Xero rides the existing composio adapter — a default-lane test proves both flipped specs resolve to "composio", the opt-in live read runs through the gateway, and xero.md documents the Composio toolkit connection + draft-only financial write; no new Xero module.</done>
</task>

<task type="auto">
  <name>Task 2: README index update + credential-docs guard extension (atomic, consistent)</name>
  <files>docs/credentials/README.md, tests/test_credential_docs.py</files>
  <read_first>
    - docs/credentials/README.md (the current index/matrix: per-provider matrix table:21-26, env-var enumeration table:33-41 — append the new rows in the SAME shapes)
    - tests/test_credential_docs.py (the four hardcoded lists + _MIN_ENV_VARS:34-53 to extend; the self-test test_derived_env_var_set_is_complete_and_non_vacuous:88-101 that must pass against the new floor/anchors)
    - the six new per-provider docs from 05-02..05-06 + Task 1 (webflow.md, bitscale.md, calcom.md, clockify.md, beehiiv.md, xero.md) — confirm each exists (they do; this plan depends_on those plans)
    - the six new live-test files (test_webflow_live.py … test_xero_live.py) — confirm each reads its env var via an all-caps literal so the derived scan matches the planned floor
  </read_first>
  <action>
    Edit `docs/credentials/README.md`: (1) add six rows to the per-provider live-lane matrix (Webflow, Bitscale, Cal.com, Clockify, Beehiiv, Xero) — each with integration style, operations, the live-lane env var(s), a link to its `<provider>.md`, and its skip behaviour (Xero: `composio_aggregator`, reuses COMPOSIO_API_KEY, draft-only financial write; Bitscale: reads-only live lane, run_grid stubbed). (2) add five rows to the "Complete live-lane env var enumeration" table (WEBFLOW_API_TOKEN, BITSCALE_API_KEY, CALCOM_API_KEY, CLOCKIFY_API_KEY, BEEHIIV_API_KEY) with provider + shape + "resolved only inside ToolGateway.execute()". (Xero reuses COMPOSIO_API_KEY — already enumerated; do NOT add a duplicate row.) Keep the "opt-in" + "skip" language the guard's `test_index_documents_the_per_provider_opt_in_skip_matrix` requires.
    Edit `tests/test_credential_docs.py` ATOMICALLY in the SAME merge: extend `_LIVE_TEST_FILES` with `test_webflow_live.py`, `test_bitscale_live.py`, `test_calcom_live.py`, `test_clockify_live.py`, `test_beehiiv_live.py`, `test_xero_live.py` (4 -> 10); extend `_PROVIDER_DOCS` with `webflow.md`, `bitscale.md`, `calcom.md`, `clockify.md`, `beehiiv.md`, `xero.md` (4 -> 10); extend `_ANCHOR_ENV_VARS` with `WEBFLOW_API_TOKEN`, `BITSCALE_API_KEY`, `CALCOM_API_KEY`, `CLOCKIFY_API_KEY`, `BEEHIIV_API_KEY` (4 -> 9); raise `_MIN_ENV_VARS` 7 -> 12. Update the source comments naming the in-scope providers to include the Phase-5 set. If the derived scan picks up an unexpected extra literal (e.g. a non-env all-caps token in a new live test), add it to `_NON_ENV` rather than lowering the floor. Verify the floor MATCHES the derived set exactly: 7 prior (HUBSPOT_PRIVATE_APP_TOKEN, GOOGLE_WORKSPACE_OAUTH, COMPOSIO_API_KEY, NANGO_SECRET_KEY, NANGO_HOST, NANGO_CONNECTION_ID, NANGO_PROVIDER_CONFIG_KEY) + 5 new = 12.
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest -q -m "not live" tests/test_credential_docs.py -x && PYTHONPATH=src python -m pytest -q -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_bitscale_live.py\|test_webflow_live.py\|test_calcom_live.py\|test_clockify_live.py\|test_beehiiv_live.py\|test_xero_live.py" tests/test_credential_docs.py` >= 6 (in _LIVE_TEST_FILES)
    - `grep -c "webflow.md\|bitscale.md\|calcom.md\|clockify.md\|beehiiv.md\|xero.md" tests/test_credential_docs.py` >= 6 (in _PROVIDER_DOCS)
    - `grep -c "_MIN_ENV_VARS = 12" tests/test_credential_docs.py` == 1
    - `grep -c "WEBFLOW_API_TOKEN\|BITSCALE_API_KEY\|CALCOM_API_KEY\|CLOCKIFY_API_KEY\|BEEHIIV_API_KEY" docs/credentials/README.md` >= 5 (env vars documented)
    - `grep -c "webflow.md\|bitscale.md\|calcom.md\|clockify.md\|beehiiv.md\|xero.md" docs/credentials/README.md` >= 6 (all six docs linked)
    - `PYTHONPATH=src python -m pytest -q -m "not live" tests/test_credential_docs.py` exits 0 — all five assertions (incl. test_derived_env_var_set_is_complete_and_non_vacuous against floor 12) pass
    - `PYTHONPATH=src python -m pytest -q -m "not live"` exits 0 (whole default suite green and creds-free)
  </acceptance_criteria>
  <done>The README index links all six new per-provider docs + documents the five new env vars + skip-matrix rows; the credential-docs guard is extended atomically (10 live tests, 10 docs, 9 anchors, floor 12) and all its assertions — including its own non-vacuity self-test — pass on this merge; success criterion 4 is now enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> Composio Tool Router -> Xero | COMPOSIO_API_KEY brokers the Xero OAuth; a financial write crosses to a real accounting surface |
| guard enumeration -> CI | the guard's hardcoded lists are the enforcement of D-12 doc completeness; under-extension silently disables criterion 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | EoP | xero_create_invoice executing without approval or finalising an invoice | mitigate | category financial -> approval_required (manifest invariant, 05-01); reached only after the upstream approval gate; DRAFT-only (Status: DRAFT, never auto-finalised); documented in xero.md; not exercised live |
| T-05-07-02 | Repudiation | success criterion 4 silently unenforced if the guard lists are not extended | mitigate | this plan extends all four lists + the floor ATOMICALLY; the guard's own non-vacuity self-test (floor 12 / 9 anchors) fails if under-extended |
| T-05-07-03 | Tampering | the guard reddening intermediate merges if extended too early | mitigate | this is the FINAL plan (depends_on 05-02..06); every referenced doc/live-test already exists, so the extension passes on this merge and only this merge; 05-01 deliberately did NOT touch the guard |
| T-05-07-04 | Information disclosure | COMPOSIO_API_KEY leaking | mitigate | resolved only inside execute() (04-03), never returned/logged; Xero reuses the existing key (no new secret surface) |

No high-severity threat open: the financial write is draft-only + approval-gated, and the guard extension lands atomically after its referenced artifacts exist.
</threat_model>

<verification>
- `make test` exits 0 (default lane green and creds-free, including the extended guard)
- `adapter_key_for` resolves both flipped Xero specs to the existing "composio" adapter (no new module)
- `pytest -m live tests/test_xero_live.py` skips cleanly without COMPOSIO_API_KEY
- The README links all six new docs + documents the five new env vars; the guard enumerates 10 docs / 10 live tests / 9 anchors / floor 12, all assertions green
</verification>

<success_criteria>
Xero reads through the existing composio adapter (default-lane resolution proven, opt-in live
read); xero.md + the README index + the atomically-consistent credential-docs guard extension
land in this final merge enforcing success criterion 4 — default suite green and creds-free,
guard never reddened on any intermediate merge.
</success_criteria>

<output>
Create `.planning/phases/05-reference-adapter-breadth/05-07-SUMMARY.md` when done
</output>
