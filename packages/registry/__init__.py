"""Routine + Release manifest registry.

Candidate routines proposed by the system land here as
`status=candidate`. Async review promotes them to `active` or archives
them. Hard delete is a separate, audited operation (archive-before-delete).
"""
